# Visual Component Specs v1.0

All components: vanilla JS + SVG/canvas, no libraries, embedded in the single-file player. Each is a factory function `renderVisual(componentId, mountEl, opts)` so a module invokes one by id. Theme-aware via CSS variables (dark/light). All respect `prefers-reduced-motion` (static final frame + caption). All copy uses prior-auth vocabulary.

## Interactive simulations
| ID | Name | Behavior | Modules |
|----|------|----------|---------|
| SIM-01 | PA Pipeline Map | Request cards flow intake → eligibility → clinical review → auto-approve/deny/manual queue. Recurring course map: each track highlights the stage it improves. Click a stage for its module list. | M-1.1, track intros |
| SIM-02 | Vibe vs Spec Race | Split screen; vibe agent sprints then thrashes (red rewrite flickers, rework counter climbs), spec agent starts slow, finishes clean. Scrub bar to replay; toggle "add auth requirement" to trigger the break. | M-2.2, L-1.2 |
| SIM-03 | Context Budget | Window bar with five segments (floor/memory/code/conversation/debris) against a 200k window; the floor and memory numbers are REAL measurements (recordings/m73-context-budget.txt: floor 25,514 tok; 36-line CLAUDE.md +1,025; 170-line bloat +4,609 → ~28 tok/line). Sliders: memory-file size, pasted logs, session length. Meters: used / headroom (warns under 40k) / carried-every-session. /compact drops debris + shrinks conversation but KEEPS memory (doc-verified: project-root CLAUDE.md is re-read and re-injected); /clear returns to floor+memory, not to zero. Deviation from v1.0 spec: no "cost-per-turn" meter — the measured cost gap is dominated by cache creation, so a per-turn cost figure would overclaim (see recording). | M-7.3, M-8.3 |
| SIM-04 | Threshold Slider | Drag AUTO_APPROVE_THRESHOLD; 60 request dots re-route live between approve/manual lanes. Dots scoring exactly the threshold blink and are the only ones the operator toggle (`>` vs `>=`) ever moves. Scores held as integer cents (exact comparisons, no float drift); distribution mirrors the seed's dense 0.80–0.89 band. Verdict line carries the real findings: the false "score 0.85 < 0.85" log under `>`, and the 10 seed rows the shipped code could not produce under `>=`. Built 2026-07-23 for M-5.2. | M-5.2, L-1.1 |
| SIM-05 | Subagent Org Chart | Built 2026-07-25 (M-9.4). DELEGATE mode: MAIN THREAD (gold bar stays **lean**, ~24%) spawns three read-scoped children (pa-reviewer / test-writer / security-scan), each with its **own** purple context bar filling independently and a `↩ returns: <summary>` line flowing back. NO SUBAGENTS toggle: children vanish, the parent bar **floods** (red, ~94%) — all that reading lands in the main context (the T-08 bill). Bars are **schematic** (the isolation MECHANISM, not a token measurement — stated in the code comment); the foot carries the one REAL measured result (recordings/m94-first-subagent.txt): the read-only pa-reviewer returned **1 violation + 3 passes** and could not edit even told to under acceptEdits. pa-reviewer child's return ("1 violation + 3 passes") is the real run; the other two children's returns are illustrative. Keyboard mode buttons; reduced-motion opens on the delegate end-state. **M-9.5 mounts it with `fanout:true`** (SIM-09-autotrace-style opts flag): header → "PARALLEL FAN-OUT · 3 LAYERS AT ONCE", children swap to the three REAL layer reviewers (controller-review → resolveManualReview has no endpoint; service-review → strict > at 0.85; repo-review → N+1 + dead batch finder), and the foot carries the measured concurrency numbers (recordings/m95-parallel-fanout.txt: api 185,890ms vs wall-clock 87,981ms ≈ 2.1×, parent output 1,415 tokens, $0.70 — faster+leaner not cheaper). M-9.4's plain mount is unchanged (harness asserts both). | M-9.4, M-9.5 |
| SIM-06 | Session Bill Meter | Replays the TWO REAL recorded runs (recordings/m82-context-pollution.txt) as a per-turn ticking meter on the same question with the same answer ("13"): paste = 3 turns to **$1.2525** with an annotated spike ("pasted 193KB (~48k tok) ↑") at turn 1; reference-by-path = 4 turns to **$0.1292**. Toggle re-runs the cheaper line; verdict panel lands the 9.7× ratio and the "spent a dollar to save three cents" point (the cheap run used MORE turns). Replay button; reduced-motion settles on the final frame. | M-8.2, L-4.1 |
| SIM-09 | API Architecture Map | Clickable four-layer map of priorauth-api built from source ground truth (controller endpoints, 4 services, 4 repos, Postgres+seed): click a node for responsibility + key file + where the course dissects it; TRACE button lights the POST /api/requests path in sequence (policy can deny at eligibility; the score can only approve or escalate); ApprovalService carries the 0.85 ⚑ (course-canon contradiction, fixed in M-5.2). Keyboard-operable; reset; reduced-motion = static traced map. Accepts opts.autotrace (M-3.4 mounts it mid-trace). | M-3.1, M-3.4 |
| SIM-08 | Tool-Fit Drill | 12 PA scenario cards, per-card pick GOLD/CYAN/PURPLE/ANY against the BLUEPRINT tool-fit table; reveal scores with per-card rationale citing the row. Two contested cards accept either defended pick; cold-onboarding honestly scores ANY; the CI card carries the real recorded run's numbers (49s, $0.89, 9 turns — recordings/m25-ci-run.txt). Keyboard-operable; reset; reduced-motion = pre-picked revealed board. | M-2.5 |
| SIM-07 | Rubric Board | 2×2 quadrant drill (x = lifespan, y = cost-of-wrong; quadrants vibe / plan-first / verify-twice / spec). Eight PA task cards placed click-to-place (keyboard-operable, no drag), then reveal scores the board with one-line rationales; the four T-02 cards carry their real recorded numbers (4.1-min vibe, rework 24, 4.5-min plan, 1.2-min throwaway script). Reset button; reduced-motion = pre-placed revealed board. | M-2.4, L-1.2 candidate |

## Animated diagrams (loop / scroll-triggered)
| ID | Name | Behavior | Modules |
|----|------|----------|---------|
| ANIM-07 | Ghost Text Editor | Fake editor types usePriorAuthForm; ghost suggestions appear, Tab-accept, next-edit suggestion hops to the related file tab. | M-4.1, M-4.2 |
| ANIM-08 | Debug Loop Orbit | Circular loop: run → red trace → read → patch → green. Loop radius tightens (fewer turns) as a "context quality" dial rises. | M-5.1 |
| ANIM-09 | Coverage vs Mutation Heatmap | Five rows = the real recorded campaign (recordings/m64-mutation-campaign.txt) against the shipped 31-test suite: each shows what executes it ("10 tests execute it") then, on RUN MUTATIONS, settles to CAUGHT or SURVIVED; score panel reports **2/5 = 40%** and names that all three survivors are the auto-approve decision. **Colour deviation from v1.0 spec (deliberate):** green = CAUGHT (good), red = SURVIVED (danger), matching the course's semantics elsewhere — the original spec said "flashes red = caught". Replay button; reduced-motion settles immediately. | M-6.4 |
| ANIM-10 | Memory Hierarchy | **Four** stacked cards (spec said three; managed-policy scope added after doc verification 2026-07-24): managed policy → user → project → directory, framed hospital policy → your habits → department policy → this procedure, with the real doc-verified paths. "Send a prompt" lights each card in load order and picks up one real line from each (PHI rule, show-the-test-command, **0.85 inclusive policy of record**, injected Clock), then states they arrive together. Foot carries the two counterintuitive facts: files are concatenated not overridden, and it's context not enforcement (hooks enforce). Reduced-motion settles immediately. | M-7.1 |
| ANIM-11 | Personal Delta Dashboard | Built 2026-07-25 (M-11.3). Three gauges animate from week-1 baseline → final (CYCLE TIME ↓, REWORK ↓ "watch this one", TESTS/FEATURE ↑), each with direction-of-good, the base→final numbers, and a % delta. Fills transition baseline→final on play/replay. **Example** data (cycle 90s→24s is the course's own real M-7.1 anchor; rework 8%→6% and tests 2→6 illustrative) — the caption states the student's own logged numbers replace them. A VERDICT panel drives the M-11.2 lesson: "a win — because the panel held; cycle time fell AND rework didn't rise; a speed gain with rising rework is the amplifier, not a win." Caption carries the improvement playbook order (instruction files → workflows → agents). Replay; reduced-motion sets final values immediately. Also feeds L-5.4. | M-11.3, L-5.4 |
| ANIM-12 | AI-DLC Ceremony Flow | Built 2026-07-25 (M-12.1); **`mgr:true` variant added 2026-07-30 for L-6.1** — same lifecycle read as a **decision map** rather than a slot map: the per-unit tags become who-decides / what-is-gated (`gate: YOU — "20% of what?"` · `gate: ops lead` · `gate: clinical + compliance` · `no gate — runs unattended` · `gate: automated, fires without you` · `gate: data boundary, pre-approved`) and the caption swaps to the planning consequences (Epics→Units of Work so you size decisions not story points; the sprint boundary stops being the meaningful unit; standup inverts to "what is blocked on a decision, and whose?"; **your decision latency is the constraint, not your capacity**). Precedent: SIM-05's `fanout` flag (M-9.5). **M-12.1's default mount is byte-identical and harness-asserted.** Base build: Built 2026-07-25 (M-12.1). Staged flow: INTENT card ("Using AI-DLC: cut the manual-review queue 20%") → INCEPTION·Mob Elaboration (explodes into 3 units of work, each tagged with the course skill that powers it: Plan Mode T-03 / spec T-02 / constitution T-07) → CONSTRUCTION·Mob Construction (3 build rows: subagents/fan-out T-09 / gated tests+hooks T-06,T-10 / payer MCP T-10) → CONVERGE→PR (green, M-10.5) → Operations "future" + measure (T-11). Each node carries its human gate (states/validates/clarifies/approves). AI-DLC doc-verified 2026-07-25 (AWS DevOps blog Jul+Nov 2025, awslabs/aidlc-workflows): three phases, "Humans decide and validate, AI plans and executes", tool-agnostic. Caption: method not tool. Nodes+units stage in + loop; replay; reduced-motion static. Shared M-12.1–12.4 + L-6.1. | M-12.1–12.4, L-6.1 |
| ANIM-14 | One Repo, Three Constitutions | Bare priorauth-api card ("no instruction files") gets scan-swept; three instruction files materialize (CLAUDE.md gold, copilot-instructions.md cyan, .cursor/rules purple) with generated lines typing in, then a "YOU SIGN" threshold line stamped on each; a prompt card confirms the signed rule gets cited. Teaches: generators document what code does, owners sign what it should do. | M-1.4 |
| SIM-44 | Five Ways a Ceremony Becomes Theatre | Built 2026-07-30 (L-6.2, leadership). Anti-patterns **ordered by damage**, opening with a dashed strip carrying **AWS's two mob definitions verbatim as the standard being failed** ("entire team actively validates AI's questions and proposals" / "team provides clarification on technical decisions and architectural choices in real time" — Jul-2025 post per M-12.1's hedge, since the Nov material only name-checks the rituals), framed as: both definitions are about *humans supplying judgment*, so a ceremony where nobody validates has kept the format and lost the function. Five click-to-expand cards, each with four fields — **WHAT IT LOOKS LIKE** (recognizable from the room) / **WHY IT'S TEMPTING** (always momentum or seniority) / **WHAT IT COSTS** / green **→ THE FACILITATOR MOVE** as the payload: **Routing around a refusal** (red border + `MOST DAMAGING` flag — "you do not just ship one ungoverned change, you teach the entire system that gates are negotiable", with the live run's real escalation as the move: clinical lead / compliance + legal / CMS + state rules / disparate-impact / shadow dry-run) · **Answering to keep momentum** (open decision → unrecorded assumption; make "we don't know" recordable) · **Rubber-stamping the decomposition** ("if you have never rejected or reshaped a unit, you are not validating; you are attending") · **Taking the keyboard** (L-1.1's watch-don't-type at room scale; "your leverage is not your hands — it is that you can *decide*") · **Status-meeting drift** (the one that catches good teams because attendance and output still look healthy, so "nothing signals the failure"). Green panel = what good looks like in four moves (prepare the intent not the solution; bring the decision-owners — "that scheduling work *is* the facilitation"; record unknowns with owner + date; **end with decisions recorded, not tasks assigned**). Foot carries two honest notes: the five anti-patterns are **this course's synthesis** from the live runs and its own governance material, **not published AI-DLC methodology** (same voice discipline as L-5.3's acceptance-rate reading), and the ceremonies are young with Operations still "future" → run them as experiments against your own baseline (L-2.1), keeping the durable core: state the intent, decompose into gated units, protect the questions, honour the refusals. Click-to-expand; reduced-motion opens all. | L-6.2 |
| SIM-43 | The Queue Dashboard | Built 2026-07-30 (L-5.4, leadership, **closes L-05**). A **worked** four-zone dashboard for the course's own running initiative rather than an abstract template — and the only SIM in the leadership path with **no click interaction**, deliberately: it is meant to read as a finished artifact you could put on a wall. Intent banner states the promise ("cut the manual-review queue by 20%") and the rule that makes it a dashboard rather than decoration ("every row carries a decision agreed *before* the number was collected"). Four zones with colored left borders: **OUTCOME** (amber, "what you promised — and it lags") carrying the **MEASURED BASELINE** tag — 179 requests in manual review, 35.8% of intake, 23.5% in [0.75, 0.85) — plus the unresolved *"20% of what?"* denominator flagged as something to settle before the first review; **LEADING** (green, "moves weeks earlier — this is where you act") — drain vs arrival rate, code churn on tiered work, gate failures — each with its **IF → THEN**; **GUARDRAIL** (red, "must NOT move — the quality condition, operationalized") — false-approve rate at the threshold (carrying the measured finding that threshold-only doesn't reach −20% until ~0.75, "a decision about false approvals, not an engineering task"), change fail rate, escaped defects; **REFUSED** (dim, struck-through metric names) — the four L-5.3 vanity metrics each with a one-line reason, "written down so it cannot creep back". **Sourcing discipline: every figure is measured** (m122-mob-elaboration.txt live run) and tagged; **no threshold bound is invented** — guardrail text says "the bound you agreed" rather than fabricating a number, per the ANIM-11 illustrative-data precedent. Foot: read the shape not the rows (lagging is comfortable, leading is uncomfortable *because* it is actionable), the three disciplines (owner / cadence where action is possible / never ranked), and the track's closing thought — **a dashboard is not a reporting instrument, it is an intervention**, exactly like L-5.2's survey: what you display is what gets optimized. Static by design; no reduced-motion branch needed. | L-5.4 |
| SIM-42 | Not That — This | Built 2026-07-29 (L-5.3, leadership). **Deliberately NOT a re-skin of SIM-21** (M-11.2's engineer-path keep/refuse *sorting* drill) — the leadership problem is different: an engineer can simply not collect LOC, whereas a director gets *asked* for these by someone senior and needs an answer that isn't obstructive. Same call as L-1.2 (leadership-native visual over lifting an engineer drill). Thesis banner: "refusing a metric is a political act, not a technical one… these are not requests from people who do not understand measurement." Then **4 request cards**, each titled with the question *as it is actually asked* ("What percentage of our code is AI-written now?", "What's our acceptance rate?", "How many of our engineers are using AI?", "How many seats are we using?") and click-to-expand into four labelled fields: **WHO ASKS — AND WHAT THEY ACTUALLY WANT** / **WHAT IT MEASURES** / **WHAT BREAKS WHEN YOU REWARD IT** / and, in a green-bordered sub-block as the payload, **→ OFFER INSTEAD**. Carries the course's own evidence: the M-2.2 break run "would have looked *excellent* on this metric" (220 wrong lines, 25/25 green, invented endpoint); DORA's wedge verbatim for usage% (90% / 80%+ / negative stability relationship); L-2.3's intake framing for seats; substitutes drawn from L-4.3 (cost + delta + outcome), L-3.3 (rework/escaped defects), L-2.3 (funnel progression) and L-4.2 (active days). **Acceptance-rate framing is stated in the course's own voice with its status disclosed** — "this is the course's reading, not a quotation from anyone authoritative" — per M-11.2's recorded hedge; a validator assertion prevents it ever being upgraded to a quotation. Amber **inversion strip** = L-2.4: a high usage number from an over-relier is "a risk flag, not a win… they *point the wrong way*". Foot = **the optimization test** ("what happens when someone optimizes it?" — framed as the most persuasive sentence to say aloud) + the **five-appearance Goodhart lineage** named as one law rather than five warnings (L-3.3 churn → L-4.2 cost → L-5.1 DORA ranking → L-5.2 survey scores → these). Click-to-expand; reduced-motion opens all. | L-5.3 |
| SIM-41 | The Asking Problem | Built 2026-07-29 (L-5.2, leadership). SPACE's five dimensions sorted **by how you actually get them**, which is the leadership cut rather than the usual list. Thesis strip carries SPACE's primary-verified founding claim verbatim (productivity "cannot be measured by a single metric or dimension"). Then 5 click-to-expand dimension rows, each with a right-hand **how-you-get-it pill** + matching left border: **ASK ONLY** (green) — Satisfaction & well-being ("no system telemetry for this… the longest lag between the problem starting and the resignation landing") and Communication & collaboration (system proxies are "weak stand-ins" — fast reviews can mean a healthy team or one person rubber-stamping, "and the number looks identical either way"); **INSTRUMENT** (amber) — Performance ("why reporting only this looks responsible and tells you almost nothing new") and **Activity** carrying an `OVER-COLLECTED` flag; **ASK · SOME PROXY** — Efficiency & flow. A second banner states the pattern explicitly: **the two dimensions you can fully instrument are the two that say least about the humans**. Below, a red-bordered **rules panel** headed by the module's sharpest idea — *"a survey is an intervention, not just an instrument… ask people how they are and then visibly do nothing, and morale ends up lower than if you had never asked"* — with 5 numbered rules (short / cadence you can act between / **close the loop visibly, owner named before the first send** / never a ranking / honest anonymity, "on a team of six 'anonymous' is a claim nobody believes"). Foot = **DX Core 4 as the assembly, not a rival**: "encapsulates DORA, SPACE, and DevEx" across speed/effectiveness/quality/business impact, its balance warning ("changes to one dimension, such as speed, may negatively affect others") as L-5.1's divergence at framework level, its by-design refusal of a single headline number, and the 30%-little-or-no-trust figure as the worked example of a signal only asking reveals. **M-11.2's hedges carried forward exactly:** the sharper SPACE "(or metric!)" / "never in isolation" lines are ACM-secondary → the module leans on the primary abstract quote and **paraphrases** the in-isolation caution; "DiffDelta" is not a Core 4 term and is not used. Click-to-expand; reduced-motion opens all; pill stacks <560px. | L-5.2 |
| SIM-40 | The Five, Split | Built 2026-07-29 (L-5.1, leadership, **opens L-05**). The current five DORA metrics under **DORA's own headings**, grouping re-confirmed against dora.dev primary 2026-07-29 (l51-dora-org-level.txt). Opens with a red **stale strip** — "if your dashboard says 'the four keys' and 'MTTR', it is a 2023 dashboard" + why it matters at org scale (a stale definition propagates into every dashboard, QBR and cross-team comparison). Then 2 columns (stack <560px): **THROUGHPUT · 3** (green, "higher is better") = change lead time / deployment frequency / **failed deployment recovery time** flagged `RENAMED`; **INSTABILITY · 2** (red, "LOWER is better — note the heading") = change fail rate / **deployment rework rate** flagged `NEW`. Each metric is click-to-expand with its **verbatim** definition plus the org-level trap it carries: lead time's start point is *committed to version control* (teams starting at ticket creation aren't measuring it), "deployment" must mean the same thing everywhere before any comparison means anything, and — the sharp one — **recovery time sits under THROUGHPUT, not stability**, so the classic four-keys mental model is wrong twice (renamed *and* re-grouped, and only the rename is easy to spot). Rework rate's card distinguishes it from L-3.3's *code churn* — same word, different altitude. Center split panel carries the 2025 finding as opposing arrows: positive relationship with throughput ▲ vs adoption "does continue to have a negative relationship with software delivery stability" ▼, with the consequence stated — a throughput-only dashboard "will look like a triumph while the right column degrades underneath it." Foot = the two org-level disciplines (**distribution not average** — an org mean describes no team and hides the squads in trouble; **never rank** — same Goodhart failure as L-3.3 churn and L-4.2 cost, widest blast radius) + DORA's verbatim control-systems mechanism as the actual response, mapped to T-06 / T-10 / L-3.3. Click-to-expand per metric; reduced-motion opens all. | L-5.1 |
| SIM-39 | The ROI Submission | Built 2026-07-29 (L-4.3, leadership, **closes L-04**). The module's analogy made literal — the ROI deck as a prior-auth request, adjudicated line by line. Thesis banner ("the cost side is already settled — you can state it to three bands; every ROI deck fails on the other half… read these the way an approver would") over **6 click-to-expand submitted claims**, each with a verdict pill + left border: **3 DENIED (red)** — the borrowed vendor number ("a borrowed number does not just fail on its own, it contaminates the credible claims around it"), the self-report substitution (carrying the verified DORA 2025 wedge: 80%+ believe productivity increased *while* adoption still showed a negative relationship with delivery stability — m112-frameworks.txt:31), and **hours × loaded rate** ("those hours never left the payroll… cost *shifted*, not cost *avoided*"; the honest version is capacity redeployed with a named deliverable) — and **3 APPROVED (green)** — own-task-class 90s→24s with small-n and confounds named ("naming the confounds is what makes the number survive scrutiny"), rework→named incidents avoided (L-3.3's churn radar becoming a financial instrument), and the queue −20% business outcome baselined *before* the work started (L-2.1). Foot: what separates the halves (own instrumentation, own work, hedges stated) + the three things missing from most submissions — **show the dip** (J-curve, L-2.3), **show the quality condition** (speed with rising churn is the amplifier, M-11.3), and **say how the number will be checked against you later**, promising only what the attribution grain proves (per-user/team billing-grade, per-agent not — L-4.1). Closes: "an ROI narrative is not a one-time deck; it is a claim you will be held to." **Reframe-only — no new external claims by design** (a module about refusing borrowed numbers doesn't go shopping for them); DORA figures re-read at source, everything else from the course's own measured runs. Click-to-expand; reduced-motion opens all; verdict pill stacks <560px. | L-4.3 |
| SIM-38 | The Budget Build | Built 2026-07-29 (L-4.2, leadership). A team budget as **three bands, not one number**, worked against an 8-engineer PA squad: 3 click-to-expand band rows with left-border + amount color — **STEP · seats** (green, "flat", capitation, *cutting seats removes people not consumption*) · **SLOPE · usage** (amber, "~$13/day", carrying Anthropic's verbatim prior "$13 per developer per active day and $150–250 per developer per month… below $30 per active day for 90% of users" + their pilot-then-baseline method = L-2.1 + M-11.1 in the vendor's words, and the rule that the denominator is **active days, not headcount**) · **LUMPY · policy-driven** (red, "the surprise" — review-on-every-push × $15–25, headless automation, scheduled tasks firing while idle, agent teams at vendor-stated ~7×; "every one of these is something you switched on in L-03"). Italic note gives the worked shape (8 seats + 8 × ~18 active days × ~$13 ≈ $1.9k + lumpy) explicitly as a starting shape, not a quote. Then **THE MODEL FORMULARY** — a 2-column routing rule on *"Can you check the answer?"*: **✓ YES → small model fine** (green; grind, mechanical subagent volume; the measured 1.8× and why — same four turns, carried context doesn't get cheaper) vs **✗ NO → big model required** (red; the measured $0.0368-and-confidently-wrong threshold answer, "fluency, not stupidity", escalate on *task type* never on confidence). Foot carries the arithmetic that makes the module: the **whole current lineup spans ~10×** (realistic gap ~5×) against **9.7× measured for context delivery and 21.1× for turns on the same model**, plus 0.22% cheap-model share — so **model tier is the narrowest lever** — and introduces the second dial, **effort levels**, which move thinking spend without changing whose judgment you get. Model prices and the $13 prior dated 2026-07-29 with verify-current instructions (l42-budgeting-a-team.txt); measured ratios re-read from m81/m82/m84/m95. Click-to-expand; reduced-motion opens all; bands and formulary columns stack <560px. | L-4.2 |
| SIM-37 | The Two Meters | Built 2026-07-29 (L-4.1, leadership, **opens L-04**). Every agent bill is now a hybrid, so the visual opens with two side-by-side meter cards (grid 1fr/1fr, stacks <560px): **SEATS — A STEP · CAPITATION** (green; forecastable from headcount, "it is not a usage control") vs **USAGE — A SLOPE · FEE-FOR-SERVICE** (amber; metered on input, output *and cached* tokens, invisible until the invoice). Then **6 click-to-expand slope drivers ranked by the course's OWN measured ratios**, each showing a large magnitude figure: context delivery **9.7×** ($1.2525 paste vs $0.1292 by path; cache creation 103,192 vs 6,465; the cheap run used *more* turns) · turn count **21.1×** ($0.0134 → $0.2834, ~$0.028/turn) · carried context **+3,584** tokens/session · scoping **19%** of baseline (carrying the disproved re-anchoring hypothesis: "re-anchoring buys accuracy, scoping buys economy") · parallelism **pays each** (2.1× wall-clock for $0.6989; vendor-stated ~7× tokens for agent teams) · and **model choice ranked LAST on purpose at 0.22%**, greyed via `.low` (1.8× on grind, cheap *and wrong* on judgment). Foot block = **the three attribution grains** as a 4-row list: per user/team ✓ BILLING-GRADE · per session/model ✓ PRECISE · per subagent/skill/MCP ⚠ DIAGNOSTIC ONLY (approximate, local, list-rate) · "what did that agent cost" as an invoiced number ✗ NOT A THING. Closing foot: read the ranking not the prices (Copilot replaced premium requests with AI credits on 2026-06-01, mid-course), and **governance settings are budget settings** (review-on-every-push; sign-in method decides the billing path as well as the data policy, L-3.4). Vendor pricing verified 2026-07-29; measured ratios re-read from m81/m82/m83/m84/m95 (l41-seats-vs-tokens.txt). Click-to-expand; reduced-motion opens all. | L-4.1 |
| SIM-36 | Reconstruct the Change | Built 2026-07-28 (L-3.4, leadership, **closes L-03**). An auditor's reconstruction run against the course's own highest-stakes change: a case card showing the eligibility threshold diff (`> 0.85` → `>= 0.85`, ApprovalService line 99 / M-12.3's UoW-3) framed as "one character, a medical-necessity policy change in disguise — blast radius is not proportional to diff size". Then **7 click-to-expand auditor questions**, each with a status pill + left border: What changed → **CAPTURED** (green, the only free one); Who authorized it / On what evidence / Which gates ran / Where did the data go / Where did the code come from → **ONLY IF CONFIGURED** (amber); **What was the agent told, and shown → NOT CAPTURED** (red, carrying GitHub's verbatim "does not include client session data, such as the prompts a user sends to Copilot locally", with the versioned instruction file as partial recovery). Answers carry the verified vendor facts: per-login not per-tool ("those sessions are not covered" + forceLoginMethod/forceLoginOrgUUID, Cursor Allowed Team IDs, managed seats), duplication detection as "a filter, not a chain of custody" ("while the probability is low, Copilot may generate code suggestions that match code in the training set"), and a gate *refusing* as durable evidence a control was live (M-10.2). Green payoff panel = **M-12.3's real refusal as the authority record** (crosses into "a change of medical-necessity policy… needs sign-off from people who can own that, not a passing test suite"; clinical lead + compliance/legal + disparate-impact + shadow dry-run) — "human as approver, not typist" reframed as an *audit property*. Foot: read the column — one row free, one impossible, the rest exist only because someone decided in advance; audit depth follows blast radius (L-1.2); contractual answers (training/retention/BAA) go in writing. **Italic disclaimer line: teaching aid, not legal advice; vendor statements verified 2026-07-28 and change over time.** All vendor claims verified 2026-07-28 against primary docs (l34-ip-compliance-audit.txt). Click-to-expand; reduced-motion opens all; pill stacks <560px. | L-3.4 |
| SIM-35 | Advisory or Gate? | Built 2026-07-28 (L-3.3, leadership). The review stack read by the only question that matters to a leader: **which layers actually say no.** Thesis banner ("the agent changed the arrival rate, not the risk tier; review capacity does not scale by adding eyeballs") over 5 click-to-expand layer rows, each with a stage label, mechanism chips (tool-colored where tool-specific) and an **ADVISORY (amber) / GATE (green)** verdict pill + matching left border: IN SESSION security-guidance plugin → ADVISORY ("None of the layers block writes or commits"; independence designed in — "does not ask the same Claude instance that wrote the code to grade itself"); PRE-COMMIT hook+suite → **GATE** (the real M-10.2 run: Failures: 1 → commit denied, `--no-verify` won't dodge it); ON THE PR → **ADVISORY, unanimous across all three** (gold Claude Code Code Review "don't approve or block your PR" / neutral check run · cyan Copilot "Supplement Copilot's feedback with a human review" · purple Cursor Bugbot findings "default to `neutral`"); HUMAN tiered by blast radius → GATE (the only layer that signs; ask for the defense, M-12.4); IN CI → GATE you build (gh+jq severity parse). Red warning strip = the real M-2.2 green-but-wrong run (25/25 green, invented endpoint, "unavailable" for every user) — mechanical layers can't catch a failure of *intent*. Foot = **the rework radar**: 3 signals ordered by how fast they move (code churn *leading*, amber-flagged → deployment rework rate → escaped defects), keeping the **two different "reworks"** explicitly distinct (GitClear code churn, cited as their analysis/correlation≠causation, vs DORA's new 5th metric deployment rework rate), plus read-the-panel (throughput↑ + churn↑ = amplifier) and team-level-only (L-5.3). All tool claims verified 2026-07-28 against primary docs (l33-reviewing-ai-code.txt); metric definitions re-read from m111/m112 before quoting. Click-to-expand; reduced-motion opens all; verdict pill and signal rows stack <560px. | L-3.3 |
| SIM-34 | The Connection Register | Built 2026-07-28 (L-3.2, leadership). Data boundaries as a register a leader signs. Thesis banner ("every connection is a data-access decision, not a plugin install"; ".mcp.json is a proposal, not an approval") over a CONNECTOR ⋮ BOUNDARY ⋮ VERDICT legend and 4 click-to-expand connector rows, each = name + a dashed **boundary rule** (`.s34cross`, solid red when denied) carrying a direction glyph (→ read / ↔ both / ⊘ stopped) and what it reaches, plus data-class chips and a verdict pill (left-border + pill color green/amber/red): payer-policy lookup (INTERNAL·READ-ONLY·NO PHI, the course's own M-10.3 server) → APPROVE; issue/PR connector (INTERNAL·READ+WRITE·UNTRUSTED CONTENT) → APPROVE, SCOPED; PA production DB (PHI·READ+WRITE·REGULATED) → DENY, REPLICA ONLY (minimum necessary); unreviewed community server (UNKNOWN PROVENANCE·NETWORK EGRESS) → STOPPED AT THE LINE (carries the three vendors' own "nobody audits these" quotes + serverName "is not a security control"). Foot: the ENFORCED org layer as tool-colored chips (gold managed-mcp.json/allowedMcpServers+allowManagedMcpServersOnly · cyan chat.mcp.access "registry"/org "Registry only" · purple Cursor dashboard MCP Configuration by command·URL pattern), then the two gaps — the lethal trifecta (attributed to Simon Willison 2025, NOT a vendor) and Copilot content exclusion "not supported in Edit and Agent modes". All tool claims verified 2026-07-28 against primary docs (l32-mcp-data-boundaries.txt). Click-to-expand; reduced-motion opens all; verdict pill drops below the row <560px. | L-3.2 |
| SIM-33 | The Permission Dial | Built 2026-07-27 (L-3.1, leadership, opens L-03). The same guarded→open mode spectrum across all three tools, in 3 click-to-expand posture tiers with left-border color (green/amber/red) and the VERIFIED mode names as tool-colored chips (gold Claude Code / cyan Copilot / purple Cursor, 3 per tier): t1 Ask-first (Manual / Default Approvals / Auto-review — the safe default), t2 Auto-run-with-rules (Edit-automatically·Auto / terminal auto-approve rules / Allowlist — reveal carries "deny beats allow" + Cursor's "best-effort, not a security boundary" verbatim), t3 No-guardrails (Bypass permissions / Bypass Approvals·Autopilot / Run Everything — containers only, gate via ChatToolsAutoApprove / disableBypassPermissionsMode). Thesis banner: safe-by-default is a posture, sandbox is the boundary. Foot: the four levers (default mode / allow-deny rules / sandbox=real OS-enforced boundary Seatbelt·bubblewrap·WSL2 / enforced org policy) + the native-Windows→WSL2 sandbox caveat. ALL mode names/settings verified 2026-07-27 against primary docs (l31-permission-architecture.txt). Click-to-expand; reduced-motion opens all. | L-3.1 |
| SIM-32 | The Two Resistances | Built 2026-07-27 (L-2.4, leadership, closes L-02). Resistance as two opposite failure modes with mirror-image fixes, unified by a center axis banner quoting the verified DORA 2025 amplifier ("AI amplifies what's already there" — m112-frameworks.txt:30; "same amplifier, opposite guardrails"). Two side-by-side click-to-expand cards (grid 1fr/1fr, stacks <560px): Senior skepticism (amber top-border, "often rational") — signal always visible + reveal wrong(dismiss/mandate, red) / right(recruit onto gates & review policy L-03, green) / becomes(most credible champion, green); Junior over-reliance (red top-border, "the dangerous one", silent/looks-like-productivity, the 0.86 threshold bug) — wrong(reward velocity/AI-usage%) / right(review discipline: explain-the-diff, L-1.2 risk lens, gates M-10.2, T-06 + pair) / becomes(real judgment). Foot: high AI-usage% from an over-relier is a risk flag not a win (L-2.3/L-5.3); reward outcome+defensibility, never usage; closes L-02. Reframe-only. Click-to-expand; reduced-motion opens both. | L-2.4 |
| SIM-31 | The Enablement Funnel | Built 2026-07-27 (L-2.3, leadership). 2→200 as a managed funnel: a dashed banner ("measure by progression + business outcome, not seat count — counting seats is counting intake") over 4 narrowing stage bars (inline width% taper 100/72/48/26), Aware→Tried→Habitual→Champion, each with a rough count (≈200/124/56/12) and click-to-expand detail = intervention + honest metric + the vanity metric to refuse there (red). Three leak markers between stages: ACTIVATION (Aware→Tried, biggest), RETENTION (Tried→Habitual, the J-curve dip), LEADERSHIP (Habitual→Champion). Foot: expect the J-curve dip (DORA stability tension), find the stall like the review queue, the champion loop is the 2→200 engine, L-05 formalizes frameworks. Reframe-only (pilot→champion→cadence arc as a funnel; previews L-05 honest measurement + L-5.3 vanity list). Click-to-expand stages; reduced-motion opens all. | L-2.3 |
| SIM-30 | The Cadence Board | Built 2026-07-27 (L-2.2, leadership). Champions & cadence as the machinery that spreads adoption. Header stat banner: the verified DORA 2025 primary figures (30% little-or-no trust in AI-generated code, even as 90% use it and 80%+ feel more productive — m112-frameworks.txt, dora.dev 2026-07-25) framing trust (not capability) as the bottleneck. Three click-to-flip mechanism cards, each showing what it builds and — on tap — the anti-pattern it replaces (red ✗): Office hours (pair on real stuck work live / not a one-off course), Show-the-diff demos (show the review + a caught agent failure on real hard code, M-10.2 / not greenfield speed theatre), Internal course drops (package the portable layer L-1.3 for self-serve / not a hero champion as permanent help-desk). Foot: pull>push (unprompted attendance = trust forming; mandate = vanity usage, L-2.4/L-5.3), champion-becomes-unnecessary (teach team #2→#3), cadence>big-bang = the 2→200 multiplier. Reuses verified facts only. Click-to-flip cards; reduced-motion opens all. | L-2.2 |
| SIM-29 | The Pilot Selection Board | Built 2026-07-27 (L-2.1, leadership, opens L-02). Pilot design as a manager's selection call: a dashed caveat banner ("a pilot is an experiment, not a rollout — one squad, one repo, one baselined metric, a time box + decision gate") over 3 click-to-expand candidate-squad rows, each scored on 4 criteria chips (PAIN·FIT·SPEED·VOICE = real measured pain / AI-fit code+tests / release cadence / honest champion) with a verdict pill: Intake squad → THE PILOT (green, all ✓); "Eager volunteers" greenfield → TRAP·TOO LOW-STAKES (red; no downstream dependency, no agreed metric); Legacy claims-core monolith → TRAP·TOO HARD (red; test-less, quarterly, skeptical). Marks use the established ✓/~/✗ green/amber/red tokens (#3E9A6B/#c99a3a/#d0705a, light-theme variants). Foot = the three rules (baseline-first T-11, one agreed business metric never a vanity count, time-box+gate scale/adjust/stop) + the real deliverable (measured delta + portable layer L-1.3 + champion for L-2.2). No new external claims. Click-to-expand rows; reduced-motion opens all. | L-2.1 |
| SIM-28 | The Fleet Decision | Built 2026-07-26 (L-1.4, leadership, closes L-01). The tool-fit table read as a manager's procurement map: a dashed caveat banner ("editorial defaults, not laws — overturn with a bake-off, M-2.5") over 4 click-to-expand team-archetype rows → the default tool (color-coded gold/cyan/purple): UI-heavy squad→Cursor, platform/backend→Claude Code, GitHub-native org→Copilot, automation/CI→Claude Code, each with its why. Foot = the two manager questions (which-tool-per-use-case + standardize-vs-allow-choice = one-governance-surface vs per-team-fit) + the convergence fact (all three ship skills/hooks/subagents/MCP, AI-DLC tool-agnostic → the portable layer/constitution+method matters more than the badge, switching costs less than the debate). Reuses verified capability claims only. Click-to-expand rows; reduced-motion opens all. | L-1.4 |
| SIM-27 | The Constitution as an Asset | Built 2026-07-26 (L-1.3, leadership). Three click-to-expand cards reframing T-07's verified numbers as a manager's balance sheet: ASSET (green) — the real M-7.1 delta (36 lines → 90s→24s AND recommendation flipped from bug→policy; "appreciates, funds first"); LIABILITY (red) — M-7.3 bloat rent (+3,584 tok/session for 170 lines) + M-7.5 stale-fail-soft = load-bearing misinformation ("owner + review cadence"); GOVERNANCE (gold) — M-9.6 scope repo→user→org→enterprise nearest-wins + "learnings discovered, rules signed" ("decide what the org file enforces by default"). Foot: cheapest highest-leverage lever, playbook = instruction files→workflows→agents (M-11.3), quality predicts ROI. Reuses only verified engineer-path facts. Click-to-expand cards; reduced-motion opens all. | L-1.3 |
| SIM-26 | The AI-Code Risk Register | Built 2026-07-26 (L-1.2, leadership). The vibe/spec rubric read as a manager's risk register: four click-to-expand tiers by blast-radius×lifespan, each setting the required control — DISPOSABLE (green) → VIBE OK; INTERNAL/LOW-STAKES (cyan) → vibe + quick review; PRODUCTION FEATURE (gold) → SPEC + tests + review (M-2.3); HIGH-BLAST (red, billing/eligibility/PHI/decision path) → SPEC + review + human SIGN-OFF (M-1.6/M-12.3). A red cautionary strip carries the REAL M-2.2 fact (a vibe widget passed 25/25 tests, still "unavailable for every user" against a non-existent endpoint — green CI ≠ safe). Foot: classify by blast radius not provenance; AI changes VOLUME not tier ("amplifies what's already there", DORA-2025); police DRIFT (a spike promoted to prod unre-tiered). Reuses only verified facts (no new external claims). Click-to-expand tiers; reduced-motion opens all. | L-1.2 |
| SIM-25 | Migration Read-out | Built 2026-07-26 (M-12.5, the final engineer module). Carries the REAL spec-first migration slice (recordings/m125-domain-migration.txt, 480s, verified vs the clone): a VOCABULARY SWAP card (CPT `\d{5}`→NDC `\d{11}`, Payer→Pbm git-rename, per-payer coverage→per-PBM formulary tiers, +days-supply `DS-001`, +tier-3-flag, threshold UNCHANGED), a metrics strip (36 tests green, one red→green, +213/−137 across 16 files, 6m15s), and a click-to-expand RIPPLE map in three honest tiers — BLOCKING (the 1081-line seed.sql that IS the schema under ddl-auto=validate + un-versioned API break = the real cost centre), NON-BLOCKING (Claim vocab/README/reason catalog/dup bound), 2ND REPO (inferred not verified — not in workspace), PLANT (caught line 99 `>` vs inclusive, left it per spec, flagged a full port carries it forward). Foot: renamed lines are the cheap half (~60/40, compiler-guided); measure the real cost; a faithful migration carries your bugs forward. Reduced-motion opens the ripple. | M-12.5 |
| SIM-24 | Capstone Read-out | Built 2026-07-26 (M-12.4). **Deviation logged:** the v1.0 spec named ANIM-12 for M-12.4's metrics read-out; shipped a dedicated SIM-24 carrying the REAL full-stack capstone numbers instead (ANIM-12 stays the shared ceremony flow, referenced in-body). Carries the live run (recordings/m124-capstone.txt, verified vs both clones): feature card + two side cards — API `GET /queue-depth` + ReviewQueueService, 41 tests/0 fail, +96/−2, 194s; WEB getQueueDepth() + QueueDepthBadge + RTL test, 23 pass (+3), tsc+oxlint clean, +85, 256s — a metrics strip (cycle ~7.5min full-stack, +7 tests green, rework ~0, plants intact = quality held per M-11.3), and a click-to-expand "defend the diff" panel with the agent's own honest notes (stale badge/poll follow-up, 2 npm vulns flagged, plants left alone). Foot: everything composed (M-12.2→12.3→here); ship 3 ways, ceremony tool-agnostic (M-12.1). Reduced-motion opens the defend panel. | M-12.4 |
| SIM-23 | Mob Construction | Built 2026-07-26 (M-12.3). Build-status board carrying the REAL Construction run (recordings/m123-mob-construction.txt, 137s, verified vs the clone: branch feat/cut-queue, line 99 `>=`, 35 tests green): UoW-3 (restore `>=` + constant-referenced boundary test) BUILT ✓, UoW-2 (`POST /{id}/resolve`, `@NotNull`→400-not-silent-deny) BUILT ✓, UoW-5 (claim history as decision input) **STOPPED ✋** — click to reveal the agent's refusal reasoning (medical-necessity policy; clinical + compliance sign-off; disparate-impact risk; a shadow dry-run to produce the unknown yield; "it's legitimate for the answer to be that the rule doesn't earn its risk"). Result pills: 35 tests/0 fail (+4), diff +49/−1, commit 45d5e36, PR-ready. Foot: "the agent proposes, the human approves" — builds the mechanism, refuses the policy decision (M-1.6 at ceremony scale). Click-to-expand UoW-5; reduced-motion opens it. | M-12.3 |
| SIM-22 | Mob Elaboration | Built 2026-07-25 (M-12.2). Carries the REAL read-only Inception run (recordings/m122-mob-elaboration.txt, 398s, git clean): INTENT card → a "reshaped first" line (no queue entity / no baseline / nothing drains the queue via API; seed baseline 179 = 35.8%) → three senior CLARIFYING QUESTIONS ("20% of what?", false-approve rate, which threshold contract) → six UNITS OF WORK, each a clickable row with a human-gate badge (BUILD green / DECIDE gold / HARD GATE red) that reveals "why it cuts the queue" — grounded in real levers incl. BUG-API-01 (line 99 misroute), BUG-API-03 (batch N+1), and the missing resolve endpoint (M-3.3); instrument-first echoes T-11. Foot = the honest read (the intent is a risk decision, not a coding task — the only known-yield lever trades false-approvals). AI-DLC doc-verified 2026-07-25. Click-to-expand units; reduced-motion opens all. | M-12.2 |
| SIM-21 | Measure or Refuse | Built 2026-07-25 (M-11.2). SIM-18-style drill: sort 10 metrics into KEEP (balanced-panel signal) vs REFUSE (adoption/activity mistaken for value); reveal scores + per-card rationale with the framework each keep belongs to. KEEPs: the 5 DORA metrics (change fail rate, change lead time, deployment frequency, failed-deployment-recovery-time [renamed from MTTR], + implicitly deployment rework rate), SPACE developer satisfaction, M-11.1's rework/escaped-defects. REFUSEs: LOC, AI acceptance rate, % AI-written, commits/PRs-per-day. Foot carries the current DORA finding (AI helps throughput but still hurts stability; "AI doesn't fix a team; it amplifies what's already there"). Doc-verified 2026-07-25 (dora.dev five metrics + de-acronym + report rename; 2025 DORA/Google Cloud AI finding; SPACE "cannot be measured by a single metric or dimension"; DX Core 4 four dimensions). Keyboard picks; reduced-motion pre-sorted + revealed. | M-11.2 |
| SIM-20 | The Baseline Ledger | Built 2026-07-25 (M-11.1). Three clickable metric tiles split SPEED (cyan) vs QUALITY (gold): CYCLE TIME, ESCAPED DEFECTS, REWORK/CHURN. Click a tile → an IS / IS-NOT / WHY panel. Definitions doc-verified 2026-07-25: DORA lead-time ("committed → deployed in production"), LinearB cycle-time, GitClear churn ("reverted or substantially revised within ~2 weeks", ≈3.6% 2021→5.5% 2023 rising with AI — attributed as GitClear's analysis, not law), Plandek escaped-defect, Oxford-CEBM Hawthorne effect. Load-bearing point (in the sub + caption): baseline QUALITY alongside speed because AI can go faster while rework climbs — a speed-only baseline flatters you; same task class, n≥3, Hawthorne caveat; frameworks/vanity metrics deferred to M-11.2. Keyboard tiles (aria-pressed); reduced-motion static (one tile's detail shown). | M-11.1 |
| SIM-19 | The Team & the Quality Gate | Built 2026-07-25 (M-9.7). Toggle GATE ON/OFF. Top: the org chart — `lead` with `tools: Agent(pa-reviewer, lazy-reviewer)` allowlist chip + two read-only specialists (least-privilege chips). Bottom: the flow — lazy-reviewer returns "the code looks fine" (incomplete); GATE ON → SubagentStop BLOCK (reads `last_assistant_message`, missing verdict/`cited:`/marker) → reviewer redoes → VIOLATION · `cited: ApprovalService.java:99` · `REVIEW COMPLETE (lazy-reviewer)` → ACCEPTED (green); GATE OFF → the incomplete output is ACCEPTED as-is (red). Foot carries the real run (recordings/m97-agent-team-gate.txt): the gate blocked once (its counter file = 1) and the contract-less reviewer emitted a marker it was never told to produce — proof the gate changed the output. Keyboard mode buttons; reduced-motion shows all rows. Cyan (gate) accent. | M-9.7 |
| SIM-18 | Three Primitives, One Job Each | Route nine PA needs to skill / hook / subagent (reveal + score + reset, the SIM-10 drill idiom; answers 3/3/3). A legend defines the three roles (skill teaches / hook enforces / subagent isolates), and a **proof strip** carries the real measured result (recordings/m93-skill-hook-subagent.txt): same model, same prompt, same acceptEdits, one variable — WITH HOOK the write is DENIED (line stays `>=`, git diff empty), NO HOOK the regression LANDS (`>=`→`>`, past the javadoc that says INCLUSIVE). Colours are the tool palette **repurposed for roles** (gold/cyan/purple = skill/hook/subagent), NOT tool identity — foot states this. Keyboard picks; reduced-motion pre-fills correct + reveals. | M-9.3 |
| SIM-17 | Where Does the One File Live? | Two directory options, each showing per-tool status from the measured runs (recordings/m92-one-skill-two-tools.txt). `.claude/skills/`: Claude **REGISTERED** (probe named the skill with zero file reads) + Copilot **PICKED UP** (doc-quoted) → "this is the home". `.github/skills/`: Claude **NOT REGISTERED** (probe found none) + Copilot **NATIVE** → flagged as the trap, with the naive 10-turn "firing" explained as the agent reading a file it found. Foot states the one-directional interop and the probe-not-firing rule. Keyboard tabs; reduced-motion opens on the trap. | M-9.2 |
| SIM-16 | Grind or Judgment | Two tabs over real recorded runs (recordings/m84-model-economics.txt). GRIND: both models answer "13", small $0.0912 vs large $0.1612 — only **1.8×**, with the explanation that carried context doesn't get cheaper when the model does. JUDGMENT: small model $0.0368, fluent and line-cited and **WRONG** (reported the javadoc's ">=" as the code's behaviour; line 99 is strict >), vs the large model leading with "the repo contradicts itself". Card borders + verdict pills mark correct vs wrong; foot states the routing test (can you CHECK the answer?). Keyboard tabs; reduced-motion opens on the judgment tab. | M-8.4 |
| SIM-15 | The Constitution Audit | Six-line flawed CLAUDE.md — the exact file used in the recorded stale-command experiment (recordings/m75-antipatterns.txt). Click (or keyboard-activate) any line to audit it: it tags STALE—QUIET / STALE—LOUD / ESSAY / KEEP and explains with the recorded verdict. Meters count audited / flagged / survives; "audit every line" cuts 5 of 6 and lands the closing point — the remaining title still has no domain rule, owner or date, so an audit that only deletes is half done. Reduced-motion opens fully audited. | M-7.5 |
| SIM-14 | One File, Four Tools | Three tabs replay the real recorded experiment (recordings/m72-agents-md.txt): RUN A (AGENTS.md alone, free exploration → found it, cited AGENTS.md:5-8 — i.e. read it as a file), RUN A2 (no file reads → "my loaded context does not contain that rule"), RUN B (4-line CLAUDE.md starting `@AGENTS.md`, no file reads → "the rule IS in my loaded context"). Below, the doc-verified support matrix (Claude Code / VS Code Copilot / VS 2026 / Cursor) with each tool's native file. Foot states the thesis: discovered vs loaded. Keyboard tabs; reduced-motion opens on RUN B. | M-7.2 |
| SIM-13 | Boundary Grid — the untested validator | 8 confidenceScore boundary cases for BUG-WEB-03's untested validate(): each row shows the spec verdict vs what validate() actually does, computed by a faithful in-visual port of the guard (no Date/random). Seven agree (green ✓); the whitespace-only row is flagged as a SPEC GAP (red) — the real bug the spec-anchored generation caught live (Number('   ')===0 slips the guard). Note box explains the JS gotcha. Static table. | M-6.3 |
| SIM-12 | Test Quality — Three Patterns | Three-tab panel over the SAME target (ConfidenceGauge @ 0.85, BUG-WEB-04), each tab a real recorded generation: MIRROR (0.92/0.61 shipped test — green & useless), "MAKE IT PASS" (naive prompt — green, but edited the component to erase the bug), SPEC-ANCHORED (disciplined — red, source untouched, caught it). Verdict pill + rationale per tab; foot: "a generated test's value is what it would catch, not whether it passes." Keyboard tabs; reduced-motion opens on the spec-anchored tab. | M-6.1 |
| SIM-11 | Flaky Calendar Heatmap | 365 days of 2026 as a 12-month grid; red = the flaky test's hard-failure days (last 3 of each month = 36, from the real probe run), today (Jul 23) gold-outlined, next-failure (Jul 29) pulsed. Toggle shipped (LocalDate.now()) vs fixed (pinned to 2026-06-01) → all 36 turn green with zero production change, proving it was a test bug. No Date.now() in the component (dates are static data — safe under the harness Date ban). Readout carries the 9.9% figure. | M-5.4 |
| SIM-10 | Layer Drill | 12 PA tasks vs the four rungs (ghost/inline/chat/agent), SIM-08 mechanics with neutral pick styling (tool colors stay tool-reserved): pick per card, reveal scores with rationales carrying real course numbers (the $0.89/9-turn envelope on agent cards, 13-row seed answer, M-2.1/2.2/3.2 callbacks); one contested card (terminal-inline vs chat) accepts either defended pick. Reset; keyboard-operable; reduced-motion pre-picked revealed. | M-4.4 |
| ANIM-32 | Issue to PR | Built 2026-07-25 (M-10.5, T-10 close-out). Abstract staged pipeline (no fake chrome), gold. Seven steps of the REAL end-to-end run (recordings/m105-full-workflow.txt, 141s, all verified against the clone), each tagged with the T-10 tool that powers it: ISSUE (the spec) → PLAN (Plan Mode T-03, 3 files) → BRANCH (`feat/queue-depth`) → BUILD (`countByStatus`+`manualReviewQueueDepth()`, additive) → TEST (GREEN 32/0, gated by a hook M-10.2) → COMMIT (`6088d14`, +14/−0) → PR (`PR.md`, opened by the Action/cloud M-10.4). A dashed red FOUND row: spotted BUG-API-01 unprompted, left it untouched, flagged it inflates the queue depth the feature reports. Caption: the human reviews a PR, doesn't type the middle; T-10 in one line (encode ritual/enforce rule/connect world/choose where). Rows stage in + loop; replay; reduced-motion static. | M-10.5 |
| ANIM-31 | Where the Agent Runs | Built 2026-07-25 (M-10.4). Abstract 2-lane staged diagram (no fake chrome), doc-verified 2026-07-25. Lane 1 (gold) ON YOUR MACHINE — headless & background: `claude -p --output-format json` → the envelope; a REAL background run (recordings/m104-cloud-background.txt: `total_cost_usd 0.23`, `num_turns 9`, `24s`, `is_error false`) collected after, never blocked on; `--bg` first-class (returns session id) + `-c`/`-r` resume. Lane 2 (cyan) SOMEWHERE ELSE — cloud & CI: Copilot cloud agent ("Delegate this task to the cloud agent" / assign `@copilot` → GitHub's remote infra, ephemeral Actions-powered → draft PR); Claude Action `anthropics/claude-code-action@v1` (`@claude` → PR, on YOUR runners). Caption: same work, different locus — choose by who owns the compute + audit trail. Rows stage in + loop; replay; reduced-motion static. | M-10.4 |
| ANIM-30 | The MCP Connection | Built 2026-07-25 (M-10.3). Abstract 2-lane staged diagram (no fake chrome), gold/Claude-Code, MCP doc-verified 2026-07-25. Lane 1 AN OUTSIDE TOOL, CONNECTED: server (`payer-policy`, one `registerTool` over stdio) · wire it (`claude mcp add` / `.mcp.json`, scopes local/project/user) · transports (stdio / streamable-http, SSE deprecated) · tools as `mcp__<server>__<tool>`. Lane 2 THE LIVE CALL + ALLOWLIST, real results (recordings/m103-mcp.txt): allowed → MERIDIAN/97110 covered, MERIDIAN/70553 CPT-002 (matches EligibilityService, headless via --mcp-config); blocked → un-allowlisted under dontAsk denied, agent refuses to guess (M-1.6 boundary); govern → allow/deny `mcp__server__*`, enterprise `allowManagedMcpServersOnly`+`allowedMcpServers`. Caption: the payer rule hard-coded in EligibilityService becomes a queryable service. Rows stage in + loop; replay; reduced-motion static. | M-10.3 |
| ANIM-29 | The Hook Lifecycle | Built 2026-07-25 (M-10.2). Abstract 2-lane staged diagram (no fake chrome), gold/Claude-Code, hooks doc-verified 2026-07-25. Lane 1 THE EVENTS OF A TURN in order with can-block: UserPromptSubmit (block/add, NOT rewrite) → PreToolUse (deny) → PostToolUse (can't block; annotate/`updatedToolOutput`) → Stop/SubagentStop (block). Lane 2 THREE PATTERNS pinned to their event w/ REAL results (recordings/m102-hooks.txt): test gate @ PreToolUse(git commit) — hook runs the suite itself → red=DENY (blocked), green=ALLOW (landed), can't be dodged with `--no-verify`; formatter @ PostToolUse(Edit) — fix-up-after; redaction @ PostToolUse `updatedToolOutput` — `sk-…`→`[REDACTED]` (a hook can't rewrite your prompt → redact output/block the read). Caption = the two live Windows portability bugs (matcher `Bash` vs `PowerShell`; `./mvnw` vs `.\mvnw.cmd`). Rows stage in + loop; replay; reduced-motion static. | M-10.2 |
| ANIM-28 | The Command Expands | Built 2026-07-25 (M-10.1). Abstract 2-lane staged diagram (no fake terminal chrome), gold/Claude-Code. Lane 1 THE FILE (`.claude/commands/fix-and-test.md`): frontmatter (description/argument-hint/`allowed-tools: Bash(./mvnw:*)`) + the 4 ritual steps with `$ARGUMENTS` highlighted gold. Lane 2 ONE INVOCATION (`/fix-and-test ClaimHistoryServiceTest`, 68s REAL run from recordings/m101-slash-commands.txt): RED (4 run, 1 fail) → root cause `ClaimHistoryService.java:29` `.limit(limit+1)`→`.limit(limit)` → GREEN (4/0) → bonus: flagged the N+1 in historyFor as out-of-scope, not fixed. Caption: a command has no new powers — it standardises the prompt so the good procedure runs the same every time; `.claude/commands/` is legacy, a skill is current (M-9.1). Rows stage in + loop; replay; reduced-motion static. | M-10.1 |
| ANIM-27 | The Custom Agent, Filed Three Ways | Built 2026-07-25 (M-9.6). Abstract 3-lane staged diagram (no fake IDE chrome), Copilot custom agents doc-verified 2026-07-25. Lane 1 ONE FILE: `.agent.md` frontmatter (name/description/tools "unset = ALL"/model + user-invocable/disable-model-invocation) over a body "prepended to your prompt". Lane 2 WHERE IT LIVES — NEAREST COPY WINS: repo `.github/agents/` · user `~/.copilot/agents/` (VS Code & CLI) vs `%USERPROFILE%\.github\agents` (Visual Studio) · org/ent in the org `.github`(-private) repo · precedence row "lowest (nearest) wins: repo > org > enterprise" (verbatim). Lane 3 HOW YOU CALL IT — CHECK YOUR SURFACE: VS Code agents dropdown · Visual Studio `@name` (VS 2026 v18.4+) · CLI `--agent`; runs as a subagent "its own context window". Caption: cross-tool anchor (Claude `.claude/agents/`, Copilot, Cursor) + `.chatmode.md`→`.agent.md` rename. Rows stage in + loop; replay; reduced-motion static (all rows shown). Cyan-accented (Copilot). | M-9.6 |
| ANIM-26 | The Description Is the Trigger | The real skill's frontmatter (description highlighted as the matching surface) above two clickable recorded prompts: the match — "Is the auto-approve threshold comparison correct?" → **FIRED**, 8 turns, produced the enforced THRESHOLD AUDIT format with all four values correct; and the control — "What does NotificationService do when the gateway throws?" → **SILENT**, answered the real question. Foot states the craft rule (write the description in the user's words, test both directions). Keyboard-activatable; reduced-motion opens on the control. | M-9.1 |
| ANIM-25 | Two Levers (re-anchor vs scope) | Three staged bars from the real measured runs (recordings/m83-compaction-reanchor.txt): A rediscover 9 turns $0.3267 · B anchored-unconstrained 9 turns **$0.4447 (worse than A — the hypothesis that failed)** · C anchored+scope-limited **1 turn $0.0611 (19% of A)**. Punchline panel states the correction explicitly, then a four-step strip (checkpoint / clear / re-anchor→accuracy / scope→economy) with steps 3 and 4 colour-separated as the two levers. Run button; reduced-motion settles immediately. | M-8.3 |
| ANIM-24 | Anatomy of a Turn | Three staged rows from the real metered runs (recordings/m81-anatomy-of-a-turn.txt): trivial 1-turn $0.0134, single-file 2-turn $0.0648, multi-step 10-turn $0.2834, each bar split cache-read / cache-write / output and labelled with the tokens the user actually typed (2, 4, 7). Punchline panel lands the 1:17,765 typed-vs-carried ratio and the 21.1× turn multiplier; below, the real per-model split (haiku $0.0006 = 0.22% vs opus $0.2828 = 99.78%) to pre-empt "just use a cheaper model". Run button; reduced-motion settles immediately. | M-8.1 |
| ANIM-23 | Two-Tier Memory | The REAL auto-memory directory from this course build (recordings/m74-memory-practice.txt): tier 1 = MEMORY.md index, 479 B, "loaded every session — this is the rent"; tier 2 = topic files (2,757 B + 1,421 B) "read on demand — free until needed", plus a real pruned file shown struck through. Meters: on-disk / loaded-every-session (~10%) / index vs the 25KB cap. Buttons write a new memory (detail → topic file, one pointer → index) and prune a fulfilled one (explains silent truncation past the cap). Static; no motion dependency. | M-7.4 |
| ANIM-22 | Test-First Loop + the Gate | Three staged panels from the real recorded loop (recordings/m65-test-first.txt): RED (verbatim compile error — "ReviewAgeService does not exist"), GREEN (11/11, minimal impl), GATE (5 mutations, all caught). Then a two-card comparison — tests-written-after 2/5 · 40% (M-6.4's shipped suite) vs tests-written-first 5/5 · 100% — and a "why" panel carrying the source-verified mechanism (spec-language `Duration.ofDays(2)` assertions vs referencing the implementation's constants). Run button; reduced-motion settles immediately. | M-6.5 |
| ANIM-21 | Mock vs Container | Two staged columns of what each test layer can prove (mocked service test: logic + speed, blind to SQL/mapping/constraints; Testcontainers: real SQL, @ServiceConnection wiring, proves M-5.6's batch-IN fix) with ✓/✗ marks, plus a Docker up/down toggle whose meter carries the real recorded numbers (up: 37/37; down: 31 pass + 6 skip, BUILD SUCCESS via disabledWithoutDocker). Replay; reduced-motion static. | M-6.2 |
| ANIM-20 | Stale Closure vs Fresh Fetch | Two polling lanes stepped over the same backend transition (SUBMITTED → AUTO_APPROVED): the shipped lane hoists the promise (getRequest calls frozen at 1, widget frozen on the first value), the fixed lane fetches per tick (calls climb 1→4, status tracks the change). Numbers from the recorded run. Caption carries the "green suite only proves what it simulated" lesson. Poll button restages; reduced-motion shows the final frame. | M-5.5 |
| ANIM-19 | Static vs Runtime Evidence | Two columns staged in sequence: reading the code (T-03's hypothesis chain, ending "reasoned, cited, unobserved") vs the REAL captured runtime frame (recordings/m53-runtime-frame.txt — score/threshold both 0.85, `>` false and `>=` true on the same pair, Double.compare = 0, plus the application's own false log line emitted unprompted). Below, Visual Studio's six documented Debugger Agent steps as chips with the final human-validation gate in gold. Replay; reduced-motion static. | M-5.3 |
| ANIM-18 | Scope Ladder & Key Map | Two staged lanes: the four-rung scope ladder (keystroke → selection → conversation → turn, each with its cost framing incl. the recorded $0.89 tier) and the cross-tool key map with doc-verified chords (VS Code Ctrl+I inline chat + scope quote; Cursor Ctrl+K selection/generate/terminal modes; Cursor Ctrl+I = agent panel — the muscle-memory trap called out in the caption). Rows stage in and loop; replay; reduced-motion static. | M-4.3 |
| ANIM-17 | The N+1 Fan-out | Two lanes with the real recorded numbers (recordings/m35-smell-run.txt): the promise lane holds constant at 2 queries / 435 rows (one pass, batch method now dead code); the reality lane fans out 1+P per-patient query chips with a P slider (4–92, seed scale 92 → 93 statements, ≈40k rows scanned for ≈359 needed). Foot carries the scaling law (doubling business quadruples work) and M-5.6 fix ownership. Replay; reduced-motion static at seed scale. Reused by M-5.6 (the fix: the promise lane's constant 2 becomes reality — 25→1 measured). | M-3.5, M-5.6 |
| ANIM-16 | Read-Only Toolbox & the Handoff | Two staged lanes for Copilot's Plan agent, abstract per the no-fake-IDE-chrome rule: lane 1 the read-only research tools with doc-verbatim descriptions (#codebase, #usages = "Combination of Find All References, Find Implementation, and Go to Definition", #readFile/#textSearch, clarifying-questions beat, plan.md in session memory with its cleared-at-end lifetime); lane 2 the three human-initiated handoff exits (start the implementation / background CLI / open in editor). Caption carries the Visual Studio durable-plan split. Rows stage in and loop; replay; reduced-motion static. | M-3.3 |
| ANIM-15 | The Two Boundaries | Two staged lanes. Data boundary: context chips flow toward the prompt — seed.sql and CLAUDE.md pass the gate, phi/patient-export.csv stops at a DENY-rule wall (the deny beat is the real recorded run, recordings/m16-boundary-run.txt). Decision boundary: 0.91 → AUTO_APPROVED (pass gate), 0.62 → MANUAL_REVIEW (human gate — never auto-denied), PAYER-001 → DENIED with a POLICY stamp, not a model stamp; mirrors ApprovalService/EligibilityService exactly. Rows stage in and loop; replay button; reduced-motion static. | M-1.6 |
| ANIM-33 | A Shift in the Life | Built 2026-07-26 (L-1.1, first leadership module). A leader's-eye timeline of one engineer+agent work session as staged rows, each badged HUMAN (gold) / AGENT (dim) / GATE (cyan) and tagged with the REAL course run it comes from: HUMAN states intent → AGENT plans + asks "20% of what?" (M-12.2) → HUMAN validates units → AGENT issue→PR w/ tests (M-10.5) → GATE blocks a red commit (M-10.2) → AGENT debugs/traces (M-5.1) → HUMAN reads the diff + signs off → GATE the agent refuses the policy call (M-12.3) → HUMAN approves→ship. The split is the point: agent does the volume, the human's few moments are all decisions, the gates fire on their own. Caption: unit of output = a reviewable PR not lines typed; manage the gates, not the keystrokes. Rows stage in + loop; replay; reduced-motion static. Feeds L-1.1 (and the L-01 mental-model track). | L-1.1 |
| ANIM-13 | Copilot Mode Spectrum | Same PA task ("update denial-reason copy") flows down three lanes — Ask answers only; Plan writes a read-only workup; Agent crosses sign-off gates (tool confirmations with scope), shows Keep/Undo diffs, drops checkpoint markers. Lanes match the current agents dropdown (Agent/Plan/Ask — Edit mode deprecated, verified 2026-07-20). Abstract behavior diagram by design: no fake IDE chrome or fabricated screenshots. | M-1.3 |

## Terminal replays (TERM-xx)
Typing-animation component replaying trimmed real Claude Code sessions (recorded during repo build). One per Claude Code lab. Speed control ×1/×2/skip. Copilot and Cursor labs instead use annotated screenshot strips (their IDE UIs don't fake honestly in HTML) with step-highlight animation.

| ID | Name | Source & behavior | Modules |
|----|------|-------------------|---------|
| TERM-05 | Plan Steer Replay | Real recorded two-round Plan Mode steer cycle on priorauth-api (source: recordings/m32-plan-steer.txt): round 1 plans the refactor the repo's own javadoc forbids and flags it ("the code explicitly says don't", strict-> preserved bit-for-bit, modest payoff); a steer message accepts the flags and cuts scope; round 2 returns the five-step scoped plan; git-status-clean proof after both rounds. Annotation card teaches the doc-verified interactive gate (the four approval options, Ctrl+G plan editing, Shift+Tab exit). Reuses .term01 styles. | M-3.2 |
| TERM-04 | Spec Plan Replay | Real recorded headless Plan Mode run of M-2.3's spec mission against priorauth-api lab/start (source: recordings/m23-spec-plan-run.txt, v2.1.216, 4.5 min): spec → plan opening with the spec-vs-code "headline conflict" (strict `>` at ApprovalService.java:99 vs the spec's inclusive boundary) → consequence findings (COVERED-map coupling, no migration framework) → git-status-clean proof of zero edits → annotation on the approve/steer gate and the headless wrinkle (no interactive gate; plan lands in ~/.claude/plans/). Reuses .term01 styles. | M-2.3 |
| TERM-03 | Vibe Run Replay | Real recorded vibe mission against the real priorauth-web (source: recordings/TERM-03-m2.1-vibe-run.txt): one-sentence prompt → trimmed real output (extend-vs-new decision, files) → real elapsed/build/test tail (4.1 min, 21/21) → annotation on the "accepted by eye" contract. Reuses .term01 styles. | M-2.1 |
| TERM-02 | First Flight Replay | Real recorded headless run of M-1.5's actual mission against the real repo (source: recordings/TERM-02-m1.5-first-flight.txt): mission prompt → trimmed real agent summary (files it chose) → real `mvnw test` green tail → annotation card with the debrief questions. Reuses the .term01 styles; same honesty rules (annotations never fake output). | M-1.5 |
| TERM-01 | First Session Replay | Real capture (v2.1.215, 2026-07-19) in `recordings/TERM-01-m1.2-first-session.txt`: `claude --version` + headless `-p` Q&A citing ApprovalService and the inclusive >= 0.85 rule. Interactive-only mechanics (Shift+Tab, /clear, rewind menu) render as annotation cards, never as fake terminal output. Re-record when the real priorauth-api ships. | M-1.2 |

## Shared conventions
- Gold = Claude Code, cyan = Copilot, purple #6E5AAE = Cursor — everywhere. Purple comes from the reference palette (no invented colors); pair it with white text for AA.
- Every sim has a one-line "what to notice" caption and a reset button.
- Max one heavy canvas per viewport; sims below the fold lazy-init on IntersectionObserver.
