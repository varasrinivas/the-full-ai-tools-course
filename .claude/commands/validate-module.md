Validate module $ARGUMENTS. Report pass/fail per check; fix trivial fails, list the rest.
- Schema: all module-template fields present; path/track/id consistent with BLUEPRINT.md.
- Dual-path: labA and labB present (or track exempt per blueprint).
- Domain: prior-auth vocabulary throughout; threshold stated as >= 0.85 inclusive.
- Visual: factory id exists, mounts without console errors, reduced-motion fallback works.
- Theme: render in dark and light; check contrast on new elements.
- Currency: lastVerified set; needsVerification items mirrored in PROGRESS.md.
- Player: total file still opens standalone; no new external deps.
Then update PROGRESS.md (mark ✅ or ⚠ with reasons) and stop.
