Build or repair the labs for module $ARGUMENTS.
1. labA = Claude Code path, labB = Copilot path (VS Code; call out Visual Studio differences where BLUEPRINT.md says so).
2. Each lab: numbered steps, expected output per step, target time ≤30 min, references the planted bug or repo location from REPO-SPECS.md by ID.
3. Terminal replay for labA must come from /recordings — if missing, add a stub and log "recording needed: <id>" in PROGRESS.md.
4. Verify both labs against the actual repos if available in this workspace; otherwise mark needsVerification.
