# PROGRESS.md — 10x Toolkit build log
## Status board
Engineer path: **60/60 — ✅ COMPLETE (2026-07-26)** · Leadership path: **12/22 built (L-01 ✅ · L-02 ✅ · L-03 ✅)** · Player shell: scaffolded (2026-07-19) + lead-path lab/pill rendering added (2026-07-26)
| ID | Status | lastVerified | Notes |
|----|--------|--------------|-------|
| L-3.4 | ✅ built 2026-07-28 | 2026-07-28 | LEADERSHIP (L-03, 12/22 — **L-03 Governance & Guardrails COMPLETE (4/4)**; the track arc = permission → data → review → proof). IP, compliance & the audit trail. **FRESH primary-doc verification 2026-07-28**: code.claude.com/docs/en/{data-usage,zero-data-retention} + docs.github.com copilot responsible-use/code-completion & review-audit-logs + cursor.com/docs/enterprise/privacy-and-data-governance → recordings/l34-ip-compliance-audit.txt. **STANDING GUARDRAIL, enforced by validator checks:** not legal advice; does NOT assert any vendor's compliance status, does NOT resolve who owns AI output (framed as unsettled + counsel's call), does NOT summarize indemnity terms. **Spine:** you'll be asked to prove **provenance / data flow / authority** — "was AI involved?" is none of them — and almost nothing is captured by default, so the audit trail is designed BEFORE you need it, with depth following blast radius (L-1.2). **Three verified findings:** (1) **the data posture is PER-LOGIN, not per-tool** — commercial terms ≠ consumer terms (Anthropic doesn't train "under commercial terms" but *does* train Free/Pro/Max when the setting is on), and ZDR verbatim "if a developer signs in to Claude Code with a personal account… those sessions are not covered" → all three vendors ship the same fix (`forceLoginMethod`/`forceLoginOrgUUID`, Cursor MDM "Allowed Team IDs", managed seats). An engineer on a personal account silently opts out of your agreement. (2) **the vendor audit log is an ACCESS log** — GitHub verbatim: it "does not include client session data, such as the prompts a user sends to Copilot locally" (records policy/seat events, 180 days) → the decision record must be BUILT from artifacts: versioned instruction file (L-1.3) + gate result where a *refusal* proves the control was live (M-10.2) + PR carrying the defense (L-3.3) + a test pinning the inclusive rule to the constant. (3) **provenance controls are filters, not chain of custody** — duplication detection blocks/annotates public-code matches, but "while the probability is low, Copilot may generate code suggestions that match code in the training set" and responsible-use guidance puts the risk on the user. **Fourth honest caveat in a row for L-03** (after native-Windows sandbox, content-exclusion-misses-Agent-mode, advisory-not-gate) — the artifact leaders call "the audit trail" is an access log; pattern now named across the track. Also verified & taught: ZDR is not a checkbox (Enterprise-qualified, per-org, disables web/cloud/artifacts/feedback/Remote Control, Fable 5 unavailable, policy-violation retention up to 2y) and **does not cover MCP/third-party integrations** (closes the loop L-3.2 opened); local plaintext transcripts under `~/.claude/projects/` 30d (`cleanupPeriodDays`); /feedback transcripts 5y; a HIPAA agreement is referenced as something you arrange (never asserted as status). Sourcing hygiene: the Copilot 65-lexeme figure and Cursor's privacy-mode-OFF detail came only via search summary → **number deliberately omitted**, off-state framed as "check the current page"; Cursor audit logging "not documented on that page" rather than claimed absent. Built SIM-36 (Reconstruct the Change: the real `>` → `>=` threshold diff as the audit subject + 7 auditor questions pilled CAPTURED/ONLY-IF-CONFIGURED/NOT-CAPTURED + M-12.3's refusal as the **authority record** — "human as approver, not typist" reframed as an audit property + italic not-legal-advice line). Labs: Run the reconstruction on a real change (answer 7 questions from artifacts alone, 2 structural fixes, contractual answers in writing, check enforced login) + Find where the record actually comes from. **114/114 jsdom** incl. eng-60 + reduced-path guards + L-3.1/3.2/3.3-untouched + 12 sourcing/guardrail assertions (incl. no-compliance-status, no-indemnity-claims, IP-left-to-counsel); live Chromium both themes, no console errors, no overflow at 283px. Fixed during validation: the threshold appeared once without its inclusive framing → now stated as "0.85 or above auto-approves" / "score >= 0.85" per CLAUDE.md. No needsVerification |
| L-3.3 | ✅ built 2026-07-28 | 2026-07-28 | LEADERSHIP (L-03, 11/22). Reviewing AI-written code — policy that scales + the rework radar. **FRESH primary-doc verification 2026-07-28** (hard-verification track): WebFetch of code.claude.com/docs/en/{code-review,security-guidance} + docs.github.com/copilot/concepts/code-review + cursor.com/docs/bugbot → recordings/l33-reviewing-ai-code.txt; metric definitions RE-READ from m111/m112 before quoting (no fresh fetch, L-2.2 precedent). **THE HEADLINE FINDING — unanimous and verified: an AI code reviewer is an ADVISOR, not a GATE, in all three tools by default.** Claude Code: findings "don't approve or block your PR" + check run "always completes with a neutral conclusion so it never blocks merging"; Cursor Bugbot: findings "default to `neutral`"; Copilot: "not guaranteed to spot all problems… **Supplement Copilot's feedback with a human review**." A leader who switches these on and reports review is covered is wrong twice (doesn't block, isn't guaranteed to catch); gating is YOUR CI work (Claude Code documents the gh/jq severity parse). **Third honest caveat in a row for L-03** — same shape as L-3.1's native-Windows sandbox and L-3.2's content-exclusion-misses-Agent-mode; the pattern is now named in-module. **Thesis handed over verbatim by Anthropic's docs:** "Each later stage catches what earlier ones miss. The plugin's value is reducing the volume that reaches them, not eliminating the need for them" = AI review buys VOLUME REDUCTION, not sign-off elimination. **Spine:** the agent changed the ARRIVAL RATE, not the risk tier (L-1.2 holds), and review capacity is the one control that doesn't scale by adding eyeballs → make review effort deliberately unequal by blast radius; know which layers actually refuse (M-10.2's non-bypassable pre-commit gate vs a PR comment); green is a FILTER not a verdict (M-2.2's real 25/25-green-but-endpoint-invented, "unavailable for every user" — mechanical layers can't catch a failure of *intent*); ask for **the defense** not the diff (M-12.4) and **encode the standard as a file** — `REVIEW.md` injected as highest-priority into every review agent (severity recalibration, nit caps, skip-what-CI-enforces, verification bar "behavior claims need a file:line citation") = **L-1.3's instruction-file-as-asset applied to review policy**, the module's strongest new idea. **Precision catch worth teaching:** TWO different "reworks" — GitClear code churn (lines reverted/substantially revised within ~2 weeks, *leading*, cited as their analysis + correlation-not-cause per M-11.1 discipline) vs DORA's NEW 5th metric **deployment rework rate** (unplanned deploys from a production incident, stability side). Say which you mean; read at **team level only** (individual scorecard → Goodhart, L-5.3). Also verified but scoped out: security-guidance plugin's designed-in review independence ("does not ask the same Claude instance that wrote the code to grade itself"), its layered stack, Code Review's Team/Enterprise + no-ZDR gating and $15–25/review cost (forward-links L-04). Built SIM-35 (Advisory or Gate?: 5 click-to-expand layers pilled ADVISORY amber / GATE green + M-2.2 warning strip + the rework radar ordered by how fast each signal moves). Labs: Write the review policy that scales (tier it, audit advisory-vs-gate honestly, write the required review questions, name signal+trigger+owner) + Watch a review catch what green missed (M-9.4 reviewer cites BUG-API-01 against the javadoc → then M-2.2's green-but-wrong → find the same gap in your own stack). **113/113 jsdom** incl. eng-60 + T-04/T-08 reduced-path guards + L-3.1/L-3.2-untouched + verified-quote checks; live Chromium pass both themes, no console errors, no overflow at 280px. Scope held: provenance/IP/audit → L-3.4; org DORA + vanity list → L-05. No needsVerification |
| L-3.2 | ✅ built 2026-07-28 | 2026-07-28 | LEADERSHIP (L-03, 10/22). MCP & data boundaries — what agents may touch. **FRESH primary-doc verification 2026-07-28** (hard-verification track, like L-3.1): WebFetch of code.claude.com/docs/en/{mcp,managed-mcp,security} + code.visualstudio.com/docs/copilot/customization/mcp-servers & /docs/enterprise/ai-settings + docs.github.com configure-mcp-server-access & content-exclusion + cursor.com/docs/context/mcp → recording sheet recordings/l32-mcp-data-boundaries.txt. **Spine:** L-3.1 set how much an agent may DO; this sets what it may REACH — the unit of governance is the CONNECTION, and approving one is a data-access decision. Four moves: (1) **nobody is vetting these for you** — all three vendors say so in their own docs (Anthropic verbatim "does not security-audit or manage any MCP server"; VS Code "can run arbitrary code on your machine"; Cursor "can access external services and execute code on your behalf"); (2) three questions per connection — direction / data class / blast radius — with **minimum necessary** applied to PHI (agent needs *shapes*, not patients → de-identified replica); (3) **a checked-in config is a PROPOSAL, not an approval** (Claude Code parks project servers at `⏸ Pending approval`, "a cloned repository can't approve its own servers") → the decision must live in an enforced org layer: managed-mcp.json / allowedMcpServers+allowManagedMcpServersOnly · chat.mcp.access "registry" + org "Registry only" · Cursor dashboard MCP Configuration; (4) **match on identity, never on name** — verbatim "a `serverName` entry… is not a security control… a user can call any server `github`" (= L-3.1's "the list is not a boundary" one level deeper), with deny-beats-allow intact ("nothing overrides a denylist match") and per-TOOL granularity (mcp__server__tool). **Two honest caveats (the L-3.1 native-Windows pattern repeated):** Copilot content exclusion is "not supported in Edit and Agent modes" — the file-level block leaders assume covers them stops where the agent starts; and Claude Code trust verification "is disabled when running non-interactively with the `-p` flag" (the automation gap). Exfiltration framing = the **lethal trifecta attributed to Simon Willison (2025), NOT to a vendor** — vendor-verified support quoted separately. Reuses M-10.3's live good-failure (denied the tool, the agent refused to guess a coverage determination) as labB. Built SIM-34 (Connection Register: thesis banner + 4 click-to-expand connector rows crossing a dashed boundary rule, direction glyph + data-class chips + verdict pill, the M-10.3 payer-policy server as row 1; foot = enforced layer per tool + the two gaps). Labs: Write your connection register (inventory→rule→enforce, owner+signer per row, one revocation) + Watch the boundary hold (allowlisted vs blocked, see the refusal, then find your own gap). **108/108 jsdom** incl. eng-60 guard + T-04/T-08 reduced-path guards + L-01/L-02-still-4 + L-03-now-2 + real player-render and tab-switch assertions; verified live in Chromium both themes, no console errors, no overflow at 400px. Scope held: retention/IP/audit → L-3.4, review policy → L-3.3. No needsVerification (all claims doc-sourced this session) |
| L-3.1 | ✅ built 2026-07-27 | 2026-07-27 | LEADERSHIP (L-03, 9/22 — **opens L-03 Governance & Guardrails, FIRST hard-verified leadership module**). Permission architecture ("safe by default"). **FRESH primary-doc verification 2026-07-27** (NOT reframe): claude-code-guide agent for Claude Code + WebFetch of cursor.com/docs/enterprise/llm-safety-and-controls + code.visualstudio.com/docs/agents/approvals & /security → recording sheet recordings/l31-permission-architecture.txt. **Spine:** an agent runs with your privileges on your machine (Cursor verbatim: "no security boundary between agents and your user account"), so permissions = a privileged-access decision and "safe by default" is a POSTURE a leader sets, via 4 levers: (1) default MODE — guarded not open [verified mode names all 3 tools: ask-first Manual/Default-Approvals/Auto-review · auto-run acceptEdits·Auto/terminal-auto-approve/Allowlist · no-guardrails Bypass-permissions/Bypass·Autopilot/Run-Everything]; (2) allow/deny RULES — deny beats allow universally (Copilot verbatim + Claude Code deny→ask→allow) BUT an allowlist is "best-effort, not a security boundary" (Cursor verbatim), denylist porous to prompt injection; (3) the SANDBOX = the real OS-enforced boundary (macOS Seatbelt / Linux bubblewrap / WSL2) — **honest caveat: Claude Code sandbox NOT supported on native Windows → WSL2/dev-container** (same platform-honesty as M-10.2); (4) enforced org POLICY that a dev can't override (managed-settings precedence managed>CLI>project>user, disableBypassPermissionsMode / ChatToolsAutoApprove / Cursor team dashboard). Built SIM-33 (Permission Dial: thesis banner + 3 posture tiers × 3 tool-colored verified-mode chips + 4-levers foot). Labs: Set your default posture (one-page: default mode, gated modes, deny rules, sandbox decision, enforced-policy control) + See the dial & the boundary (cycle modes, prove sandbox>rules, deny>allow). Hands to L-3.2 (MCP & data boundaries). 42/42 jsdom incl. eng-60 guard + L-02-still-4 + L-03-opened + verified-quote checks. No needsVerification (all claims doc-sourced this session) |
| L-2.4 | ✅ built 2026-07-27 | 2026-07-27 | LEADERSHIP (L-02, 8/22 — **L-02 track COMPLETE**). Resistance patterns: two OPPOSITE failure modes with mirror-image fixes, not one "resistance." Reframe-only — unified by the verified DORA amplifier verbatim ("AI amplifies what's already there," re-confirmed at m112-frameworks.txt:30 before quoting) + reuses L-1.2 risk lens / M-10.2 gates / T-06 / the BUG-API-01 threshold-bug scenario / L-2.3 honest measurement. **Spine:** the amplifier hits a senior's judgment AND a junior's lack of it → opposite guardrails. Senior skepticism = usually rational; RECRUIT it (show-the-diff on hard code → own the gates/review policy L-03) → best champion, don't dismiss/mandate. Junior over-reliance = the dangerous one because it's SILENT (looks like productivity; ships the 0.86 that should've queued); fix = review discipline (explain-the-diff + L-1.2 + M-10.2 + T-06) + pair with the skeptic — a training/gates problem, not a tooling one. **Meta-trap:** treating both as enthusiasm-to-manufacture — high AI-usage% from an over-relier is a RISK FLAG not a win; reward outcome+defensibility (L-2.3/L-5.3 applied to people). Closes the playbook arc (pilot→cadence→funnel→resistance) and points at L-03 (the senior's recruited skepticism becomes formal policy). Built SIM-32 (Two Resistances: amplifier axis + 2 mirror click-to-expand cards, amber skeptic / red over-relier, wrong/right/becomes). Labs: Name your two resistances (mirror-image plan + incentive audit) + Convert one skeptic (show-the-diff → hand them a gate). 43/43 jsdom incl. eng-60 guard + L-01-still-4 + L-02-now-4. No needsVerification |
| L-2.3 | ✅ built 2026-07-27 | 2026-07-27 | LEADERSHIP (L-02, 7/22). From 2 power users to 200: the pilot→champion→cadence arc turned into a managed funnel + honest measurement. Reframe-only — no new external claims (reuses the DORA J-curve/stability tension from T-11, previews L-05/L-5.3). **Spine:** adoption is a 4-stage funnel (Aware→Tried→Habitual→Champion), each stage needing a DIFFERENT intervention (reach demos / office-hours activation / advanced patterns / give-them-a-squad); treating it as one undifferentiated push is why it stalls. **Honest measurement = progression + the business outcome (queue −20%), NEVER seat count** — seats/logins/AI-usage%/lines are intake vanity (L-5.3). Read the leaks (activation biggest, then retention) and intervene at the stall like working the review queue; expect the J-curve dip (don't panic / don't over-claim week-one). The champion loop (each champion seeds the next cohort) is the only arithmetic that reaches 200. **Analogy:** the enablement funnel IS a pipeline like the PA pipeline — judge by throughput-to-decision + where cases stall, not intake volume (domain-native, load-bearing). Built SIM-31 (Enablement Funnel: banner + 4 tapering click-to-expand stage bars + 3 leak markers). Labs: Draw your funnel (place people, find the leak, pick honest metric pair + refuse-list) + Instrument it honestly (progression+outcome only, pre-agree the J-curve). Hands to L-2.4 (resistance). 42/42 jsdom incl. eng-60 guard + L-01-still-4 + L-02-now-3 + funnel-taper check. No needsVerification |
| L-2.2 | ✅ built 2026-07-27 | 2026-07-27 | LEADERSHIP (L-02, 6/22). Champions & cadence: the machinery that spreads a pilot's win. **Re-verified** the DORA 2025 adoption stats against m112-frameworks.txt before quoting (90% use AI / 80%+ feel productive / 30% little-or-no trust in AI-gen code — dora.dev primary, 2026-07-25) → no fresh web-verify needed, reuse of already-verified primary. **Spine:** trust — NOT capability — is the adoption bottleneck (the 30% stat), and you close it by making work visible + reproducible on a rhythm, never by mandate. Three mechanisms: office hours (pair on real stuck work live, L-1.1 watch-don't-type), show-the-diff demos (show the review + a caught agent failure on real hard code, M-10.2 — not greenfield speed theatre), internal course drops (package the L-1.3 portable layer for self-serve — so the champion scales, not becomes a help-desk). Two disciplines: pull>push (unprompted attendance = trust forming; mandate = vanity usage, forward-links L-2.4/L-5.3) + champion-becomes-unnecessary (teach team #2→#3) = the 2→200 multiplier; hands to L-2.3 (enablement funnel + measuring adoption honestly). Built SIM-30 (Cadence Board: trust-stat banner + 3 click-to-flip mechanism↔anti-pattern cards). Labs: Build your cadence (schedule the rhythm, script a trust-building demo, name the pull signal) + Run a show-the-diff (deliver a real diff-demo w/ a caught failure, observe what moves a skeptic). 39/39 jsdom incl. eng-60 guard + L-01-still-4 + L-02-now-2. No new external claims → no needsVerification |
| L-2.1 | ✅ built 2026-07-27 | 2026-07-27 | LEADERSHIP (L-02, 5/22 — **opens L-02 The Adoption Playbook**). Pilot design: picking the first squad, repo and win. Change-management module; reframes the T-11 baseline discipline + L-1.3 portable-layer asset + seeds L-5.3 vanity-metric refusal — no new external claims. **Spine:** a pilot is an EXPERIMENT, not a rollout — one squad, one repo, one baselined metric, a fixed time box, a decision gate (scale/adjust/stop). Pick the squad on 4 criteria (real measured pain / AI-fit code+tests / fast cadence / honest champion) = "trustworthy evidence fast," not the keenest or the hardest. Two named traps: eager-volunteer greenfield (no downstream dependency, no agreed metric → convinces no skeptic, no baseline) and too-hard legacy monolith (test-less, quarterly, skeptical → a stall poisons the program). Baseline BEFORE touching anything (no before, no after); one agreed business metric (queue −20%), never a vanity count. **Real deliverable ≠ the feature:** measured delta + portable layer (L-1.3) + a champion who can teach team #2 → the 2→200 handoff to L-2.2. Built SIM-29 (Pilot Selection Board: caveat + 3 candidate rows × 4 PAIN·FIT·SPEED·VOICE chips + verdict pills, click-to-expand). Labs: Design your pilot (score 3 candidates, one-page experiment w/ gate) + Baseline the first win (capture the before, sanity-check it's a business not vanity metric). 41/41 jsdom incl. eng-60 guard + L-01-still-4 + L-02-opened checks. No new external claims → no needsVerification |
| L-1.4 | ✅ built 2026-07-26 | 2026-07-26 | LEADERSHIP (L-01, 4/22 — **L-01 track COMPLETE**). The fleet decision: the tool-fit table read as a manager's procurement call. Reuses BLUEPRINT tool-fit + M-2.5's overturn-with-measurement + the T-09/T-10 convergence + M-12.1 tool-agnostic AI-DLC — no new external claims. **Spine:** the manager's question is "which tool per use case + standardize vs allow choice," not "which is best" (fan question). Tool-fit defaults (verified): UI-heavy→Cursor, backend/agentic→Claude Code, GitHub-native→Copilot, CI/automation→Claude Code — but they're EDITORIAL DEFAULTS, overturn any with a measured bake-off (the capstone's ship-3-ways). Standardize-vs-choice = one-governance-surface (procurement/training/one set of instruction-files+workflows+policy) vs per-team-fit. **The stakes-lowering fact:** all three converged (skills/hooks/subagents/MCP near-identical) + AI-DLC tool-agnostic → the PORTABLE LAYER (constitution + method) travels, matters more than the badge, switching costs less than the debate. Built SIM-28 (The Fleet Decision: caveat banner + 4 click team-archetype→tool rows + convergence foot). Labs: Playbook (map your fleet, assign a bake-off to the contested call, make the standardize-vs-choice call explicit) + Walkthrough (see fit real-vs-marginal + confirm the portable layer travels, M-9.2). Fixed a stray `</p>` in the analogy field (caught pre-commit). 35/35 jsdom (incl. eng-60 guard + L-01-complete check). No teaching-repo work. No needsVerification |
| L-1.3 | ✅ built 2026-07-26 | 2026-07-26 | LEADERSHIP (L-01, 3/22). The instruction file (CLAUDE.md/AGENTS.md) as an ASSET whose quality predicts team ROI. Reuses T-07 + M-11.3 verified numbers — no new external claims. **Spine:** it's a small versioned artifact every agent run draws on, and the FIRST place to invest (playbook: instruction files→workflows→agents, M-11.3). **Measured ROI (M-7.1):** 36 lines → 90s→24s AND flipped the recommendation from shipping the bug → citing the signed rule — it added not capability but a settled ambiguity, compounding across every session. **Liability of neglect:** M-7.3 bloat rent (+3,584 tok/session, 170 lines) + M-7.5 stale-fail-soft = load-bearing misinformation → needs an owner + review cadence. **Governance:** M-9.6 scope repo→user→org→enterprise nearest-wins + "learnings discovered, rules signed" → the leader decides what the org file enforces by default (sets up L-3). Built SIM-27 (The Constitution as an Asset: 3 click-to-expand cards Asset/Liability/Governance + ROI foot). Labs: Playbook (audit your instruction files as an asset register, assign owners, pick one org-rule to sign) + Walkthrough (watch the constitution flip the recommendation, M-7.1). 34/34 jsdom (incl. eng-60 guard). No teaching-repo work. No needsVerification |
| L-1.2 | ✅ built 2026-07-26 | 2026-07-26 | LEADERSHIP (L-01, 2/22). Vibe vs spec as a RISK LENS for managers. Reuses T-02's verified vibe/spec material (M-2.4 rubric cost-of-wrong×lifespan; M-2.2's real green-CI-but-wrong run) + the DORA-2025 amplifier — no new external verification. **Spine:** classify AI-written code by BLAST RADIUS, not provenance ("was AI involved?" is the wrong question); a 4-tier register (disposable→vibe / internal→vibe+review / production→spec+tests+review / high-blast→+human sign-off, M-1.6/M-12.3) sets the control per tier; AI doesn't change the TIER, it changes the VOLUME (amplifier → guardrails must scale); the failure mode to police is DRIFT (a Tier-0 spike promoted to prod unre-tiered) → a re-tiering gate. Cautionary anchor: M-2.2's 25/25-tests-green-but-"unavailable for every user" (invented endpoint) = green CI ≠ safe. **Built SIM-26 (The AI-Code Risk Register)** — 4 click-to-expand tiers + the real M-2.2 warning strip + the amplifier/drift foot; leadership-native rather than lifting the SIM-02/07 engineer drills the blueprint suggested (logged: chose a purpose-built register). Labs: Playbook (tier your team's recent AI work, find the drift) + Walkthrough (watch vibe break on the green check). 33/33 jsdom (incl. eng-60 guard). No teaching-repo work. No needsVerification |
| L-1.1 | ✅ built 2026-07-26 (first LEADERSHIP module) | 2026-07-26 | LEADERSHIP path opens (L-01, 1/22). No coding — for EMs/directors/principals. **Player shell extended (approved by Vara this session):** minimal lead-aware tweak to `renderLabs()` + the pill — `path:'lead'` → pill "LEADERSHIP" and lab tabs labeled "◆ PLAYBOOK LAB" (cyan, first) + "◆ LIVE-TOOL WALKTHROUGH" (gold), reusing the existing tab CSS (keys b/a); **engineer path rendering untouched** (regression-guarded: eng module still shows TRI-PATH + CLAUDE CODE/COPILOT/CURSOR). This completes the scaffold the shell already started (LEGENDS.lead/TRACKS.lead/PATH_DESC.lead existed). **Lead lab structure decided (Vara):** 2 labs — a Playbook lab (management exercise) + a Live-tool walkthrough (watch, don't type); labA=Playbook, labB=Walkthrough. **Module:** reuses the engineer path's OWN measured artifacts as its evidence base (no new external verification): the shift as a lifecycle where the agent does the mechanical volume + the human is the approver at every gate; the real runs pinned (M-12.2 "20% of what?", M-10.5 issue→PR, M-10.2 gate-blocks-red, M-5.1 debug, M-12.3 refuses-the-policy-call, M-10.3 refuses-to-guess); the unit of output = a reviewable PR not lines typed; leverage = the guardrails, grounded in the verified DORA-2025 amplifier ("AI amplifies what's already there"); the leadership move = "watch, don't type." New ANIM-33 (a shift timeline, HUMAN/AGENT/GATE badges, real course moments). 36/36 jsdom (incl. eng-60 regression guard + lead-path pill/tabs). No teaching-repo work (leadership modules are conceptual). No needsVerification |
| M-12.5 | ✅ built + spec-first domain migration run LIVE, measured 2026-07-26 | 2026-07-26 | TRI-PATH, **T-12 COMPLETE (5/5) → ENGINEER PATH COMPLETE (60/60).** The bonus capstone: agent-driven domain migration clinical PA → pharmacy PA, spec-first, defend + measure. **LIVE (claude 2.1.220, 480s / 6m15s):** wrote PHARMACY-SPEC.md scoping the ELIGIBILITY slice (CPT `\d{5}`→NDC `\d{11}`, Payer→PBM, per-PBM formulary + tiers, +days-supply `DS-001`, threshold FROZEN); the agent migrated it — git-renamed `Payer.java`→`Pbm.java`, added the rules, migrated the test — **36 tests green** through one real red→green (two `CPT-002` literals the compiler couldn't see), **+213/−137 across 16 files**. **The standout = the honest RIPPLE MAP + effort:** identified the *real cost centre* = the 1081-line `seed.sql` (the schema under ddl-auto=validate, still clinical vocab, no days_supply → all Testcontainers/boot fail once Docker present; hidden here only by disabledWithoutDocker) + un-versioned API break; split blocking / non-blocking; was explicit the **web repo is inferred, not verified** (not in workspace); and **caught BUG-API-01 (line 99 `>` vs inclusive), left it per spec, flagged a full port carries the discrepancy forward** — "migration is not a cleanup." Effort: "~60/40 mechanical rename vs new logic, the rename half compiler-guided; the real cost isn't this slice, it's the seed regeneration." Verified vs clone (Pbm.java rename, NDC/formulary/DS rules, line 99 untouched). New SIM-25 (vocab swap + green metrics + click-to-expand 3-tier ripple + plant-rides-along). recordings/m125-domain-migration.txt. 36/36 jsdom (incl. eng==60 + T-12==5 assertions). Disposable clone deleted, both teaching repos pristine, all plants intact. No needsVerification |
| M-12.4 | ✅ built + FULL-STACK capstone shipped LIVE across both repos 2026-07-26 | 2026-07-26 | TRI-PATH, T-12 (4/5). The capstone: ship one feature end to end (API endpoint → React screen → tests → PR), defend the diff, read the metrics, ship three ways. **LIVE full-stack (claude 2.1.220), feature = "surface the manual-review queue depth" (UoW-1, previously un-built):** **API 194s** — `GET /api/requests/queue-depth` + new `ReviewQueueService` over derived `countByStatus` + MockMvc & service tests → **41 tests, 0 failures** (6 Testcontainers skipped), **+96/−2**, commit 6137cf3 on feat/queue-depth; **WEB 256s** — `getQueueDepth()` + `QueueDepthBadge` (loading/error) wired to header + RTL test → **23 tests all pass (+3)**, tsc+oxlint clean, **+85**, commit c437012. ~7.5 min full-stack, +7 tests green, both plants intact both repos (verified: api line 99 still `>`; web left ConfidenceGauge/usePriorAuthStatus/RequestTable/SubmitRequest.test alone). **Defend-the-diff = the real skill:** the agent surfaced its own edges (badge fetches-once/goes-stale → poll follow-up; npm install flagged 2 high-sev vulns; unknown-id inherited; plants deliberately left) — "the agent proposes and surfaces its edges; you rule." **Metrics read-out (T-11 applied):** speed with quality holding = a real win (M-11.3), not the amplifier. **Three-ways:** ran ONE tool live (Claude Code) as exemplar; Copilot/Cursor = same tool-agnostic ceremony (M-12.1) = the student's comparison/tool-fit-verdict exercise (M-2.5 overturn-with-evidence) — did NOT re-run them (honest, Copilot cloud needs remote infra). **VISUALS deviation logged:** v1.0 spec named ANIM-12 for the read-out; shipped a dedicated **SIM-24** carrying the real numbers + defend-the-diff panel (ANIM-12 stays the shared ceremony flow). recordings/m124-capstone.txt. 32/32 jsdom. Both disposable clones deleted, both teaching repos pristine, all plants intact. No needsVerification |
| M-12.3 | ✅ built + Mob Construction run LIVE (built the slice, STOPPED at the human unit) 2026-07-26 | 2026-07-26 | TRI-PATH, T-12 (3/5). The Construction ceremony, run for real, paired with M-12.2's elaboration. **LIVE (claude 2.1.220, 137s):** fed the validated units (UNITS.md) with the human ruling recorded; the agent-as-crew built the low-risk slice on branch feat/cut-queue: **UoW-3** (`>`→`>=` at line 99 + boundary test pinning 0.85→AUTO_APPROVED, *constant-referenced so it can't drift* — the M-5.2 mutation lesson unprompted) and **UoW-2** (`POST /{id}/resolve` wiring the untouched `resolveManualReview`, `@NotNull`→400-not-silent-deny), reached **35 tests green (+4)**, committed a minimal **+49/−1** diff (45d5e36), and was honest about edges (flagged 404/409-for-unknown-id as a follow-up unit, didn't over-build). **THE STANDOUT BEAT — it STOPPED at UoW-5** (claim history as decision input) and refused: "that crosses the line from a code change into a change of medical-necessity policy ... needs sign-off from people who can own that, not a passing test suite" — named a clinical lead (the rule + lookback), compliance/legal (payer contracts, CMS/state, **disparate-impact** risk for patients with prior denials, appealable reasons), and a **shadow dry-run** to produce the unknown yield; closed with "it's legitimate for the answer to be that the rule doesn't earn its risk." That refusal IS the module: M-1.6's decision boundary at ceremony scale — the agent may build the mechanism alone but never decide the policy alone; "human as approver, not typist" is a safety property. Verified vs clone (branch, line 99 `>=`, endpoint wired, +49/−1). Composes the arc: M-12.2 elaboration → M-12.3 construction (here) → the diff = M-10.5's PR, gated by the M-10.2 hook. New SIM-23 (build-status board; UoW-5 click reveals the refusal governance). recordings/m123-mob-construction.txt. 31/31 jsdom. Disposable clone deleted, teaching repo pristine, BUG-API-01 line 99 still `>` on main. No needsVerification |
| M-12.2 | ✅ built + Mob Elaboration run LIVE, read-only, git clean 2026-07-25 | 2026-07-25 | TRI-PATH, T-12 (2/5). The first AI-DLC ceremony, run for real. **LIVE (claude 2.1.220, 398s, `--permission-mode plan`, git status 0 changes — Inception writes no code):** the intent "cut the manual-review queue by 20%" → the agent (1) **RESHAPED the intent against the real system** ("there is no queue entity ... no baseline ... nothing can leave the queue via the API" — independently rediscovered the M-3.3 no-resolve-endpoint gap), pulled a real seed baseline (179 in review = 35.8%, 23.5% in [0.75,0.85)), and caught BUG-API-01 as queue inflation; (2) asked **three senior clarifying questions** ("20% of *what*?" backlog/arrival/reviewer-hours; "what false-approve rate buys it?" 0.85→~0.75; "which threshold contract is authoritative?"); (3) decomposed into **six grounded units of work** (instrument-first=T-11's no-baseline-no-delta; POST /resolve=M-3.3 drain-rate-zero; restore `>=`=BUG-API-01; config threshold; claim-history-as-input yield-unknown; batch load=BUG-API-03) each with files + why-it-cuts-the-queue + a **human-validation gate**; (4) the payoff **honest read**: "the intent can't be met by engineering judgment alone — the only known-yield lever (threshold) doesn't reach 20% until ~0.75," a risk trade, surfaced before a line is built. Every unit maps to a REAL repo lever (incl. 2 plants + the M-3.3 gap) — proof the elaboration is grounded, not generic. Teaching: Mob Elaboration's value is catching the un-buildable/needs-a-human-decision intent in Inception when course-correction is free. AI-DLC facts reused from M-12.1 (recordings/m121). New SIM-22 (carries the real run: intent → reshape → 3 questions → 6 gated clickable units → honest read). recordings/m122-mob-elaboration.txt. 32/32 jsdom. Disposable clone deleted, priorauth-api pristine, BUG-API-01 line 99 still `>`. No needsVerification |
| M-12.1 | ✅ built + AI-DLC doc-verified against AWS primary 2026-07-25 | 2026-07-25 | TRI-PATH, **T-12 opens (1/5)**, engineer path 56/60. The graduate arc's orientation: for 11 tracks you learned TOOLS; AI-DLC is the METHOD that organizes them. NEW subject (AWS AI-DLC) — **all facts verified against AWS-owned primary sources** (aws.amazon.com/blogs/devops Jul+Nov 2025; github.com/awslabs/aidlc-workflows MIT-0). **Verbatim anchors:** AI-DLC = "a new methodology designed to fully ingrain AI capabilities into the very fabric of software development" (vs "retrofitting AI as an assistant"); **tool-agnostic** "works with any IDE, agent, or model" (repo lists Claude Code/Copilot/Cursor) → so the whole toolkit slots in; three phases **Inception (WHAT/WHY, Mob Elaboration) → Construction (HOW, Mob Construction) → Operations (deploy/monitor, "future")**; intent stated "Using AI-DLC, ..." → "Epics are replaced by Units of Work"; human gate = **"Humans decide and validate, AI plans and executes" / "the agent proposes, the human approves"** at every phase = the course's own plan-gate/review-diff/sign-rule discipline named. **Hedges kept:** "bolts" + the "hours/days vs weeks" speed claim are Jul-2025-blog vintage (dropped Nov 2025) → presented as AWS's original vocabulary/claim, "units of work" as durable; "bouncer/typist" is the COURSE's gloss (M-12.3 title), NOT attributed to AWS. **Core mandate delivered — the slot map:** each course track mapped to its phase (T-02/03/07 → Inception; T-04/05/06/09/10 → Construction; M-10.5 → converge; M-10.4/T-11 → Operations). No live run (overview; M-12.2 runs the first live ceremony). Built **ANIM-12** (pre-specced, shared M-12.1–12.4+L-6.1): staged INTENT→Mob-Elaboration(3 tagged units)→Mob-Construction(3 tagged builds)→PR→Operations, each with its human gate. recordings/m121-ai-dlc.txt. 33/33 jsdom. No needsVerification |
| M-11.3 | ✅ built 2026-07-25 | 2026-07-25 | TRI-PATH, **T-11 COMPLETE (3/3)**. Engineer path 55/60. Methodology closer (no new external verification — composes M-11.1 baseline + M-11.2 panel/amplifier + the course's own measured numbers). **Spine:** re-run the SAME task class with the full toolkit, compute the delta on the whole PANEL (cycle time / escaped defects / rework), and — the load-bearing move from M-11.2 made personal — **a speed gain only counts if quality held**; a 60%-faster/2×-rework result is the amplifier, not a win (this is why M-11.1 logged three numbers not one). **Honest-reading discipline:** small-n → directional; name the confounds (you also got better at the task independent of AI; a faded Hawthorne can understate the after) → trust the shape not the exact %. **Improvement playbook (the module's deliverable), ordered cheapest-highest-leverage first:** instruction files (T-07 — the real 90s→24s / 36-line constitution anchor) → workflows (T-10 slash commands + hooks) → agents (T-09 subagents/skills, the heaviest); "most teams invert this, buy the fleet first, wonder why the delta is thin." Built **ANIM-11** (pre-specced) as 3 baseline→final gauges (cycle ↓, rework ↓ "watch this one", tests/feature ↑) + a VERDICT panel enforcing read-the-panel; example data (cycle 90→24 = real M-7.1; rest illustrative, "your numbers replace them"); also feeds L-5.4. 33/33 jsdom. No needsVerification |
| M-11.2 | ✅ built + frameworks doc-verified, DORA currency corrected 2026-07-25 | 2026-07-25 | TRI-PATH, T-11 (2/3). Methodology module (no headless artifact; record = dated definitions sheet). **The verification produced a currency correction that BECAME the module's spine (stale-framing catch #7):** DORA "de-acronymed", renamed its report "Accelerate State of DevOps" → **"State of AI-assisted Software Development"**, went **four→five metrics**, and **MTTR→"failed deployment recovery time"**; and the AI finding **FLIPPED** — 2024 said AI hurt BOTH throughput and stability; **2025 (current): AI HELPS throughput but STILL HURTS stability.** Teaching 2023's "four keys / MTTR / AI hurts everything" would have been wrong. **Load-bearing quotes (all primary, dora.dev / cloud.google.com 2025 DORA, 2026-07-25):** the five metrics verbatim; "positive relationship between AI adoption on both software delivery throughput and product performance ... does continue to have a negative relationship with software delivery stability"; the mechanism ("without robust control systems, like strong automated testing, mature version control practices, and fast feedback loops ... instability" = exactly T-06/T-10); the AMPLIFIER money quote "AI doesn't fix a team; it amplifies what's already there"; the WEDGE that kills vanity metrics with primary data — "90% ... use AI", "80% believe it has increased their productivity", "30% ... little or no trust" — coexisting with worse stability. **SPACE** anchored on the solid primary abstract quote ("cannot be measured by a single metric or dimension"); **DX Core 4** "encapsulates DORA, SPACE, and DevEx" / "speed, effectiveness, quality, and business impact". **Honest hedges kept:** 2024's "25%/1.5%/7.2%" numbers are secondary → NOT used; SPACE "(or metric!)"/"never in isolation" lines are ACM-403 secondary → module leans on the primary abstract quote; the crisp "acceptance-rate=engagement-not-value" sentence couldn't be pinned → framed as widely-held + carried by SPACE + the DORA wedge. New SIM-21 (10-card KEEP/REFUSE drill; foot = amplifier finding). recordings/m112-frameworks.txt. 35/35 jsdom. No needsVerification |
| M-11.1 | ✅ built + metric definitions doc-verified 2026-07-25 | 2026-07-25 | TRI-PATH, **T-11 opens (1/3)**. Methodology module (capture your week-1 no-AI baseline) — no headless artifact by nature (like M-1.6/M-4.1); the "recording" is the dated definitions sheet. **Scope held per blueprint:** M-11.1 = baseline CAPTURE (cycle time, escaped defects, rework); DORA/SPACE/DX depth + the vanity-metrics refuse-list are M-11.2 (only lightly teased here); ANIM-11 dashboard is M-11.3's. **Load-bearing thesis (elevates it past "time yourself"):** baseline QUALITY (escaped defects + rework) alongside SPEED (cycle time), because the documented AI-era risk is faster-but-sloppier — a speed-only baseline flatters you. **Doc-verified 2026-07-25** (5 sources, all hedged appropriately): DORA lead-time "committed to version control to deployed in production" (dora.dev); cycle-time = first-commit→released (LinearB, "no single canonical def" → attributed to tooling); **rework/churn** "reverted or substantially revised within ~2 weeks" + rising with AI ≈3.6% 2021→5.5% 2023 (GitClear — cited as *their analysis*, correlation≠causation, projection flagged); escaped defect = "found after the work is done" (Plandek, "commonly defined"); Hawthorne "modify behaviour in response to awareness of being observed" (Oxford CEBM); vanity-metric anchor = SPACE verbatim "productivity cannot be reduced to a single dimension (or metric!)" / activity counts "never used in isolation" (ACM Queue). Uses the course's OWN real timings as illustrations of measured tasks (90s→24s M-7.1, 141s M-10.5) — framed as "what a reading looks like; yours is your task class, no-AI." New SIM-20 (3 clickable metric tiles, speed vs quality, is/is-not/why). recordings/m111-baseline-metrics.txt. 36/36 jsdom. No needsVerification |
| M-10.5 | ✅ built + FULL issue→PR pipeline run LIVE end-to-end 2026-07-25 | 2026-07-25 | TRI-PATH, **T-10 COMPLETE (5/5)**. The track synthesis: issue → plan → branch → build → test → commit → PR, agent-driven, human at both ends only. **LIVE (claude 2.1.220, one headless invocation, 141s):** a committed `ISSUE.md` ("report the manual-review queue depth") → the agent PLANNED (3 files), branched **feat/queue-depth**, IMPLEMENTED minimally (`PriorAuthRequestRepo.countByStatus` derived query + `ApprovalService.manualReviewQueueDepth()` @Transactional(readOnly)), added a mocked-repo TEST, ran the suite **GREEN (32 tests, 0 failures)**, COMMITTED **6088d14 (3 files, +14/−0, purely additive)**, and wrote a real **PR.md** (title/rationale/test plan). **Every headline claim VERIFIED against the clone afterward** (branch, `git show --stat`, tests, line 99) — not trusted. **Two unscripted judgment wins:** (1) chose ApprovalService for the method "because it already owns the queue" (routes into + drains MANUAL_REVIEW) — real architecture call; (2) **found BUG-API-01 on its own** (`>` vs inclusive javadoc), **deliberately left it untouched** per the rule, AND connected it to the feature ("queue depth will be inflated by any exactly-at-threshold requests"). Plant intact (line 99 still `>`). **Composes the whole track:** slash command (M-10.1) kicks / Plan Mode (T-03) plans / hook (M-10.2) gates the commit / MCP (M-10.3) supplies policy / cloud+Action (M-10.4) opens the PR. **Honest boundary:** deliberately did NOT push/`gh pr create` (teaching repo stays clean; PR body is the artifact) — that final step is M-10.4's remote handoff; and the gate/MCP were composed conceptually, not re-wired into this run (each proven live in its own module). Ran under `--dangerously-skip-permissions` precisely because no hook was being observed here (the M-10.2 lesson). New ANIM-32 (7-step pipeline + the found-plant beat, all real numbers). recordings/m105-full-workflow.txt. 34/34 jsdom. No needsVerification |
| M-10.4 | ✅ built + background Claude agent run LIVE (detached, envelope collected) 2026-07-25 | 2026-07-25 | TRI-PATH, T-10 (4/5). Axis = execution LOCUS (where the agent runs + who owns the compute), not capability. **LIVE:** launched a REAL Claude agent **detached** (shell-backgrounded `claude -p "analyze priorauth-api → compact JSON {services, reviewQueuePath}" --output-format json`) and kept working (launched a doc-verification agent + edited files) while it ran, then collected its envelope — **`total_cost_usd 0.2320`, `num_turns 9`, `duration_ms 24211` (~24s), `is_error false`**, wrapping an accurate 6-service + review-queue summary (even reported the boundary as "not strictly greater than 0.85" = a faithful read of the `>` code). Confirms the `-p --output-format json` envelope schema LIVE (result/session_id/total_cost_usd/duration_ms/num_turns/is_error) — the thing that makes headless a scriptable step (branch on is_error, log cost). **Doc-verified 2026-07-25:** Claude `--bg` first-class ("returns immediately with a session ID"), `-c`/`-r` resume with `-p`, background subagents "-p waits ... capped at 10 min"; official Action `anthropics/claude-code-action@v1` ("@claude in any PR or issue ... create pull requests"), `/install-github-app`. **Copilot cloud agent** (docs.github/code.visualstudio): delegate via `@copilot` on issue/PR or VS Code "Delegate this task to the GitHub Copilot cloud agent" button; runs on "remote infrastructure ... ephemeral ... powered by GitHub Actions" (NOT your machine); "works independently while you review changes before creating pull requests" → draft PR. **KEY CONTRAST taught:** Copilot cloud = GitHub-HOSTED compute; Claude Action = YOUR runners (you pay minutes) — a cost/governance choice ("whose machine, where's the log"). **Honest scope:** the background HEADLESS case ran for real; `--bg`, the Action, and Copilot cloud are doc-quoted but need remote infra → verify-in-your-env, stated in-module. New ANIM-31 (2-lane local-vs-cloud w/ the real envelope). recordings/m104-cloud-background.txt. 33/33 jsdom. No needsVerification |
| M-10.3 | ✅ built + real MCP server built & QUERIED live + allowlist gate proven 2026-07-25 | 2026-07-25 | TRI-PATH, T-10 (3/5). **Engineer path hits 50/60.** Built a REAL stdio MCP server (`payer-policy-server.js`, `@modelcontextprotocol/sdk` + zod: `McpServer` → `registerTool('lookup_payer_policy',{inputSchema:{payer,cptCode}})` → `StdioServerTransport`), data mirroring `EligibilityService.COVERED` (MERIDIAN 97110/29881, NORTHSTAR +70553; reason codes PAYER-001/CPT-001/CPT-002). **LIVE RUN 1 (allowed):** `claude -p "...MERIDIAN cover 97110? 70553?..." --mcp-config <f> --allowedTools "mcp__payer-policy__lookup_payer_policy"` → agent CALLED the tool: 97110 **covered**, 70553 **CPT-002 not covered** — matching EligibilityService EXACTLY incl. the null-reason-on-covered detail (only in the tool's real JSON → proof it was invoked, not recalled). **Answers the undocumented question: `claude -p` loads an MCP server via `--mcp-config` and calls its tool headless.** **LIVE RUN 2 (allowlist gate):** same query, un-allowlisted under `--permission-mode dontAsk` → tool **BLOCKED**, and the agent **refused to guess** ("a payer/CPT coverage determination shouldn't come from my recollection") — M-1.6's decision boundary enforced by the allowlist. **Doc-verified 2026-07-25** (mcp.md/mcp-quickstart/headless/permissions): `claude mcp add`/`.mcp.json`, scopes local(`~/.claude.json`)/project(`.mcp.json`)/user; transports stdio + streamable-http (SSE deprecated); namespacing "mcp__<server>__<tool>" verbatim; permissions `mcp__server`/`mcp__server__tool`/`mcp__server__*`, `deny:["mcp__*"]`; enterprise `allowManagedMcpServersOnly` + `allowedMcpServers`; untrusted-folder ignores project enable-settings; SDK minimal pattern. **Teaching motivation:** the payer rule hard-coded in EligibilityService becomes a queryable service. **Honest scope:** built+run on stdio locally; HTTP/remote + OAuth named/quoted but NOT exercised (no `/mcp` OAuth headless) — stated in-module. **NO teaching-repo change** (externalisation taught as a pattern; EligibilityService left as-is so plants + "policy in code" starting point stay intact). New ANIM-30. Used escaped `\'` in module JS strings (learned from M-10.2's raw-quote bug). recordings/m103-mcp.txt. 33/33 jsdom. No needsVerification |
| M-10.2 | ✅ built + pre-commit gate PROVEN live (real red→green) + 2 Windows portability bugs found live 2026-07-25 | 2026-07-25 | TRI-PATH, T-10 (2/5). Built THREE real hooks (`.claude/hooks/` + settings.json), all unit-tested (piped payloads, M-9.3/9.7 method): **format-after-edit** (PostToolUse Edit|Write → normalise .java), **precommit-test-gate** (PreToolUse → run suite, deny red commit), **redact-secrets** (PostToolUse Read → `updatedToolOutput` `sk-`/`AKIA`/`password=`/`Bearer`→`[REDACTED]`). **Pre-commit gate PROVEN live, real before/after** (claude 2.1.220): off-by-one → agent commit → gate ran mvnw ITSELF (independent of agent perms) → `Tests run: 4, Failures: 1` → **DENY, commit blocked**; fix → gate green → **ALLOW, commit landed**. Agent: "`--no-verify` wouldn't dodge it" (Claude hooks non-bypassable, unlike git's pre-commit). **TWO Windows portability bugs found LIVE (the module's sharpest teaching):** (1) matchers target the TOOL NAME — on Windows the shell tool is **PowerShell not Bash**, so `matcher:"Bash"` silently NEVER fires (fix: `Bash|PowerShell`); the agent itself diagnosed it. This also corrected my own mid-debug wrong hypothesis ("--dangerously-skip-permissions bypasses hooks" — FALSE; it was the tool name). (2) `./mvnw` (bash wrapper) won't run under the hook's cmd.exe → gate threw and "failed" EVERY commit (probed: `./mvnw` FAIL, `mvnw.cmd` FAIL, `.\mvnw.cmd` OK) → fix: platform-detect. **Doc-verified 2026-07-25** (hooks.md/hooks-guide.md): PostToolUse "after a tool call succeeds", CANNOT block ("can't undo"), `updatedToolOutput` "replaces the tool's result before Claude sees it"; PreToolUse `if:"Bash(git commit *)"` scoping (NOT relied on — a live agent even claimed it "isn't a real field"; the script filters tool_input.command instead = version-proof); **UserPromptSubmit CANNOT rewrite the prompt** (only block/add context) → "secret redaction" corrected: block secret-file READS or redact tool OUTPUT, never the prompt. **Redaction: unit-tested only** (no live agent read-report run this session; stated honestly, not overclaimed). New ANIM-29 (event lifecycle + 3 patterns pinned + portability caption). Fixed a JS-string bug (raw `'` in a jq example broke the MODS script — caught by validation, escaped to `&#39;`). recordings/m102-hooks.txt. 33/33 jsdom. No needsVerification |
| M-10.1 | ✅ built + /fix-and-test run LIVE, headless slash-command CONFIRMED 2026-07-25 | 2026-07-25 | TRI-PATH, **T-10 opens (1/5)**. Built a real `.claude/commands/fix-and-test.md` (frontmatter: description/argument-hint/`allowed-tools: Bash(./mvnw:*), Read, Edit, Grep, Glob`; body = 4-step ritual using `$ARGUMENTS`). **LIVE (claude 2.1.220, 68s):** planted a NON-plant off-by-one (`ClaimHistoryService.java:29` `.limit(limit)`→`.limit(limit+1)`, confirmed RED 4/1 first), then `claude -p "/fix-and-test ClaimHistoryServiceTest" --dangerously-skip-permissions` → ran the whole ritual: RED 4/1 → root cause line 29 → minimal one-line fix → **GREEN 4/0**, git diff = 1 file/1 line. **Settled the undocumented question: `claude -p "/custom-command args"` DOES invoke a custom slash command headless** ($ARGUMENTS "ClaimHistoryServiceTest" flowed into `mvnw -Dtest=`). **Unscripted discipline win:** it fixed the code not the test, respected plant tags, and FLAGGED the N+1 in `historyFor` as "out of scope, not fixed" rather than scope-creeping — the module's thesis ("the value is the judgement baked into the steps, incl. what NOT to do") shown live. Authentic artifact: this course's OWN `.claude/commands/` (plan/build/build-lab/validate/refresh-module) are five real `$ARGUMENTS` rituals. **Doc-verified 2026-07-25** (slash-commands.md): filename→command name; `.claude/commands/` is "legacy format", `.claude/skills/<name>/SKILL.md` recommended (same `/name` + autonomous), "CLI continues to support both" (ties M-9.1 from the commands side); `$ARGUMENTS`/`$1`; `!`cmd`` bash-inject needs `allowed-tools: Bash(...)`, `@file`; subdirs do NOT namespace; `disable-model-invocation` NOT in commands docs (not used). **Cross-tool doc-verified for labB/labC:** Copilot prompt files `.github/prompts/<name>.prompt.md`→`/name` with `${input:}`/free-form args (agent vs older `mode:` = verify); Cursor commands `.cursor/commands/[command].md`→`/` (NO arg mechanism; 2.4+ folding into `.cursor/skills/` = verify). New ANIM-28 (the file → one invocation → the real RED→GREEN ritual + N+1 flag). recordings/m101-slash-commands.txt. 35/35 jsdom. No needsVerification |
| M-9.7 | ✅ built + SubagentStop quality gate PROVEN live 2026-07-25 | 2026-07-25 | TRI-PATH, **T-09 COMPLETE (7/7)**. The track synthesis: org-chart + least-privilege + a **SubagentStop quality gate** (= M-9.3 enforcement on M-9.5 teams). Built a real gate (`.claude/hooks/review-gate.js` wired as SubagentStop) that reads `last_assistant_message` and blocks a review missing verdict/`cited:`/marker. **Unit-tested first (4 piped payloads, M-9.3 method):** conform→allow, non-conform→block, repeat→loop-guard allow, non-reviewer→ignore. **LIVE (claude 2.1.220, 91s, $0.4429, 5 turns):** a deliberately contract-less `lazy-reviewer` returned "the code looks fine"; the gate **blocked once** and the reviewer came back with **VIOLATION + `cited: ApprovalService.java:99` + `REVIEW COMPLETE (lazy-reviewer)`** — a marker its definition NEVER mentions. **Two independent proofs the gate fired:** (1) the gate's own **counter file = 1** (written only on the block branch); (2) the exact phrase "REVIEW COMPLETE (lazy-reviewer)" (agent name interpolated by MY hook) exists ONLY in the gate's feedback → the reviewer could only have learned it from the gate. **Honest nuances taught:** the block reason is injected server-side, NOT echoed in the `-p` prose → verify a gate by its **effect** (counter file + adopted format), same trace-not-prose discipline as M-9.5; and **SubagentStop has NO `stop_hook_active` loop guard** (Stop does) → bring your own counter (MAX_BLOCKS=1 here). **Doc-verified 2026-07-25** (hooks.md/sub-agents.md/agent-teams.md, verbatim): SubagentStop fires "when a subagent finishes", payload has `last_assistant_message` + agent_type/agent_id/stop_reason; block = exit 2 "prevents the subagent from stopping—the subagent continues" or `{"decision":"block"}`+additionalContext; org-chart `tools: Agent(worker, researcher)` allowlist ("omit Agent → can't spawn any"); nesting off by default (`CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH`); teams "use significantly more tokens ... work best when teammates operate independently"; least privilege "allow only necessary tools for security and focus". New SIM-19 (gate ON/OFF toggle: sloppy review accepted-as-is vs blocked-and-corrected). recordings/m97-agent-team-gate.txt. 37/37 jsdom. No needsVerification |
| M-9.6 | ✅ built + claims doc-verified 2026-07-25 | 2026-07-25 | TRI-PATH, T-09 (6/7). **COPILOT-LED** (no headless surface — doc-verification is the method, M-1.3/M-4.1 precedent). Tri-path kept: labB (Copilot, the subject) is the deep lab; labA compares the Claude subagent from M-9.4; labC compares Cursor. **Blueprint correction = the module's spine (stale-claim catch #6; blueprint text left as-is per M-3.3/M-7.5 convention):** the blueprint's "user-level agents in ~/.github/agents/, @agent syntax" is **surface-specific, not universal** — `@name` mentions + `%USERPROFILE%\.github\agents` are **Visual Studio only**; **VS Code** uses the agents dropdown + `~/.copilot/agents/`; **CLI** uses `--agent <name>` + `~/.copilot/agents/`. Teaching the accurate split avoids any needsVerification flag AND is the lesson ("check your surface; a confident wrong path fails silently for half your team"). **Doc-verified 2026-07-25** (verbatim, 5 live sources): `.agent.md` = frontmatter (name/description=chat placeholder/tools "unset defaults to ALL tools"/model/user-invocable/disable-model-invocation) over a body "prepended to the user chat prompt"; four scopes repo `.github/agents/` → user → org/`.github`(-private) → enterprise, **precedence "the lowest level configuration takes precedence ... repo > org > enterprise"** (verbatim, = M-7.1 hierarchy at company scale); isolation "the subagent has its own context window" (= M-9.4, second tool); org sharing → "available to Copilot cloud agent on GitHub.com, ... in IDEs, and GitHub Copilot CLI"; naming "previously known as custom chat modes ... rename [.chatmode.md] to .agent.md" (docs don't say the literal word "deprecated" → module says "previously known as/rename", not "deprecated"); VS requires 2026 v18.4+; preview for JetBrains/Eclipse/Xcode. New ANIM-27 (abstract 3-lane staged diagram, cyan/Copilot, no fake IDE chrome — one file / four scopes nearest-wins / per-surface invocation + cross-tool caption). recordings/m96-copilot-custom-agents.txt. 32/32 jsdom. No needsVerification |
| M-9.5 | ✅ built + parallel fan-out run LIVE, concurrency PROVEN from the trace 2026-07-25 | 2026-07-25 | TRI-PATH, T-09 (5/7). Built three read-only layer reviewers (`.claude/agents/{controller,service,repo}-review.md`, each `tools: Read, Grep, Glob`, marker-ended) and fanned them out over priorauth-api. **Two runs, claude 2.1.220.** RUN A (plain text, 95s): three DISTINCT real findings — controller → `resolveManualReview` has NO HTTP endpoint (M-3.3 gap, "read the queue but no door to approve/deny"); service → strict `>` at 0.85 (BUG-API-01); repo → N+1 in ClaimHistoryService + dead batch finder called only from tests (BUG-API-03), traced to commit 674272e. **Unscripted convergence** (kept as a teaching beat): controller + repo reviewers independently flagged the same N+1 from opposite directions, service noted it too — "the one defect all three layers saw." BUT the synthesized prose had **0 marker lines** — the parent summarized them away, so the text alone can't prove delegation (the M-9.4 probe problem returns). RUN B (`--output-format stream-json --verbose`, 93s) SETTLES IT: (1) all three markers appear in the stream 3× each (inside subagent tool_result blocks) → all three genuinely ran; (2) **concurrency PROVEN by event ordering** — all three DISPATCHED before ANY returned (background default v2.1.198), internal Read/Grep calls interleaved, RETURNED OUT OF DISPATCH ORDER (service→repo→controller = finished in completion order, not sequence). **MEASURED ECONOMICS** (result event): `duration_api_ms` **185,890** vs wall-clock `duration_ms` **87,981** = **2.1× — ~186s of review delivered in ~88s** (the concurrency dividend, numeric); parent's OWN output just **1,415 tokens** (cache_read 52k) despite three whole-layer reviews (isolation, numeric); **$0.6989** total (opus+haiku per M-8.1). **Honest thesis, tested not borrowed:** parallel buys wall-clock + a lean parent, **NOT fewer tokens** — you pay for every child (pre-empts "parallel=cheaper" like M-8.4 did "cheap model=cheaper"; no doc claims parallel is cheaper). **Key advanced lesson:** when the parent synthesizes, the marker leaves the prose and lives in the trace → verify a fan-out with stream-json, not the summary. **Doc-verified 2026-07-25** (sub-agents.md / agent-sdk/subagents.md): "concurrently... time of the slowest... rather than the sum"; 20-concurrent cap (`CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS`); background default v2.1.198; "only its final message returns to the parent." UNDOCUMENTED→TESTED: per-subagent cost attribution (none; only total_cost_usd), whether -p fans out (it does). **Reused SIM-05 with `fanout:true`** opts flag (SIM-09-autotrace precedent): swaps to the 3 real layer reviewers + measured foot; M-9.4's plain mount unchanged (both harness-asserted). recordings/m95-parallel-fanout.txt. 37/37 jsdom (M-9.4 re-run 38/38, M-9.3 39/39 — no regression from the shared-visual change). No needsVerification |
| M-9.4 | ✅ built + first subagent run LIVE, three beats measured 2026-07-25 | 2026-07-25 | TRI-PATH, T-09 (4/7). Built a real read-only `.claude/agents/pa-reviewer.md` (`tools: Read, Grep, Glob` — the docs' own code-reviewer scoping that "excludes Edit and Write"; 4 signed PA rules + a MANDATORY marker-ended output format so firing is detectable). **Three runs, claude 2.1.220:** (1) natural-language headless review → main agent did it ITSELF ("confirmed by reading the file directly"), **NO marker** → delegation is NOT automatic headless — the M-9.2 probe discipline again (a right answer ≠ the mechanism fired). (2) explicit "use the pa-reviewer subagent" → **DELEGATED**: "the pa-reviewer found one violation and three passes," having read ApprovalService/EligibilityService/PriorAuthRequest/test file **in its own context** and returned a compact verdict → isolation payoff shown live (parent got the summary, not the files = the T-08 token argument). (3) `--agent pa-reviewer` + "fix it now" under `--permission-mode acceptEdits` → **could NOT edit** ("I don't apply code changes, my role is review only"), emitted the enforced PA DOMAIN REVIEW + marker, **git diff EMPTY** → read-only tool scoping is a HARD capability boundary, not etiquette. **Contrast taught vs M-9.3:** a hook DENIES an action at the gate; tool scoping means the action was never granted ("design the door, not the sign"). Bonus: a REAL domain review — 1 VIOLATION (BUG-API-01 plant, cited vs javadoc + contradicting log) + 3 PASSES (no auto-deny / history advisory / CPT `\d{5}`), not just bug-hunting. **Doc-verified 2026-07-25** (sub-agents.md, verbatim): location/frontmatter (name/description/tools "Inherits every tool... if omitted"/model default inherit/permissionMode); proactive delegation via "use proactively" in description; three invocation patterns (natural language / @-mention "guarantees" / `--agent` session-wide); isolation "does that work in its own context and returns only the summary"; /agents wizard removed v2.1.198 (edit files directly); Task→Agent rename v2.1.63. **Headless project-subagent auto-delegation is UNDOCUMENTED — tested, not asserted** (Run 1 showed it doesn't auto-fire; name it). New SIM-05 built (org chart, delegate/solo toggle; shared w/ M-9.5). recordings/m94-first-subagent.txt. 38/38 jsdom. No needsVerification |
| M-9.3 | ✅ built + enforce-vs-advise MEASURED live 2026-07-25 | 2026-07-25 | TRI-PATH, T-09 (3/7). The boundary module, and the measurement is a **controlled A/B on one variable**. Fixed ApprovalService to the correct inclusive `>=`, installed a real PreToolUse **hook** (`.claude/settings.json` matcher `Edit\|Write\|MultiEdit` → `node .claude/hooks/guard-threshold.js`) that reads the proposed edit CONTENT off stdin and denies any reintroduction of strict `>` at the 0.85 boundary (deny JSON: `hookSpecificOutput.permissionDecision:"deny"`, doc-verified). Under `--permission-mode acceptEdits` (so ONLY the hook can gate): agent told to make exactly-0.85 not auto-approve → **write DENIED, git diff EMPTY**, agent even declined to dodge the string-match. **CONTROL** = identical prompt/model/mode, hook removed (one variable) → **edit LANDED, `>=`→`>`**, regression back in tree, model obeying past its own javadoc that says INCLUSIVE. Killer detail: the advisory layer (docs/skill) is exactly as reliable as model discretion (ties M-8.4's confidently-wrong cheap model); a hook is not. **Doc spine (skills.md, verbatim)**: "strengthen the skill's description... OR use hooks to enforce behaviour deterministically" — the docs draw the persuade-vs-enforce line themselves. Subagent = isolation (subagents.md "fresh, isolated context window... doesn't see... the files Claude has already read"; `context: fork`). Honest limit taught: a hook always RUNS but only checks its predicate (the "nudge the constant" sidestep) — determinism ≠ completeness. **Cross-tool convergence finding (2026-07-25)**: all three tools now ship all three primitives with the SAME deny semantics (exit 2 / permissionDecision:deny, fail-closed preToolUse) — Copilot hooks-reference + `.agent.md` sub-agents "isolated context"; Cursor hooks "observe, block, or modify" + subagents "clean context" + SKILL.md. Boundary NOT stealing M-10.2 (deep hooks) — this is when-to-use-which. New SIM-18 (route 9 needs 3/3/3 + proof strip). recordings/m93-skill-hook-subagent.txt. 39/39 jsdom. No needsVerification |
| M-9.2 | ✅ built + interop asymmetry MEASURED 2026-07-24 | 2026-07-24 | TRI-PATH, T-09 (2/7). **Blueprint claim VERIFIED (it held)**: GitHub changelog — "if you've already set up skills for Claude Code in the .claude/skills directory in your repository, Copilot will pick them up automatically"; VS Code recognises `.claude/skills/` + `.agents/skills/` as project skill dirs alongside `.github/skills/` and `~/.copilot/skills/`; agentskills.io open standard; reach = Copilot coding/cloud agent, CLI, code review, VS Code, Visual Studio, JetBrains. **NEW FINDING the docs don't state — the interop is ONE-DIRECTIONAL.** Naive test (skill only in `.github/skills/`) *appeared* to fire — full house format, 10 turns — but the M-7.2 registration probe ("don't read files; list registered project skills") returned **"I can't identify any as project skills"**; from `.claude/skills/` it named threshold-audit with **zero file reads**. So the apparent firing was the agent exploring and reading the file at full cost. **Conclusion: the shared file belongs in `.claude/skills/`** — put it in `.github/skills/` and you get a skill for Copilot and an expensive accident for Claude. Third time discovered-vs-loaded flipped an answer. Cursor support deliberately NOT claimed (verify-in-your-version). New SIM-17. recordings/m92-one-skill-two-tools.txt. 27/27 jsdom. No needsVerification |
| M-9.1 | ✅ built + skill firing tested WITH A CONTROL 2026-07-24 | 2026-07-24 | TRI-PATH, opens T-09 (1/7). Wrote a real `.claude/skills/threshold-audit/SKILL.md` with a mandatory output format + closing marker so firing is **detectable**. **POSITIVE**: prompt never named the skill ("Is the auto-approve threshold comparison in this codebase correct?") → **FIRED**, 8 turns, produced the enforced THRESHOLD AUDIT format with all four values correct (operator `>`, documented inclusive, MISMATCH, 13 rows). **CONTROL**: unrelated question same repo/skill → **stayed silent**, answered the real question — proving the description discriminates. Craft lesson: **the description field is the matching surface**, written in the user's words with triggers enumerated; a skill that fires on everything is an expensive preamble. Doc-verified: skills load **only when used** (the direct answer to M-7.3's rent problem → "facts in the constitution, procedures in skills"); **custom commands have MERGED into skills** (.claude/commands/x.md ≡ .claude/skills/x/SKILL.md, both give /x); optional frontmatter `allowed-tools`, `disable-model-invocation`, `context: fork`; Agent Skills open standard (sets up M-9.2). New ANIM-26. recordings/m91-skill-anatomy.txt + m91-SKILL.md.txt. 27/27 jsdom. No needsVerification |
| M-8.4 | ✅ built + small-vs-large MEASURED live 2026-07-24 | 2026-07-24 | CLAUDE-LED, **T-08 COMPLETE (4/4)**. Two task types, two models. **GRIND** (count rows at 0.85): both answered **13**; small $0.0912 vs large $0.1612 = only **1.8×** — because both did the same 4 turns and carried context doesn't get cheaper when the model does (2nd finding this track that the model isn't where the money is). **JUDGMENT** (M-7.1's threshold question): small model 7 turns **$0.0368** and **WRONG** — asserted "auto-approves at >= 0.85 (inclusive)" citing javadoc+constant+decision block by line number, when line 99 is strict `>`; it reported the javadoc's semantics as the code's behaviour and flagged NO discrepancy, while the large model led with "the repo contradicts itself" + 4 sources + the introducing commit. Ground truth re-verified from source before publishing. Teaching: failure mode is **fluency, not stupidity**; routing test is **"can you CHECK the answer?"** not task size; fallback chains escalate on **task type, never confidence** (the small model was maximally confident while wrong). Blueprint's required **three-way cost-model comparison** included as *structure* (metered/Claude vs per-seat/Copilot vs hybrid/Cursor) with an explicit "don't quote our price points" instruction — harness asserts no dollar-per-seat figures. New SIM-16. recordings/m84-model-economics.txt. 29/29 jsdom. No needsVerification |
| M-8.3 | ✅ built + hypothesis TESTED AND DISPROVED live 2026-07-24 | 2026-07-24 | CLAUDE-LED, T-08 (3/4). Same question, three framings: **A rediscover** 9 turns $0.3267 · **B anchored, unconstrained** 9 turns **$0.4447 (MORE than rediscovery)** · **C anchored + "answer from these facts alone, don't read files"** **1 turn $0.0611 = 19% of A**. **My hypothesis (re-anchoring saves money) was wrong and is reported as such in-module** — the anchor didn't stop a capable agent verifying and going deeper (more output, same turns). Corrected lesson, which is better than the original: **two separable levers — re-anchoring buys ACCURACY, scoping buys ECONOMY**; people pull only the first and are surprised the bill rises. Full pattern taught in order (checkpoint→/clear→re-anchor→scope) with the 4th step named as the one everyone skips. Compaction survival facts reused from M-7.3 (project-root CLAUDE.md re-read+re-injected; nested not; conversation = summariser fodder). Lab includes a correctness caveat (a scoped answer built on wrong facts is "confidently wrong and cheap"). New ANIM-25. recordings/m83-compaction-reanchor.txt. 27/27 jsdom. No needsVerification |
| M-8.2 | ✅ built + paste-vs-path MEASURED live 2026-07-24 | 2026-07-24 | CLAUDE-LED, T-08 (2/4). Same question ("how many rows score exactly 0.85?"), same repo, same correct answer (**13**), two delivery methods: **PASTE** (193KB seed.sql piped in) 3 turns, cache_wr 103,192 + cache_rd 261,017 = 364,215 in-tokens, **$1.2525**; **REFERENCE BY PATH** 4 turns, cache_wr 6,465 + cache_rd 101,786 = 108,259, **$0.1292**. **9.7× cost ratio — $1.12 wasted on one question.** Killer detail: the cheap run used MORE turns (4 vs 3) — "you spent a dollar to save three cents" (a turn ≈ $0.028 per M-8.1). Mechanism taught: pasted content is written to cache then re-read EVERY turn, while a path lets the agent grep/slice — cache creation 103,192 vs 6,465 (~6%). Nuance kept: paste is right when content isn't on disk (a colleague's trace, unreachable system output) — and even then paste the slice. SIM-06 built to spec with both real runs. recordings/m82-context-pollution.txt. 26/26 jsdom. No needsVerification |
| M-8.1 | ✅ built + three runs METERED live 2026-07-24 | 2026-07-24 | **CLAUDE-LED** (T-08 reduced-path per BLUEPRINT — labA only; harness asserts the pill + single lab tab), opens T-08 (1/4). Three tasks metered via the JSON usage envelope: trivial 1 turn **$0.0134** (25,512 cache rd) · single-file 2 turns **$0.0648** · multi-step 10 turns **$0.2834** (109,867 cache rd + 14,484 wr + 3,320 out). **Headline: typed prompt 7 tokens vs 124,358 carried input = 1:17,765** — you pay for the conversation the agent has with itself. **Turns are the multiplier: 21.1× from 1→10 turns (~$0.028/turn)** while prompts were 2/4/7 tokens. **Per-model attribution from `modelUsage`**: one invocation ran TWO models — haiku $0.0006 (**0.22%**) vs opus $0.2828 (**99.78%**) — pre-empting "just use a cheaper model" ahead of M-8.4. M-7.3's steady-state-vs-cache-creation caveat carried forward as a non-negotiable methodology rule, with a lab step that demonstrates the spike deliberately. New ANIM-24. recordings/m81-anatomy-of-a-turn.txt. 27/27 jsdom. No needsVerification |
| M-7.5 | ✅ built + stale-command trap tested LIVE 2026-07-24 | 2026-07-24 | TRI-PATH, **T-07 COMPLETE (5/5)**. Deliberately stale CLAUDE.md put in front of an agent told to "run the test suite" (55s): it caught BOTH errors, and the difference is the module — `npm run dev` = **loud staleness** (obviously wrong ecosystem, caught on sight); `-Pfast-tests` = **quiet staleness**, the dangerous kind: "the profile doesn't exist ... Maven will warn and **run the full suite anyway**, so -Pfast-tests is doing nothing" — it succeeds while doing something other than it claims, so it survives for months. Also refused to invent a result when blocked, and flagged leftover target/surefire-reports from my earlier drills as stale artifacts (cleaned). All four anti-patterns carry measurements from THIS track: essay (+3,584 tok, M-7.3), stale command (here), duplicate drift (M-7.2 labC), un-pruned index → silent truncation (M-7.4). Seven-question audit checklist. **Self-audit finding included in-module**: the course's own authoring CLAUDE.md still says "ONE module per session" after the 2026-07-23 continuous-mode directive — flagged, NOT edited (owner's call). New SIM-15 interactive audit. recordings/m75-antipatterns.txt. 28/28 jsdom. No needsVerification |
| M-7.4 | ✅ built + real memory dir measured 2026-07-24 | 2026-07-24 | TRI-PATH, T-07 (4/5). Live artifact is **this course's own auto-memory directory** (accumulated while building these modules): MEMORY.md index **479 B / 3 lines** loaded every session vs ~4.2 KB of topic files loaded on demand → **only ~10% loads**; index sits at 1.9% of the 25KB cap. Teaches the two-system split (CLAUDE.md = you write, loaded in full; auto memory = Claude writes, index-only), capture destinations by plain language, /memory + /context, and pruning ("a memory that has done its job is debris, and debris is rent") using a REAL example from this build — the next-session-m23 memory deleted the day M-2.3 shipped. Failure mode taught: an over-cap index is **silently truncated**, not errored. **Stale-naming catch #5**: the `#` quick-capture shortcut is absent from the current official memory docs and has public reports of failing to save → taught as verify-in-your-version, with natural language + /memory as the documented paths. Dividing line: *learnings may be discovered, rules must be signed*. New ANIM-23. recordings/m74-memory-practice.txt. 27/27 jsdom. No needsVerification |
| M-7.3 | ✅ built + context MEASURED live 2026-07-24 | 2026-07-24 | TRI-PATH, T-07 (3/5). Real token accounting via `claude -p --output-format json` usage envelope, identical trivial prompt across three contexts: **floor 25,514 tok** (bare repo — system+tools+harness, for a 4-token answer); **+1,025** for M-7.1's lean 36-line CLAUDE.md; **+4,609** for a deliberately bloated 170-line version (dir listing + deps + restated architecture) = **+3,584 bloat penalty every session**. HONEST COST CAVEAT recorded and taught: the $0.013/$0.052/$0.088 spread is dominated by **cache creation** (a changed prefix must be re-written; writes > reads), so the defensible claims are (a) size delta is permanent, (b) the price spike is paid on every EDIT/cold start, (c) a churning memory file costs more than its length implies — explicitly NOT "bloat costs 6.5x per turn forever". /compact vs /clear doc-verified (project-root CLAUDE.md re-read + re-injected; nested ones aren't; conversation-only instructions are summariser fodder → "if it must survive, it must be in a file"). SIM-03 built to spec minus the cost-per-turn meter (would overclaim — deviation logged in VISUALS.md). recordings/m73-context-budget.txt. 25/25 jsdom. No needsVerification |
| M-7.2 | ✅ built + THREE-RUN experiment LIVE 2026-07-24 | 2026-07-24 | TRI-PATH, T-07 (2/5). Tested the terse doc claim ("Claude Code reads CLAUDE.md, not AGENTS.md") three ways with a probe rule that CONTRADICTS the code (new classes = `*Policy`, all existing are `*Service`): **RUN A** (AGENTS.md alone, free exploration, 30s) — found it, cited `AGENTS.md:5-8`, i.e. **read it as a file** → "Claude ignores AGENTS.md" is FALSE; **RUN A2** (same, forbidden to read files, 11s) — *"My loaded context does not contain that rule"* → definitively NOT auto-loaded; **RUN B** (4-line CLAUDE.md starting `@AGENTS.md`, no file reads, 6s) — *"The rule IS in my loaded context"* + grandfathering clause. Thesis sharpened from the docs: the axis is **discovered vs loaded** (discovery depends on the agent choosing to look; loading is guaranteed). Cross-tool matrix verified: VS Code Copilot auto-reads workspace-root AGENTS.md since v1.104 (`chat.useAgentsMdFile`, default on); Cursor reads root+subdirs; VS 2026 does NOT; Claude bridges via import (symlink needs admin on Windows). `.agent.md` ≠ AGENTS.md caution included. New SIM-14. recordings/m72-agents-md.txt + m72-AGENTS.md.txt. 23/23 jsdom. No needsVerification |
| M-7.1 | ✅ built + before/after run LIVE 2026-07-24 | 2026-07-24 | TRI-PATH, opens T-07 (1/5). **The payoff the repos stayed bare for.** Identical question ("at exactly what score does a request auto-approve, and what should I implement?") asked twice: **BEFORE** (no CLAUDE.md, 90s) — brilliant forensics, found the bug, tabled 4 contradicting sources, then said "I can answer what the code does, but not what the rule is" and **recommended implementing the BUG** (`>`, for consistency with production); **AFTER** (36-line CLAUDE.md, 24s) — definitive `>=`, cited the signed rule by name, and unprompted warned "do not copy the current code", flagged line 99, named BUG-API-01, carried rules 2+4 forward. Delta: 90s→24s, "cannot determine" → cited, **recommendation FLIPPED from bug to policy**. Constitution kept as recordings/m71-priorauth-api-CLAUDE.md.txt; repo restored BARE (students build their own). Memory hierarchy doc-verified same day (managed/user/project/local + on-demand subdirs, concatenated-not-overridden, @path 4 hops, <200 lines, context-not-enforcement). ANIM-10 built — spec said 3 cards, shipped **4** (managed-policy scope added post-verification), noted in VISUALS.md. recordings/m71-constitution.txt. 25/25 jsdom. No needsVerification |
| M-6.5 | ✅ built + full TDD loop run LIVE 2026-07-24 | 2026-07-24 | TRI-PATH, **T-06 COMPLETE (5/5)**. Real red→green→mutation-gate on a NEW additive behavior (ReviewAgeService: SLA ageing FRESH/AGING/BREACHED off the injected Clock — deliberately the opposite verdict from M-5.4, since this code DOES read time). RED = genuine compile failure ("package ...ReviewAgeService does not exist"), 11 tests pinning day 0/2/3/6/7/partial. GREEN = 11/11, test file untouched, minimal @Service impl. GATE = 5 mutations (both comparisons, both constants, toDays→toHours) **all CAUGHT: 5/5 = 100%** vs M-6.4's retrofitted **2/5 = 40%**. MECHANISM verified in the test source (not assumed): TDD assertions speak requirement-language (`Duration.ofDays(2)`) and never reference the impl's constants, so a moved constant still fails — M-5.2's retrofit referenced AUTO_APPROVE_THRESHOLD and followed it, which is why 0.85→0.84 survived there. Honest n=1 scope stated in-module. New ANIM-22. recordings/m65-test-first.txt. Clone pristine, all plants intact. 25/25 jsdom. No needsVerification |
| M-6.4 | ✅ built + REAL mutation campaign run 2026-07-24 | 2026-07-24 | TRI-PATH, T-06 (4/5). Five mutations vs the SHIPPED 31-test unit suite (green throughout, each applied→run→reverted): **SURVIVED** — threshold operator `>`→`>=`, threshold constant 0.85→0.84, history gate `>3`→`>=3`; **CAUGHT** — CPT regex \d{5}→\d{4} (6 failures), recentClaims limit+1 (1 failure). **Mutation score 2/5 = 40%, and all three survivors are the auto-approve decision** — the rule the domain turns on — while `process()` is executed by 10 tests (coverage looks perfect). Closes the thread seeded in M-6.1 (mirror test can never be killed) and M-5.2 (mutation-B: constant unpinned even after the fix): shipped baseline is 0/3 on the decision. Teaches cure-a-survivor-then-prove-the-kill by re-mutating. ANIM-09 built to its v1.0 spec with a deliberate colour-semantics deviation (green=CAUGHT), documented in VISUALS.md. recordings/m64-mutation-campaign.txt. 24/24 jsdom. No needsVerification |
| M-6.3 | ✅ built + live generation caught a real bug 2026-07-24 | 2026-07-24 | TRI-PATH, T-06 (3/5). BUG-WEB-03 = deleted form-validation tests (commit daee497 pruned 59 lines); validate()/submit() ship untested. Disciplined spec-anchored regeneration (M-6.1 contract, 181s, source untouched) wrote a boundary-thorough suite → LIVE run **29 tests, 28 green, 1 RED**: a whitespace-only confidenceScore is silently accepted because `Number('   ')===0` (not NaN) slips the `===''||isNaN` guard → submit() sends a score the user never typed. The deleted tests had guarded this; regeneration re-caught it first run. 4th "agent can't run vitest → traced by hand, predicted the exact red, human confirmed" occurrence. Teaching: RTL for the form (accessible queries) vs pure unit tests for validate(); "write the missing tests" resurrects the hidden bug. New SIM-13 boundary grid (its own validate() port independently flags the whitespace row — harness-verified 7 ok/1 gap). recordings/m63-rtl-form.txt. Source left pristine (gap not fixed — scope call). 23/23 jsdom. No needsVerification |
| M-6.2 | ✅ built + skip-behavior verified LIVE 2026-07-24 | 2026-07-24 | TRI-PATH, T-06 (2/5). Testcontainers module, taught from the real committed RepositoryIntegrationTest + TestcontainersConfiguration (@ServiceConnection PostgreSQLContainer postgres:16-bookworm). Docker DOWN this session (machine state, same as M-2.2) — turned into the live artifact: `./mvnw test` → **37 run, 6 skipped, 31 green, BUILD SUCCESS** (disabledWithoutDocker=true skips the DB tests, doesn't fail them). Docker-UP 37/37 cited from the 2026-07-20 repo build. Core teaching: mock proves LOGIC, container proves the DB SEAM (SQL/mapping/constraints/@EntityGraph); the batch-IN integration test proves M-5.6's fix returns correct grouped rows (mock only counted calls). New ANIM-21 two-column proof-matrix + Docker up/down meter. recordings/m62-testcontainers.txt. 22/22 jsdom. No needsVerification (Docker-up 37/37 re-confirm is the standing 2-min note) |
| M-6.1 | ✅ built + TWO live generations during build 2026-07-24 | 2026-07-24 | TRI-PATH, opens T-06 (1/5). Target: ConfidenceGauge (BUG-WEB-04 — inline `score > 0.85` vs its own "INCLUSIVE" doc + the correct meetsThreshold helper). Three test-quality patterns, each a real artifact: (1) shipped test mirrors 0.92/0.61, never 0.85 — green & certifies the bug; (2) NAIVE "make the suite pass" (148s) wrote a good 0.85 test then **silently edited the component** (swapped `>` for meetsThreshold) — erased the plant inside a "write tests" task; (3) SPEC-ANCHORED + "don't touch source, a failing test is the finding" (120s) — source untouched (git diff empty), 3 boundary tests RED against the bug, agent traced it to commit 93f67f8 and refused to fix. Lesson: a generated test's value = what it can CATCH, governed by the prompt contract. New SIM-12 three-tab panel. BUG-WEB-04 left intact. recordings/m61-test-generation.txt. 21/21 jsdom. No needsVerification |
| M-5.6 | ✅ built + BOTH fixes verified LIVE during build 2026-07-23 | 2026-07-23 | TRI-PATH, **T-05 COMPLETE (6/6)**. The performance pair, both measured for real. HALF A (BUG-API-03, M-3.5's payoff): query-counting probe on a 25-patient queue → **25 queries before, 1 after** the batch fix (groupingBy over findByPatientRefIn); existing ClaimHistoryServiceTest 4/4 green (contract preserved), full *ServiceTest 22/22. HALF B (BUG-WEB-01): render-counting probe → **50 rows recomputed on an identical poll**; fix = React.memo Row + useCallback (profiler is the lab instrument). Theme: correctness tests stay green through both — only a COST instrument sees them. REUSES ANIM-17 (M-3.5's fan-out, now the fix target). Both clones restored pristine, plants intact. Self-caught: a naive computeIfAbsent-on-immutable-list fix threw UnsupportedOperationException — fixed to groupingBy shape by running it. recordings/m56-performance-pair.txt. 20/20 jsdom. No needsVerification |
| M-5.5 | ✅ built + labA verified LIVE during build 2026-07-23 | 2026-07-23 | TRI-PATH, T-05 (5/6). BUG-WEB-02 (stale closure) fixed on a scratch branch — FIRST module driven by the priorauth-web toolchain (React 19/Vitest/RTL/fake timers, npm not Maven). Bug: usePriorAuthStatus hoisted `const inFlight = getRequest(id)` out of the tick, so every poll re-awaits one settled promise → API called ONCE ever, status frozen on first value; doc comment still promises fresh copies (comment-is-the-bug signature). Two exposing tests written first, REAL red captured (transition mock: "expected 'SUBMITTED' to be 'AUTO_APPROVED'"; call-count: "expected 1 to be greater than 1"); agent diagnosed the hoisted promise + explained why both shipped tests were blind (constant mock + vacuous unmount assertion), fixed +2/-4 lines; **agent again could not run the suite (permission layer) — human verified 20/20 + 2 exposing green** (3rd occurrence, reinforces the final-human-validation theme). New ANIM-20 dual-lane poll. recordings/m55-stale-closure.txt. 21/21 jsdom. No needsVerification |
| M-5.4 | ✅ built + labA verified LIVE during build 2026-07-23 | 2026-07-23 | TRI-PATH, T-05 (4/6). BUG-API-02 (the time bomb M-5.1's agent flagged unprompted). A real calendar probe turned "flaky" deterministic: the sanity assertion fails the **last 3 days of every month = 36/year = 9.9%**, today (Jul 23) passes, next red **Jul 29** — all from a live run. Agent diagnosis (88s): the KEY distinction — EligibilityService.check reads no clock, is fully deterministic and always passes; the flakiness is entirely in the TEST scaffolding (LocalDate.now()). Fix menu smallest-first (delete 4 sanity lines / pin to LocalDate.of(2026,6,1)) and correctly REFUSED injecting a Clock into innocent code ("a seam for nothing"). Deliberate contrast with M-1.5 (Clock injection WAS right there — that code stamps decidedAt). New SIM-11 calendar heatmap (365 cells, 36 red, shipped/fixed toggle → all green with zero prod change; no Date.now() — static data). recordings/m54-flaky-test.txt. 25/25 jsdom. No needsVerification |
| M-5.3 | ✅ built, VS claims doc-verified + labA equivalent run LIVE 2026-07-23 | 2026-07-23 | TRI-PATH via nearest-equivalent rule, T-05 (3/6). Visual Studio can't run on this machine (M-1.3 precedent) — so the CLAUDE-side equivalent ran for real: a temporary probe test printed the actual runtime frame at the boundary (score=0.85, threshold=0.85, `>` false, `>=` true, **Double.compare = 0**, status MANUAL_REVIEW) AND the app emitted the false log line "score 0.85 < 0.85" unprompted — confirming M-3.4's static prediction word for word (recordings/m53-runtime-frame.txt; probe deleted, clone pristine). VS claims doc-verified with exact quotes: Debugger Agent "validates bugs against real runtime behavior instead of relying on static analysis", all six steps incl. **final human validation**, Debugger mode dropdown / @debugger, conditional-breakpoint suggestion flow, Ask/Analyze with Copilot, # IDE references. **Convergence recorded**: VS ships as step six exactly what M-5.1 discovered live. New ANIM-19. 22/22 jsdom. No needsVerification |
| M-5.2 | ✅ built + labA verified LIVE end-to-end during build 2026-07-23 | 2026-07-23 | TRI-PATH, T-05 (2/6). **BUG-API-01 actually fixed on a scratch branch, red→green→mutation, all runs real.** Plan (224s, zero edits) quantified the blast radius from seed.sql: 0.85 is the ONLY value where the operators disagree (2-decimal scores) — 13 rows, 10 already AUTO_APPROVED ("outcomes the shipped code cannot produce — the fixture is the assertion"), 2 SUBMITTED = 8% of the pending queue mis-routed; survived because "every score in the suite is ≥0.05 away from 0.85". RED captured (12 tests/1 failure), one-line fix, GREEN 24/24, test file additions-only. Mutation A (revert operator) → exactly 1 failure. **Mutation B (keep >=, constant 0.85→0.84) → ALL GREEN — unplanned discovery: the suite pins the operator, nothing pins the value**; taught as the honest limit, M-6.4 owns the general case. SIM-04 built to spec (integer cents after a real float-collision bug my own harness caught). recordings/m52-threshold-fix.txt. Clone restored pristine; plant tag intact. 25/25 jsdom. No needsVerification |
| M-5.1 | ✅ built + labA verified LIVE during build 2026-07-23 | 2026-07-23 | TRI-PATH, opens T-05 (1/6). Full debug loop ran for real on a scratch branch: hand-planted one-character regex regression (\d{5}→\d{4}) → REAL surefire output, 6 failures with mutually contradictory messages + 10 green in the mocked suite; agent found the single root cause in 89s and explained the short-circuit cascade AND why the green suite stayed green (it mocks the service); fix = one line, zero test edits (leash held). **The agent could not run verification** (permission layer blocked mvnw) — said so plainly, offered surrogate evidence, asked for approval; human closed the loop: 22/22 green, tree restored to pristine. Unprompted bonus: it flagged BUG-API-02's time-bomb test — recorded, deliberately NOT spoiled in module copy (harness asserts). New ANIM-08 with a context dial driving lap count. recordings/m51-debug-loop.txt. 24/24 jsdom. No needsVerification |
| M-4.4 | ✅ built 2026-07-23 | 2026-07-23 | DUAL-PATH, **T-04 COMPLETE (4/4)**. The layer drill: 12 tasks vs the four rungs, priced with the course's own recorded numbers ($0.89/9-turn envelope, 13-row seed answer, M-2.1/2.2/3.2 callbacks); misrung-in-both-directions labs with deliberate timings (incl. knowingly using Cursor's Ctrl+I trap chord); three sorting questions (edit at all? / selection contains it? / needs to read-run beyond the open file?) compose with M-2.4 mode + M-2.5 tool habits. No new tool claims (all doc-verified earlier this week). New SIM-10 (neutral pick styling — tool colors stay reserved). One harness check initially looked for a number in the wrong element — fixed in the harness, not the module. 19/19 jsdom. No needsVerification |
| M-4.3 | ✅ built, claims doc-verified 2026-07-23 | 2026-07-23 | DUAL-PATH, T-04 (3/4). Doc-verified same day: VS Code inline chat Ctrl+I editor+terminal with the "scoped to the code in the active editor" contract; commit-message generation reading 10+10 recent messages; Cursor Ctrl+K inline edit (selection-modify / no-selection-generate / terminal command bar with Cmd+Enter). The cross-tool KEY TRAP taught explicitly (Ctrl+I = inline chat in VS Code but agent panel in Cursor) with a deliberate trap-drill rep in labC. Terminal drills anchored to a verifiable seed fact (13 rows at exactly 0.85 — repo canon since 07-20). New ANIM-18 Scope Ladder & Key Map. 20/20 jsdom. No needsVerification |
| M-4.2 | ✅ built, claims doc-verified 2026-07-23 | 2026-07-23 | DUAL-PATH, T-04 (2/4). NES deep dive on the same-day doc verifications (location-model architecture from the long-distance NES release, gutter/Tab-Tab flow, nextEditSuggestions.enabled + showCollapsed; Cursor jump-in-file + cross-file quotes). Core teaching: the ripple workflow (seed change by hand, ride the consequences), the silence trap ("the compiler certifies done, not the quiet gutter"), and the three-way propagation rule (F2 rename → NES judgmental ripple → agent with plan), with revert-after-drill discipline in both labs. Visual REUSES ANIM-07 per its original spec (M-4.1, M-4.2); harness confirms both modules mount it. 16/16 jsdom. No needsVerification |
| M-4.1 | ✅ built, claims doc-verified 2026-07-23 | 2026-07-23 | DUAL-PATH (T-04 reduced-path: Copilot+Cursor-led, Claude exemption stated in-module), opens T-04 (1/4). No live run possible (IDE-only surfaces — M-1.3 precedent); doc verification same day with exact quotes: VS Code Tab / Ctrl+→ "accept either the next word of a suggestion, or the next line" / hover alternatives / NES gutter arrow + Tab-navigate-Tab-accept + showCollapsed; Cursor Ctrl/⌘→ next-word (editor.action.inlineSuggest.acceptNextWord) + Tab multi-line/cross-file/jump (verified 07-23 for M-2.5). House rule taught: never Tab through a boundary (0.85 tie-in). ANIM-07 built to its pre-existing VISUALS spec (typed ghost sequence → partial accept → NES tab hop; stylized abstraction, no IDE chrome). Harness proves DUAL-PATH pill + 2 lab tabs. 23/23 jsdom. No needsVerification |
| M-3.5 | ✅ built + labA verified LIVE during build 2026-07-23 | 2026-07-23 | TRI-PATH, **T-03 COMPLETE (5/5)**. The N+1 smell hunt ran for real (5.0 min, diagnosis-only leash held, git clean verified): loop at ClaimHistoryService.java:39-41 + regressing commit named; dead batch method found at ClaimRepo.java:14 with its ironic "keep it this way" comment; TWO index gaps as multipliers; scaling law 1+P round trips / ≈P×C rows (superlinear); real seed numbers 93 statements ≈40k rows for ≈359 (~46×), 10× projection; lazy associations RULED OUT (recordings/m35-smell-run.txt). Fix explicitly deferred to M-5.6. New ANIM-17 fan-out with slider on the real law. 23/23 jsdom. No needsVerification |
| M-3.4 | ✅ built + labA verified LIVE during build 2026-07-23 | 2026-07-23 | TRI-PATH, T-03 (4/5). Value trace ran for real: one concrete request (97110/MERIDIAN picked against COVERED ground truth, score exactly 0.85) → 101s, nine cited steps, committed verdict MANUAL_REVIEW at ApprovalService.java:99 incl. IEEE-754 no-ambiguity note; unscripted gems: the literally-false log catch ("score 0.85 < 0.85") and the forensic seed argument (every 0.85 row AUTO_APPROVED — "historical data that the current > could not have produced"); negative space verified (no Denial, no notification, history advisory pinned by comment+test); test-gap closure (0.92/0.61 tested, boundary never) — recordings/m34-trace-run.txt. Visual REUSES SIM-09 via new opts.autotrace (harness proves plain M-3.1 mount unaffected). 20/20 jsdom. No needsVerification |
| M-3.3 | ✅ built + labA control verified LIVE during build 2026-07-23 | 2026-07-23 | TRI-PATH (deep-dive B + comparative A/C), T-03 (3/5). Claude-side symbol control run (23s): process() = 1 production caller (matches M-3.2's flag) + 7 tests + a correctly-labeled Mockito stub; **resolveManualReview() = ZERO production callers** — real architecture-gap discovery (no HTTP door for human review; controller re-verified line-by-line), taught as "symbol questions find absences", recorded-not-fixed (recordings/m33-symbol-run.txt). Copilot claims doc-verified same day with exact quotes: #usages ("Combination of Find All References, Find Implementation, and Go to Definition"), Plan agent cheat-sheet entry, plan.md session-memory lifetime, 3 handoff exits, planAgent settings. BLUEPRINT's "find_symbol / call-hierarchy" confirmed STALE — rename catch #4, taught in-module. New ANIM-16. labB reruns M-3.2's mission verbatim (control experiment). 24/24 jsdom. No needsVerification |
| M-3.2 | ✅ built + labA verified LIVE during build 2026-07-23 | 2026-07-23 | TRI-PATH (deep-dive A + comparative B/C), T-03 (2/5). A real two-round STEER CYCLE recorded (recordings/m32-plan-steer.txt): round 1 (118s) planned the refactor the repo's own javadoc forbids and argued against its own mission ("The code explicitly says don't", strict-> preserved bit-for-bit + characterization-test gap, modest payoff); steer accepted flags + cut scope; round 2 (84s) five steps test-first with explicit out-of-scope incl. a bonus catch (misleading >= log line). git clean after both rounds — read-only lock proven. Interactive gate doc-verified same day with exact option labels (incl. Ultraplan option, Ctrl+G plan editing, Shift+Tab exit, cycle order). New TERM-05 replay. 22/22 jsdom checks. No needsVerification |
| M-3.1 | ✅ built + labA verified LIVE during build 2026-07-23 | 2026-07-23 | TRI-PATH, opens T-03 (1/5). The four-question battery ran headless on a pristine clone: 2.0 min, citations throughout; the load-bearing citation (ApprovalService.java:99) hand-verified; Q4 found FOUR doc-vs-code disagreements incl. the 0.85 canon plant, two later-module plants, honest README rot (stale Swagger claim), and named the stale-docs meta-pattern (recordings/m31-battery-run.txt — spoiler note included; module body deliberately doesn't teach plant mechanics). New SIM-09 architecture map from source ground truth (7 nodes/4 layers, POST trace, decision-boundary facts). Cursor indexing claim re-verified (Settings → Indexing, 80% threshold). Pays off M-2.5's ANY card via the labC head-to-head. 24/24 jsdom checks. No needsVerification |
| M-2.5 | ✅ built + labA verified LIVE during build 2026-07-22 | 2026-07-22 | TRI-PATH, **T-02 COMPLETE (5/5)**. Row-six CI task ran for real twice on a pristine clone: 49s plain (500 requests, MERIDIAN 271 + NORTHSTAR 229, zero orphans, unprompted null/0/id≥3 check) and 50s with --output-format json → **total_cost_usd $0.89, 9 turns** — the module's cost/speed beat stands on a real price tag (recordings/m25-ci-run.txt). Tool claims doc-verified same day: Cursor Tab multi-line/cross-file/jump (official wording, no Composer), Copilot NES current naming incl. long-distance NES, claude -p + --output-format json cost envelope. Rankings explicitly framed as editorial defaults + overturn-with-measurement path. New SIM-08 drill (2 contested cards, honest ANY card). 29/29 jsdom checks. No needsVerification |
| M-1.6 | ✅ built + labA verified LIVE during build 2026-07-22 | 2026-07-22 | TRI-PATH, T-01 complete again (6/6). NEW module (Vara's request, placed M-1.6). Both boundary beats ran for real: deny rule Read(phi/**) blocked the read in 18s — agent inferred the compliance reason, refused to work around it, recommended keeping the rule; seed.sql task answered 500/271/229 in 27s from synthetic data (recordings/m16-boundary-run.txt). Decision boundary verified against ApprovalService (score → approve or escalate only; DENIED only via eligibility policy or human) — repo-true, cited with line-level ground truth. Copilot content-exclusion + Cursor Privacy Mode/.cursorignore claims doc-verified 2026-07-22 incl. honest gaps (no Edit/Agent-mode coverage; best-effort wording). New ANIM-15. Blueprint + counts updated (eng 60, T-01 6). 25/25 jsdom checks. No needsVerification |
| M-2.4 | ✅ built + labA vibe-extreme verified LIVE during build 2026-07-21 | 2026-07-21 | TRI-PATH, T-02 (4/5). The rubric's low/low corner ran for real: throwaway seed-report script, 1.2 min headless, tally verified against the 500-request fixture (MERIDIAN 271 + NORTHSTAR 229), then deleted — artifact lifespan ~6 min (recordings/m24-vibe-script-run.txt; honest wrinkle kept: sandbox blocked script execution, agent said so + verified by regex). Spec extreme reuses M-2.3's same-clone recording — both extremes, same repo, same day. New SIM-07 Rubric Board drills 8 PA cards with the real T-02 numbers. 26/26 jsdom checks. No new tool claims. No needsVerification |
| M-2.3 | ✅ built + labA verified LIVE during build 2026-07-21 | 2026-07-21 | TRI-PATH, T-02 (3/5). Headless Plan Mode ran the module's exact spec mission on a fresh lab/start clone: 4.5 min, ZERO edits (git status clean), and the plan opened with the spec-vs-code "headline conflict" — strict `>` at ApprovalService.java:99 vs the spec's inclusive boundary — unprompted, plus consequence finds (COVERED-map coupling, no migration framework). Copilot Plan agent (VS Code session memory vs VS .copilot/plans split) and Cursor Plan Mode claims doc-verified same day. New TERM-04 replays the real capture. 25/25 jsdom checks. No needsVerification |
| M-2.2 | ✅ built + labA verified LIVE during build 2026-07-21 | 2026-07-21 | TRI-PATH, T-02 (2/5). The break mission ran for real on a fresh clone (M-2.1 base recreated first, 4.0 min): 4.8 min, agent reversed its own 20-min-old Dashboard architecture (−23 own lines, rework 24), invented GET /api/me (doesn't exist in priorauth-api) → 25/25 tests green but widget "unavailable" for EVERY user — the module's central beat, all from the real run. SIM-02 built with the real numbers; per-theme AA color vars. 22/22 jsdom checks. No new tool claims. Full-stack browser pass of the 404 path was test-level (Docker wouldn't start — see Needs verification) |
| M-1.1 | ✅ labs verified live 2026-07-20 | 2026-07-20 | TRI-PATH. Claude Code lab ran headless against fresh GitHub clones: tour names all four layers; threshold Q cites ApprovalService + 0.85 AND flags the planted doc-vs-code boundary contradiction (lab wording updated to teach that as a T-05 foreshadow); ConfidenceGauge cited (agent also spotted BUG-WEB-04 unprompted). Copilot/Cursor steps: tool claims doc-verified 07-19/21, repo ground truth confirmed |
| M-1.2 | ✅ labs verified 2026-07-20 | 2026-07-20 | TRI-PATH. CLI behavior live-confirmed on v2.1.215; repo question verified against the real clone (same boundary-contradiction nuance folded into labA step 3). TERM-01 replay ships from a real recorded capture |
| M-1.3 | ✅ labs verified 2026-07-20 | 2026-07-20 | TRI-PATH. Ask-mode ground truth verified live: denial-reason copy located precisely (EligibilityService reason strings + REV-001 fallback, persisted-at-denial-time nuance). ANIM-13 lanes match the current agents dropdown |
| M-2.1 | ✅ built + labA verified LIVE during build 2026-07-20 | 2026-07-20 | TRI-PATH, opens T-02 (1/5). The vibe mission ran for real: one sentence, headless, 4.1 min → agent chose EXTEND, rewrote the scaffold chart as HTML/CSS bars, kept a11y + reduced-motion, fixed the test it broke; build green + 21/21 after. TERM-03 replays the capture; the module teaches the "accepted by eye" contract WITH the evidence that careful agents still make unspecced choices. 31 checks pass. No flag |
| M-1.5 | ✅ built + labA verified LIVE during build 2026-07-20 | 2026-07-20 | TRI-PATH, T-01 complete (5/5). The real first flight ran headless during the build: agent found ALL THREE second-surface parts unprompted (both creation sites, injected Clock, seed.sql schema — plus an unplanned decided_at backfill), 39/39 tests green on its branch; entity-only variant proven to fail app startup (validate), complete change proven to boot clean. TERM-02 replays the real capture, honest wrinkle included. 31 automated checks pass. Ships with NO needsVerification |
| M-1.4 | ✅ labs verified end-to-end 2026-07-20 | 2026-07-20 | TRI-PATH prelude. Full day-zero flow ran live on a disposable clone: /init drafted a real constitution (commands, pipeline, schema split) and even detected the planted contradictions while deliberately omitting them; owner signed the rule; fresh-context question cited the signed rule as authority and pointed at the violating line. Lab wording updated to match reality |
## Decisions log
- 2026-07-19 — Player shell scaffolded as a lean player (condensed hero + functional course area); marketing-only preview sections (orbit canvas, lesson preview, metrics, AI-DLC arc) intentionally not carried into the player.
- 2026-07-19 — Progress persistence: single localStorage key `tenx-progress` → `{theme, path, done:{modId:true}}`, try/catch-guarded.
- 2026-07-19 — Hero stats are computed from TRACKS/MODS per active path (tracks · modules · built · completed), not hardcoded.
- 2026-07-19 — Cursor added as a full third path (labC on every hands-on module). Accent: purple #6E5AAE from the reference palette (white text for AA) — no invented colors. Reduced-path tracks: T-04 Copilot+Cursor-led, T-08 stays Claude-led with a three-way cost comparison in M-8.4.
- 2026-07-19 — New modules from the Cursor expansion: M-2.5 (tool-fit scenario drills), L-1.4 (the fleet decision — tool per use case, for leaders), M-12.5 (bonus capstone: agent-driven migration of both repos from clinical PA to pharmacy PA — target fixed in REPO-SPECS.md). Counts now 58 eng / 22 lead.
- 2026-07-19 — Hosting: the course will be served from the learning.varasrinivas.com S3 bucket. Deploy = upload 10x-toolkit.html (single file, no build). Set the bucket's website index document to 10x-toolkit.html (or upload a copy as index.html); serve through CloudFront + ACM for HTTPS on the custom domain (progress localStorage is per-origin, so keep one canonical URL); give the file short/no cache (or invalidate on deploy) since it changes every module session.
- 2026-07-19 — deploy.ps1 added (local AWS CLI only, no GitHub Actions by design — no account details in the repo). Uploads the player as both 10x-toolkit.html and index.html with no-cache; optional CloudFront invalidation via DistributionId in gitignored deploy.config.json. Dry-run verified.
- 2026-07-19 — Course repo published to github.com/varasrinivas/the-full-ai-tools-course. Lab repo references now link to github.com/varasrinivas/priorauth-api and /priorauth-web — these 404 until the teaching repos are pushed (tracked under Needs verification).
- 2026-07-19 — Tool-fit guidance table added to BLUEPRINT.md (editorial defaults, e.g. Cursor for rapid UI work, Claude Code for long-horizon backend/agentic work, Copilot for GitHub-native team flow); every module that recommends a tool must cite it and re-verify via /refresh-module.
- 2026-07-19 — TERM-01 recording policy set by example: real captures only, per CLAUDE.md. `claude --version` run verbatim; the Q&A is a real headless `claude -p` run against a minimal priorauth-api stand-in authored from REPO-SPECS.md (raw capture + method notes in recordings/TERM-01-m1.2-first-session.txt). Interactive-only mechanics (Shift+Tab, /clear, rewind menu) render as visually-distinct annotation cards, never as fake terminal text. Re-record against the real repo when it ships.
- 2026-07-19 — Module validation now includes an automated jsdom runtime pass (boot player, open module, exercise visual + labs + quiz, reduced-motion and theme passes, zero-window-error assertion). Harness lives in the session scratchpad, not the repo — rebuild per session if wanted.

- 2026-07-20 — Copilot vocabulary shifted under us (verified against code.visualstudio.com): "chat modes" are now **agents** picked from the **agents dropdown** (built-ins: Agent, Plan, Ask); **Edit mode is deprecated** (hidden by default since v1.110, removed from v1.126) — never teach it as current; custom chat modes renamed **custom agents** (`.agent.md`, was `.chatmode.md`); approval UX is scope-based (single use/session/workspace/always) with permission modes **Default Approvals / Bypass Approvals / Autopilot**; edit review labels are literally **Keep / Undo**. M-1.3 is built on this vocabulary; earlier-written copy must be swept (see Needs verification).
- 2026-07-20 — Cursor's official docs now list **Agent / Plan / Ask** modes (cursor.com/docs/agent/modes) — supersedes the 2026-07-19 note that Ask wasn't in official docs. Also verified: Cursor model picker + Auto option; Max Mode is legacy, don't teach it.
- 2026-07-20 — ANIM-13 approach: Copilot visuals are abstract behavior diagrams (lanes, gates, checkpoint markers) — VISUALS.md's no-fake-IDE-chrome rule applied; screenshot strips deferred until real IDE captures exist.

- 2026-07-20 — Prelude module added at Vara's request: M-1.4 "Day zero: make it an AI repo" (bare non-AI repo → claude /init CLAUDE.md, Copilot instructions file, Cursor rules). Placement after tool setup (M-1.2/M-1.3) so labs run immediately; old First flight renumbered M-1.5. Counts now 59 eng / 22 lead. Labs run on a DISPOSABLE copy — the teaching repos stay bare until T-07 by design (REPO-SPECS), so the prelude teaches mechanics and T-07 keeps the before/after payoff. Plan approved 2026-07-20 (analogy: locum clinician + protocol binder; visual: new ANIM-14 "One Repo, Three Constitutions"; labs on disposable clone with own-repo substitute allowed); build next session via /build-module M-1.4.

- 2026-07-20 — HOSTING CORRECTION (supersedes the 2026-07-19 hosting decision): there is no `learning.varasrinivas.com` bucket — that was a typo for the existing shared multi-course bucket **`learnings.varasrinivas.com`** (private, OAC, fronted by CloudFront ESC8HMAS41DRF with a `learnings-dir-index` viewer-request function rewriting folder URLs to index.html). The course deploys to the prefix **`the-full-ai-course/`** (Vara's pick) → canonical URL **https://learnings.varasrinivas.com/the-full-ai-course/**. deploy.ps1 updated (Prefix support, prefixed invalidation); DistributionId lives only in gitignored deploy.config.json. First real deploy verified live 2026-07-20. The bucket-root catalog (learnings-hub repo, deploy branch `protected`) links the course since 2026-07-20 — its card tags ("59 + 22 modules", "early access · in production") should be refreshed as the build progresses.

- 2026-07-20 — **priorauth-api BUILT** (local repo at ../priorauth-api; push pending GitHub auth). Found Vara's hand-built Spring Boot 4.1 skeleton (unversioned, never compiled) already in the directory — kept Boot 4.1 over the spec's "3.x" (Vara's call; REPO-SPECS.md updated to as-built). Completed it: fixed a record accessor/factory name collision, wrote the 41-test suite (unit + MockMvc + Testcontainers Postgres ITs), 500-request seed.sql (13 rows at exactly 0.85), compose on postgres:16-bookworm (newer images drop legacy tz aliases like Asia/Calcutta and break JDBC handshakes — bookworm keeps students' machines working), surefire pins UTC, @EntityGraph payer fetches (open-in-view=false), review-queue context endpoint (the N+1's observable surface). Verified: 41/41 green incl. real-Postgres ITs; app booted against the seeded compose DB (ddl-auto=validate passed) and all endpoints answered over HTTP (179 queued, 92 patients of context). Then the five plants as innocent-looking commits, each tagged plant/BUG-API-xx: planted suite 37/37 green; a scratch boundary test proved BUG-API-01 fails exactly as intended (0.85 → MANUAL_REVIEW). lab/start branched at planted HEAD; main frozen at same commit. Environment fixes this session (machine-local, not repo): created missing /tmp in the rancher-desktop WSL distro so containers could start.

- 2026-07-20 — **priorauth-web BUILT** (local repo at ../priorauth-web; push pending GitHub repo creation). Clean scaffold this time (directory was empty): create-vite react-ts → React 19 / Vite 8 / TS 6 / Router 7 (React 19 supersedes the spec's "React 18" per the Boot-4.1 newest-stable precedent; REPO-SPECS updated as-built). All four pages, four components, three hooks, typed API client with the inclusive-threshold helper. 26 Vitest+RTL tests green first run; production build green (one TS6 `erasableSyntaxOnly` fix); dev-server /api proxy verified end-to-end against the live seeded API (179 queued requests through :5173). Four plants as innocent-looking tagged commits (plant/BUG-WEB-01..04): planted suite 20/20 green + build green. BUG-WEB-02's mechanism: hoisted stale fetch promise ("reuse one request per mount") — every poll re-awaits the first response. lab/start branched; main frozen.

- 2026-07-20 — **Lab verification pass: all four built modules flipped ⚠ → ✅.** Method: fresh GitHub clones of both teaching repos; every Claude Code lab path executed live and headless; Copilot/Cursor paths verified via current-docs claims (already done this week) + repo ground truth. Fresh-clone student experience confirmed: `./mvnw test` 37/37 (wrapper bootstrap + Testcontainers) and `npm ci && npm test` 20/20. The planted repos changed two success-checks for the better: agents reliably FLAG the boundary doc-vs-code contradiction (now taught as a T-05 foreshadow in M-1.1/M-1.2), and M-1.4's /init run detected all three planted contradictions while deliberately omitting them from CLAUDE.md — the signed-rule step then worked exactly as designed (rule cited as authority, violating line identified). Also: agent tour caught a README error in priorauth-api (OpenApiConfig → ClockConfig) — fixed and pushed to both branches (docs-only; main and lab/start realigned at b1b13da). lastVerified dates normalized to 2026-07-20 (two entries had drifted to 07-21).

- 2026-07-20 — M-1.5 built with a LIVE first flight as its own verification: the mission prompt ran headless against a fresh clone (branch flight/claude off lab/start). The agent found every part of the "second surface" unprompted — both denial-creation sites, the injected Clock, and the seed.sql/validate schema (adding a decided_at backfill nobody asked for) — and honestly reported it couldn't execute the test command under headless permissions rather than claiming success (wrinkle kept in the recording as a teaching beat). Suite then ran green on its branch (39/39). Both quiz-critical claims proven live: complete change boots clean; entity-only change fails startup validation. TERM-02 ships the real capture; raw transcript + method notes in recordings/TERM-02-m1.5-first-flight.txt.

- 2026-07-21 — **M-2.2 built with a LIVE break mission as its own verification.** Method: fresh GitHub clone; M-2.1 mission rerun first to recreate the vibe base (`999723f`, EXTEND, 4.0 min, 20/20 after), then the M-2.2 mission verbatim on top (`fe16442`, 4.8 min, +220/−24). The run delivered the module's teaching beats unscripted: rework signature = 23 minus-lines on Dashboard.tsx the agent itself wrote in run 1; invented auth = CurrentUser via GET /api/me, fail-closed, tested — and the endpoint doesn't exist in priorauth-api, so the live widget shows "unavailable" to every user while 25/25 tests pass (agent disclosed it only in its report caveat — vibe mode doesn't read reports). Property surfaces verified: API down → Vite proxy 502 (live curl) and the page dies upstream at Dashboard's own listRequests before the panel mounts; API up → /api/me 404 → unavailable-for-everyone pinned by scratch RTL test + endpoint grep (full-stack browser pass blocked: Rancher Desktop/WSL would not start this session — machine state, not repo). Raw captures + method notes: recordings/m2.2-break-mission.txt (candidate TERM asset, unused by any module yet). SIM-02: vibe lane runs the real numbers; spec lane schematic (labeled) until M-2.3 records one; toggle-off honestly lets vibe win. Colors are design-reference semantics (#B0503C/#3E9A6B, same as SIM-01/ANIM-13) with per-theme AA text variants (--s2red/--s2green) — the lighter/darker stops were already in the reference gradients. jsdom validation 22/22 (schema, tri-path, mount, exercise, both themes, reduced-motion, zero window errors).

- 2026-07-21 — **M-2.3 built with a LIVE plan run as its own verification.** Method: fresh lab/start clone (b1b13da); SPEC-threshold.md (per-payer auto-approve override, inclusive >= boundary, 0.50–0.99 range) written in; the module's exact mission run headless via `claude -p ... --permission-mode plan` (v2.1.216). Result: 268s, exit 0, `git status --porcelain` shows only the spec file — read-only claim proven. Unscripted teaching beats delivered: plan led with the "headline conflict" (strict `>` at ApprovalService.java:99 vs javadoc/log/seed, "no existing test pins the boundary — which is why the bug survives"), found the EligibilityService.COVERED coupling and the no-migration-framework reality, and produced an 11-test list whose first test "fails against current code; pins the fix". Honest wrinkles kept in the recording: headless plan mode has no interactive gate (agent noted ExitPlanMode unavailable, printed the plan, and wrote it to ~/.claude/plans/); the plan treats the `>`→`>=` fix as in-scope because spec AC3 forces it — the LAB's gate move (stop, strike, defer to T-05) is what protects the BUG-API-01 plant, and all three labs are plan-only with git-clean success checks. Tool claims doc-verified today: VS Code Plan agent (agents dropdown/`/plan`, session-memory /memories/session/plan.md cleared at conversation end, "start the implementation") vs Visual Studio Plan agent (durable .copilot/plans/plan-{title}.md, Tools > Options save path, "Implement plan") — BLUEPRINT's ".copilot/plans/" claim is VS-only, module teaches the split; Cursor Plan Mode (Shift+Tab, Markdown plan + editable to-dos, home-dir default save + "Save to workspace", Build button) — blueprint's "plan-first Composer" wording confirmed stale, not used. New visual TERM-04 (id approved in plan) replays the real capture; recordings/m23-spec-plan-run.txt holds method notes + untrimmed transcript + verbatim spec. jsdom validation 25/25 (schema, tri-path, TERM-04 mount/skip/replay/speed, module open, prev/next nav, both themes, reduced-motion static transcript, zero window errors).

- 2026-07-21 — **M-2.4 built with a LIVE vibe-extreme as its own verification.** Method: same lab/start clone as M-2.3's recording (deliberate — both rubric extremes now share one repo and one day: 1.2-min vibe script vs 4.5-min plan-gated spec). The throwaway mission ran headless under acceptEdits (v2.1.216, 73s): agent wrote count_requests.ps1, was blocked by the sandbox from executing it, said so plainly, and verified its numbers by regex — we then ran the script: MERIDIAN 271 + NORTHSTAR 229 = 500, matching the seed's own fixture comment (the "accept-by-eye has an anchor" beat), and deleted it (~6-min artifact lifespan; the module's disposition beat). Lab task choices verified against ground truth to protect plants/future modules: labC's low-stakes tweak targets App.tsx's topbar header (confirmed present; chart/gauge/table components explicitly off-limits in the lab wording since BUG-WEB-01..04 live there); the report script reads seed.sql only. New SIM-07 Rubric Board (id approved in plan): 2×2 click-to-place drill, quadrants vibe/plan-first/verify-twice/spec, 8 cards carrying real T-02 numbers, keyboard-operable, reduced-motion = pre-placed revealed board with reset still functional. No new tool-capability claims (labs reuse flows verified 07-20/21). jsdom 26/26 incl. full drill loop (deliberate misplacement → 7/8 score + correction chip, reset, reduced-motion drill re-entry).

- 2026-07-22 — **M-1.6 built with BOTH boundaries live-verified.** Method: lab/start clone + staged hazard (synthetic phi/patient-export.csv) + .claude/settings.json deny rule Read(phi/**). Run 1 (18s): read blocked; unscripted beats — agent inferred the rule was compliance-driven ("given it holds protected health information"), declined to work around it, recommended keeping it. Run 2 (27s): seed.sql answered 500 = MERIDIAN 271 + NORTHSTAR 229, matching M-2.4's independent tally — work continues inside the rail. Decision boundary taught from repo ground truth re-read this session: ApprovalService score branch → AUTO_APPROVED or MANUAL_REVIEW only; DENIED only from EligibilityService policy codes or resolveManualReview (REV-001). Module line: "the model may say yes alone; it may never say no alone." Doc-verified same day (official sources, quotes in recording + module): Claude deny rules bind file tools + recognized shell reads, not subprocesses; Copilot content exclusion = Business/Enterprise, repo Settings → Copilot, "currently not supported in Edit and Agent modes of Copilot Chat", no symlinks/remote fs; Cursor .cursorignore blocks Agent/Tab/Inline/@ but "complete protection isn't guaranteed due to LLM unpredictability", Privacy Mode = no training + ZDR, inference still transits backend, Free/Pro default OFF. Labs deliberately teach the gaps (incl. the clipboard no tool polices). Blueprint edited per approved plan (T-01 → 6, eng → 60); TRACKS/PATH_DESC data constants updated; new ANIM-15 factory. jsdom 25/25 incl. curriculum-order check (M-1.5 → M-1.6 → M-2.1 nav) and count checks.

- 2026-07-22 — **M-2.5 built with a LIVE row-six run as its own verification; T-02 complete.** Method: pristine lab/start clone (M-1.6 staging removed, git clean); the CI integrity mission run twice — plain (49s; verdict: all 500 requests resolve, 271+229, explicit null/0/id≥3 sweep unprompted) and with --output-format json (50s, total_cost_usd 0.893096, num_turns 9) — giving the course its first real per-invocation price tag, and a third independent derivation of 271/229 (script, doc-read, scan — all agree). Doc verifications same day: Cursor Tab "predicts cross-file edits when changes in one file need updates in another" + multi-line + jump-in-file (cursor.com/docs/tab/overview, Composer absent); Copilot "next edit suggestions" current naming, long-distance NES (Feb 2026), setting github.copilot.nextEditSuggestions.enabled; claude -p current headless mode with --output-format json cost envelope (code.claude.com/docs/en/headless). **Resolves the outstanding tool-fit item** (see Needs verification): capability claims now doc-verified; the RANKING claims ("Cursor best for rapid UI", "Tab vs NES") are deliberately NOT asserted as fact — module copy labels them editorial defaults, dated and versioned, with the overturn-by-measurement path (M-11.1) as the honest resolution. SIM-08 drill: 12 cards, contested cards accept either defended pick, onboarding scores ANY, CI card carries the real numbers. jsdom 29/29 incl. a regression guard (a mid-build misedit briefly renamed M-2.4's id — caught and reverted same minute; guard asserts no PLACEHOLDER ids remain) and contested-pick scoring (deliberate wrong pick → 11/12 + correction chip).

- 2026-07-23 — **WORKFLOW CHANGE (Vara, mid-M-3.1 build): continuous mode.** "Build other modules without waiting for my approval" — the per-module plan-approval pause is waived; everything else in the module rhythm stays (plan logged in-session, verify-before-build, live recorded runs, jsdom validation, PROGRESS entry, commit+push+deploy per module). Still stopping for genuine user decisions (curriculum placement, scope changes). Supersedes the strict read of "ONE module per session" for this continuous phase; the quality bar per module is unchanged.

- 2026-07-23 — **M-3.1 built with a LIVE battery run as its own verification; T-03 opened.** Method: pristine lab/start clone; the module's four-question battery (entry-point trace, layers, load-bearing rule, contradiction hunt) + citation demand ran as one headless mission (v2.1.216): 120s, exit 0. Results: trace included pre-business-logic reality (security chain, validation-before-logic, unknown-payer→eligibility-denial); five layers named (model+config beyond the assumed four); 0.85 fork named load-bearing WITH its operator problem; four cited doc-vs-code disagreements + the introducing commits + the stale-docs meta-pattern. Load-bearing citation hand-verified against source. Plant policy per the 07-20 precedent: findings recorded as T-05/T-06 foreshadow; module body avoids plant mechanics (harness asserts this). Ground truth for SIM-09 read from source this session (controller endpoints incl. review-queue/context, 4 services incl. NotificationService gateway pattern, 4 repos, ClockConfig/SecurityConfig). One environment wrinkle: the first battery launch failed with `claude: command not found` in a background bash (PATH profile variance) — reran with the absolute npm path; zero impact on the recording. jsdom 24/24.

- 2026-07-23 — **M-3.2 built with a LIVE two-round steer cycle as its own verification.** Method: pristine lab/start clone; round 1 planned the "forbidden" ApprovalService.process() refactor in plan mode (the javadoc's resist-tidying note is the designed trap) — the plan quoted the javadoc, demanded bit-for-bit preservation of the strict > behind a new characterization test, and called the payoff modest; round 2's prompt was the steer verbatim (accept flags, scope-cut to the eligibility-denial extraction + the 0.85 pin, ≤5 steps) → five steps, test-first, explicit out-of-scope, bonus catch of the misleading >= log line at 101. `git status --porcelain` empty after both rounds. Headless wrinkle consistent with M-2.3 (no interactive gate; plan file in ~/.claude/plans/). Interactive-gate claims doc-verified against code.claude.com/docs/en/permission-modes with exact labels: "Yes, and use auto mode" / "Yes, auto-accept edits" / "Yes, manually approve edits" / "No, refine with Ultraplan on Claude Code on the web" / "No, keep planning"; Ctrl+G plan editing; Shift+Tab exits without approving; "⏸ plan mode on"; /plan prefix; approval switches session mode + names the session. Tri-path kept via comparative labB/labC (Copilot Plan agent re-cites M-2.3 verified claims; Cursor document-first steer). New TERM-05 (id approved in plan). jsdom 22/22.

- 2026-07-23 — **M-3.3 built; Copilot Plan-agent deep dive with a live Claude control run.** Verification method mirrors M-1.3 (Copilot-led modules can't run headless: doc verification with exact quotes) PLUS a live Claude-side control (the symbol question) that doubles as labB's #usages cross-check ground truth. Doc verifications (code.visualstudio.com, quoted verbatim in module + ANIM-16): #usages combo definition; Plan agent "Research requirements, ask clarifying questions, and generate a structured plan with steps, verification, and decisions"; clarifying-questions-after-research behavior; /memories/session/plan.md + "cleared when the conversation ends" + Chat: Show Memory Files; handoff trio (start the implementation / background Copilot CLI session / open planning prompt in editor); settings chat.planAgent.defaultModel, github.copilot.chat.implementAgent.model, planAgent.additionalTools (experimental). BLUEPRINT naming correction recorded: "find_symbol / call-hierarchy navigation" → #usages (stale-name catch #4; blueprint text left as-is per convention, module + PROGRESS carry the correction). The zero-production-callers finding for resolveManualReview() is deliberately taught as an onboarding-recorded gap — building the resolve endpoint is future-lab material (candidate for T-10's full-workflow module or a T-06 lab); do NOT "fix" it in earlier modules. jsdom 24/24.

- 2026-07-23 — **M-3.4 built with a LIVE value trace; SIM-09 reused instead of a new visual.** The shape-vs-value framing is the module's spine: M-3.1's battery answered shape questions; M-3.4 pins concrete values (score exactly 0.85) so the trace must COMMIT to one outcome, forcing the docs-vs-code resolution at runtime terms ("comments, javadoc, log strings, and seed fixtures have no effect on control flow" — the agent's own words, now the module thesis). Mission inputs chosen against ground truth (EligibilityService.COVERED read first: MERIDIAN covers 97110/29881) so the request survives Stage 1 and reaches the threshold — a deliberate design step recorded here for future value-trace labs. Visual policy note: no new component; SIM-09 gained a 2-line opts.autotrace hook (M-3.4 mounts mid-trace; M-3.1's plain mount proven unaffected by harness check). The trace's seed-forensics beat (0.85 rows as evidence of the pre-bug rule) is now course canon usable by M-5.2. jsdom 20/20.

- 2026-07-23 — **M-3.5 built with a LIVE smell hunt; T-03 COMPLETE.** Mission design: symptom + leash ("feels slow as the queue grows" / "without changing any code") + a numbers invitation — the leash held ("You asked for diagnosis only, so I changed nothing", git clean verified). The hunt exceeded the plant's design: beyond BUG-API-03's loop it independently characterized the two missing indexes as multipliers (claim.patient_ref → full scan per query; request.status minor), produced the superlinear law (doubling business quadruples DB work), computed seed-scale forensics (179 queued / 92 patients / 435 claims → 93 statements, ≈40k rows scanned, ~46× waste, ~900 queries at 10×), found the dead batch method with its "keep it this way" comment as evidence of the regression path, and ruled out entity-graph N+1s (lazy + open-in-view=false). All figures consistent with the 07-20 build notes (179 queued / 92 patients live-verified then). ANIM-17 encodes the real law with a P slider. T-03's five read-only modules all shipped with live recorded runs and zero lines changed in the teaching repo — the track's closing claim is itself verified. jsdom 23/23.

- 2026-07-23 — **M-4.1 built; T-04 opened as the course's first reduced-path track.** Verification method: doc-only with exact quotes (no headless surface exists for ghost text — recorded-run precedent explicitly N/A, M-1.3 pattern applies). The Claude Code exemption is taught in-module ("no autocomplete layer, by design; its unit of work is the turn") citing the tool-fit table, and the DUAL-PATH pill + two-tab lab rendering is harness-asserted. Pedagogical thread: the layer's contract is contrasted with M-2.5's recorded $0.89/9-turn envelope, and the "never Tab through a boundary" house rule carries the 0.85 lesson to keystroke scale (labB's drill has students log what the ghost offers for the comparison — if it offers strict >, they watch T-02's bug try to be born). ANIM-07 was pre-specced in VISUALS.md since v1.0 — first of the original visual specs to ship. jsdom 23/23.

- 2026-07-23 — **M-5.1 built with a LIVE full debug loop; T-05 opened; first module whose verification required running the teaching repo's test suite.** Method note for future T-05/T-06 modules: the drill bug was hand-planted on a scratch branch (drill/m51-loop) rather than using a tagged plant, so BUG-API-01..05 stay untouched for their own modules; branch deleted and clone restored to pristine lab/start afterward (verified). Unit-test-only invocation (`-Dtest=...ServiceTest`) avoids the Testcontainers/Docker dependency that blocked the 07-21 session — 22 tests, ~3s, no Docker needed; use this pattern for T-06 too. The recorded run's honest failure (agent blocked from executing Maven, offered stale surefire report + hand-tracing as surrogate evidence, asked for approval) became the module's fifth beat rather than a defect: "surrogate evidence is not verification" + "an agent that says 'I could not run it' is doing its job". Bonus find recorded but NOT taught here: the agent independently flagged BUG-API-02 (LocalDate.now() test that fails on the last three days of any month) while fixing something else — M-5.4 keeps that reveal, and the harness asserts M-5.1's copy doesn't spoil it. ANIM-08 built to its original VISUALS spec (orbit + context-quality dial tightening laps; high end annotated with the real 1-lap/89s run). jsdom 24/24.

- 2026-07-23 — **M-5.2 built; the course's foreshadowed bug is now actually fixed on camera.** Four-phase live method (plan → RED → GREEN → mutations), every number in the module from a real run this session; full transcript trail in recordings/m52-threshold-fix.txt. Two findings worth carrying forward: (1) **the mutation-B discovery** — with `>=` in place, changing AUTO_APPROVE_THRESHOLD from 0.85 to 0.84 leaves all 12 tests green, because both boundary tests reference the constant (the same choice that correctly defeats the "keep > and lower the constant" dodge). The suite pins the operator; nothing pins the value. M-5.2 teaches this as the honest limit of a boundary test and hands the general discipline to M-6.4 — that module should now open with this exact artifact. (2) The plan's blast-radius method (count rows where the two operators disagree, split by status, exclude rows denied upstream) is reusable for any boundary bug; consider it the T-05 house pattern. SIM-04 shipped after my own harness caught a real float/collision bug in my first generator (scores now integer cents) — the harness earning its keep on a visual, not just a module. Repo hygiene: fix lived only on drill/m52-threshold, branch deleted, clone verified pristine at b1b13da, plant/BUG-API-01 untouched for students.

- 2026-07-23 — **M-5.3 built; the "runtime truth" module, with the strongest cross-module convergence in the course so far.** Two findings to carry: (1) The recorded probe closed a loop opened three tracks earlier — M-3.4's static trace PREDICTED the routing log would print the impossible "score 0.85 < 0.85"; the live run emitted exactly that string unprompted, alongside a frame showing Double.compare == 0 (which is the single observation that ends the floating-point hypothesis every engineer reaches for first at a numeric boundary). Prediction → observation is now the module's spine and a reusable teaching shape. (2) Visual Studio's documented Debugger Agent workflow ends in "Final human validation — you rerun the scenario and confirm the fix in the live environment alongside the agent." That is precisely what M-5.1 discovered the hard way when the agent could not close its own verify station. Independent arrival at the same rule; both modules now cite each other. Tri-path preserved via BLUEPRINT's nearest-equivalent clause rather than dropping a lab: Claude = instrumented probe run (no debugger, stated plainly), VS = the agent-driven session, Cursor/VS Code = human-driven session with AI alongside; the gap between the last two IS labC's lesson. Probe-as-scaffolding discipline taught explicitly (delete the instrumentation, keep the finding).

- 2026-07-23 — **M-5.4 built; closes the loop on the time bomb M-5.1's agent spotted in passing.** Pedagogy: "flaky = deterministic on an unidentified condition" is made literal by a real probe that enumerates the 36 failing days, and the module's spine is the test-flaky-vs-code-flaky distinction (they demand opposite fixes; conflating them is how innocent code gets a Clock it doesn't need). The agent's refusal to inject a seam ("adding a seam for nothing") is quoted as the senior move, and M-1.5 is the deliberate foil — same Clock tool, opposite verdict, distinguished by one question (does the code under test read the clock?). SIM-11 note: first data-viz component that needed calendar math but had to avoid Date.now()/new Date() (harness-banned and semantically wrong for a determinism demo anyway) — all dates are static literals, today/next-failure hardcoded from the recording; if this module is ever refreshed past 2026, bump TODAY/NEXT and the year label. The probe-as-scaffolding discipline (M-5.3) held again: probe deleted, clone pristine, plant/BUG-API-02 intact.

- 2026-07-23 — **M-5.5 built; first web-toolchain module of T-05, and the "green suite ≠ correct system" thesis.** Method notes for future web modules (T-06 RTL work especially): reset the priorauth-web clone to origin/lab/start first (it was left on the M-2.2 vibe/chart-claude wreckage branch from that build — `git switch lab/start; git reset --hard origin/lab/start`). Unit-test invocation is `npx vitest run [path]` — no Docker, fast. The exposing-test-first structure is the pedagogy: BUG-WEB-02 is invisible to the shipped 20 because both hook tests pin a constant mock and never advance a timer across a tick expecting change (the unmount test is vacuously satisfied by a fetch-once bug). This is the same "hidden variable" framing as M-5.4, now spatial-across-polls instead of across-calendar-days — the two modules are explicitly linked. Third live occurrence of an agent honestly reporting it cannot run the verify step (permission layer blocks the native test runner); the pattern is now robust enough that M-5.6 and the T-06 testing track should lean into "you run the final verification" as a taught default, not an exception. ANIM-20 numbers (1 call frozen vs 1→4 climbing, SUBMITTED→AUTO_APPROVED) are the recorded run's. Probe deleted, clone pristine, plant/BUG-WEB-02 intact.

- 2026-07-23 — **M-5.6 built; T-05 COMPLETE (6/6).** The performance-pair module closes both the N+1 (M-3.5's deferred fix) and the re-render, each with a real before/after cost measurement rather than eyeballing. Method reusable for T-06 and any perf work: a Mockito-invocation-counting probe gives deterministic query counts headless (no Docker), and an RTL render-counting probe (mock a leaf component to increment a counter, rerender with identical props) gives deterministic render counts — both are honest CLI stand-ins for the profiler/query-log the labs use. Visual reuse: ANIM-17 serves both M-3.5 (diagnosis) and M-5.6 (fix) — the fan-out's constant "promise lane" (2) is literally the after-state; no new component needed. The measure-fix-measure loop is positioned as the mutation-check (M-5.2) analogue for performance, and the "only a test that asserts a COST catches a cost bug" line ties M-3.5 + M-5.5 + M-5.6 into one thesis. **T-05 retrospective**: all six modules shipped with live drills on real planted bugs (BUG-API-01/02/03, BUG-WEB-01/02 + two hand-planted teaching bugs for M-5.1); every fix on a scratch branch, every clone restored pristine, every plant tag intact; the recurring "agent honestly cannot run the verify step" finding (M-5.1/5.2/5.5) is now a documented course theme feeding into T-06. Next: T-06 Testing (M-6.1). The mutation-B artifact from M-5.2 (suite pins operator, not value) should open M-6.4.

- 2026-07-24 — **M-6.1 built; T-06 opened with the course's sharpest live artifact yet.** The naive "make the suite pass" run producing a SILENT source edit that erased a planted bug was unplanned and is the module's spine — it's a stronger lesson than the intended "boundary cases matter": the danger in AI test-gen is the prompt contract, specifically that "make it pass" inverts judge/defendant and lets the agent edit the code under test. Method note for the rest of T-06: this two-run contrast (naive vs disciplined, same target) is a reusable structure; the disciplined prompt's three clauses (assert the spec / forbid source edits / a failing test is the finding) are the takeaway. IMPORTANT for M-6.4: this same gauge + the M-5.2 mutation-B artifact are the two halves of "a test only counts if it can fail" — M-6.4 should use BOTH (mutation on the fixed threshold code, and this gauge's mirror-test that never fails). BUG-WEB-04 is now explicitly a T-06 teaching asset (the gauge boundary) as well as a T-05-era plant — it stays UNFIXED through these modules; do not let any lab "clean it up." Title carries a real apostrophe (’) not &rsquo; because modTitle renders via textContent (same rule as M-3.3's ampersand). Both clones pristine, plant/BUG-WEB-04 intact.

- 2026-07-24 — **M-6.2 built; the Docker-down constraint became the lesson instead of blocking it.** Rather than a weaker module, the skip-clean behavior (`disabledWithoutDocker=true` → 31 pass + 6 skip + BUILD SUCCESS, captured live) is itself a teachable discipline (integration tests skip locally, run in CI, never fail the build over a missing daemon) — a genuinely better module than a bare "here's how to write a container test." Method for the rest of T-06 and any DB work: run `./mvnw test` and read the skip count; unit-only invocation stays `-Dtest=*ServiceTest`. The module ties the mock-vs-container distinction back to M-5.6 (mock counted the N+1 fix's calls; the committed batch-IN integration test proves correctness on real Postgres) — good spiral reinforcement. Standing item unchanged: a Docker-up 37/37 re-confirm is a 2-min check whenever Rancher Desktop cooperates (shared with the M-2.2 note). No new tool-capability claims. Next: M-6.3 (React Testing Library — BUG-WEB-03's untested form; runs on the web toolchain, no Docker needed).

- 2026-07-24 — **M-6.3 built; the "regenerate deleted tests" premise paid off with a real caught bug, unplanned.** The whitespace-score gap (Number('   ')===0) is a genuine latent defect in usePriorAuthForm's validate() that the spec-anchored regeneration surfaced live — a stronger artifact than a coverage-restoration demo, and it makes the module's thesis ("deleting tests hides what they caught") concrete rather than asserted. SCOPE DECISION: left the whitespace gap UNFIXED and the source pristine so the artifact reproduces; the gap sits inside BUG-WEB-03's own territory (form validation) so it's on-theme, not stepping on another plant. If a future module or lab "fixes" usePriorAuthForm, note that SIM-13 and this recording assume the gap is present — update both if the source changes. Method: SIM-13 ports validate()'s guard into the visual and computes accept/reject per row rather than hardcoding, so the harness can independently confirm the flagged row — reusable pattern for any "here's what the code does" visual (the code, not my transcription, is the source of truth). This is the 4th consecutive live module where the agent honestly reported it couldn't run the test runner and traced by hand; the pattern is robust enough that M-6.4/T-06 close-out should state it as the norm. Next: M-6.4 (mutation testing — use BOTH the M-5.2 mutation-B artifact AND M-6.1's mirror-test to prove "a test only counts if it can fail"). Web clone pristine, plant/BUG-WEB-03 intact.

- 2026-07-24 — **M-6.4 built; HALFWAY on the engineer path (30/60), and the course's testing thesis is now measured rather than asserted.** The campaign result (2/5 = 40%, all three survivors clustered on the auto-approve decision) is the strongest single artifact in T-06 and retroactively sharpens three earlier modules: M-6.1's mirror test (unkillable by construction), M-5.2's mutation-B (constant unpinned even after adding boundary tests), and M-3.5/M-5.6's cost-instrument thesis (coverage answers the wrong question). Reusable method: a 15-line bash campaign (perl -pi mutate → `./mvnw test -Dtest='*ServiceTest,*ControllerTest'` → grep the summary → `git checkout --` revert) gives a real mutation score in ~2 minutes with no Docker and no PIT dependency — good enough to teach with, and honest about being a hand-rolled campaign rather than a full mutation-testing tool. VISUALS deviation recorded: ANIM-09's v1.0 spec said "flashes red = caught"; shipped green=CAUGHT/red=SURVIVED to match course colour semantics (danger is red everywhere else) — noted in the VISUALS.md row so the spec and the build don't silently disagree. Next: M-6.5 (test-first with agents — red/green/refactor where the agent writes the test before the code) closes T-06; the natural live drill is a genuine TDD loop on a small new PA behavior, with the M-6.4 mutation check as the acceptance gate.

- 2026-07-24 — **M-6.5 built; T-06 COMPLETE (5/5).** The TDD loop produced the track's payoff measurement: 5/5 mutation score on test-first code vs M-6.4's 2/5 on the retrofitted suite, with the CAUSE verified by reading the generated test rather than inferred — test-first assertions use literal domain values (`Duration.ofDays(2)`) while retrofits reach for the implementation's constants and therefore follow them. That single mechanism explains M-5.2's surviving constant mutation, M-6.1's unkillable mirror test, and M-6.4's 0/3 on the decision, so T-06 now has one unifying idea rather than five separate lessons: **assert in the language of the requirement.** In-module scope note keeps it honest (n=1, mechanism generalizes, not the ratio). **T-06 retrospective**: five modules, five live drills, two genuine latent bugs found by generated tests (M-6.1's gauge boundary via spec-anchoring, M-6.3's whitespace-score gap), one measured mutation baseline, and the Docker-down constraint turned into M-6.2's skip-clean lesson. Every clone restored pristine; every plant (BUG-API-01/02/03, BUG-WEB-01/02/03/04) still intact and unfixed for its own module. Recurring theme now spanning T-05+T-06 (7 occurrences): the agent writes, the human runs the verification. Next: T-07 Context, Memory & Agent Constitutions (M-7.1 CLAUDE.md deep dive) — note that ANIM-10 is pre-specced in VISUALS.md for it, and the course's own CLAUDE.md + this repo's constitution work is fair game as a live artifact.

- 2026-07-24 — **M-7.1 built; T-07 opened, and the bare-repo design decision from M-1.4 paid off exactly as intended.** The before/after is the strongest ROI artifact in the course: without a constitution the agent's *correct* engineering instinct (match production for consistency) leads it to recommend propagating BUG-API-01 into new code; with 36 lines of signed policy it flips to the rule AND contains the bug. Worth stating in future modules: the constitution didn't add capability, it settled an ambiguity the code cannot resolve. **Design decisions recorded:** (1) the priorauth-api CLAUDE.md written here is kept as a recording artifact but REMOVED from the clone — the repos stay bare so students build their own and get the same delta; do not commit a constitution into the teaching repos. (2) The signed-policy section deliberately includes a "known deviations — do not helpfully fix" clause naming BUG-API-01, which is now the pattern for protecting every remaining plant from well-meaning agents; reuse that wording in M-7.2/T-10 labs. (3) ANIM-10 shipped with FOUR scopes vs the v1.0 spec's three (managed policy added after doc verification) — second deliberate spec deviation this week, both logged in VISUALS.md rows so spec/build never silently disagree. Next: M-7.2 (AGENTS.md & the cross-tool standard) — the doc research for M-7.1 already confirmed the key mechanic: Claude Code reads CLAUDE.md, NOT AGENTS.md, and the supported bridge is a `@AGENTS.md` import (or symlink; on Windows use the import since symlinks need admin/Developer Mode). That's M-7.2's spine and it's already verified.

- 2026-07-24 — **M-7.2 built; a doc claim tested rather than repeated, and the naive reading turned out wrong.** Had I written the module straight from the docs it would have taught "Claude Code ignores AGENTS.md" — RUN A disproves that (the agent found and cited it in 30s). The correct, verified framing is **discovered vs loaded**, isolated by the no-file-reads probe (RUN A2/B). **Method worth reusing across the course:** to prove whether something is in *loaded context* vs merely *reachable*, add "do NOT read or open any files; from your loaded context alone; if it isn't there, say so plainly" — the agent answers that honestly and it cleanly separates memory from discovery. This is the second time this week that testing a documented claim produced a better module than quoting it (cf. M-6.2's skip-clean). Probe-design note: the test rule must CONTRADICT the codebase (here `*Policy` vs all-existing `*Service`) or a correct answer proves nothing — the agent could have inferred it. Cross-tool matrix is doc-verified fresh; VS 2026's non-support and the `.agent.md` ≠ AGENTS.md distinction are the two things teams most often get wrong. Next: M-7.3 (the context window is a budget — SIM-03 is pre-specced), and this module sets it up directly: every loaded line, including an imported AGENTS.md, is spending that budget.

- 2026-07-24 — **M-7.3 built; the course now measures its own context instead of describing it.** Reusable instrument for T-08 (which is entirely about this): `claude -p "<trivial prompt>" --output-format json` returns a `usage` envelope with cache_read / cache_creation / input / output tokens plus total_cost_usd — hold the prompt trivial and identical and every delta is context, not work. **The methodological catch worth remembering:** my first reading of the cost numbers ($0.013 → $0.088) looked like a dramatic "bloat costs 6.5x" headline; checking cache_creation vs cache_read showed the gap was mostly one-off cache writes caused by changing the prefix. The module teaches the corrected version AND the act of checking — a second instance this week of not over-reading one's own instrument (cf. M-6.5's n=1 hedge). Carry this into M-8.1/8.2: per-turn cost claims need steady-state runs, not first-run-after-an-edit. Derived constant used by SIM-03: ~28 tokens/line of CLAUDE.md (36 lines ≈ 1,025; 170 ≈ 4,609). SIM-03 deliberately ships WITHOUT the v1.0 spec's "cost-per-turn meter" for exactly the overclaim reason — third logged spec deviation (with ANIM-09's colours and ANIM-10's fourth card), all recorded in VISUALS.md rows. Next: M-7.4 (Claude memory in practice — `#` capture, /memory editing, pruning discipline); note that auto-memory is a distinct system from CLAUDE.md and the docs fetched for M-7.1 cover it (MEMORY.md index, 200-line/25KB load limit, ~/.claude/projects/<project>/memory/) — that's M-7.4's spine and it's already verified.

- 2026-07-24 — **M-7.4 built; the course used its OWN memory directory as the teaching artifact.** Nice property: the example is unfakeable and self-documenting — 479 B index / 4.2 KB topics / ~10% loaded, plus a genuine prune (next-session-m23, deleted the day it was fulfilled) that the module cites as the discipline in action. Recommend keeping this pattern where the course's own build produces a legitimate artifact (recordings/, PROGRESS decisions, this memory dir) — it is more honest than a constructed example and costs nothing to gather. **Stale-naming catch #5 (`#` shortcut)**: unlike catches 1–4 (Composer, Edit mode, @workspace, find_symbol) this one is *ambiguous rather than dead* — it exists in the wild and in an official announcement but is absent from the current memory docs, with a public bug report. Course position is "verify in your version, don't build a habit on it"; if a future refresh finds it documented again, soften the caveat rather than reversing it. Note for M-7.5 (track close): the anti-patterns module now has three concrete failure modes already measured in this track — bloat (+3,584 tok, M-7.3), silent index truncation (M-7.4), and the duplicate-instructions drift M-7.2's labC reproduces — so it can be built almost entirely from evidence this course generated rather than generic advice.

- 2026-07-24 — **M-7.5 built; T-07 COMPLETE (5/5).** The loud-vs-quiet staleness distinction is the track's best original finding: a stale line that ERRORS gets fixed, a stale line that silently no-ops (a nonexistent Maven profile that only produces a warning) becomes load-bearing misinformation. That reframes "keep your docs current" from hygiene advice into a specific hunt — look for flags/profiles/scripts that fail soft. **T-07 retrospective**: five modules, every claim either doc-verified fresh or measured in-session; the track produced four numbers the course now reuses (context floor 25,514; memory rent +1,025 lean vs +4,609 bloat; auto-memory index 479 B ≈ 10% of its directory; before/after constitution 90s→24s with the recommendation flipping from bug to policy). Three deliberate VISUALS spec deviations logged this week (ANIM-09 colours, ANIM-10 four cards, SIM-03 no cost meter). Two "test the doc claim" wins (M-6.2 skip-clean, M-7.2 discovered-vs-loaded) plus two instrument-hedges (M-6.5 n=1, M-7.3 cache creation) — that pattern of checking my own instrument before publishing a number is now the house standard. **ACTION FOR VARA (not actioned by me — editing the repo constitution is the owner's call):** CLAUDE.md's "ONE module per session. Never build two." is stale as of the 2026-07-23 continuous-mode directive; it is cited in-module as a live example of anti-pattern #2. Either update the line or re-assert the rule. Next: T-08 The Token Economy (M-8.1 What you pay for) — the JSON usage envelope method from M-7.3 is the instrument, and the M-7.3 caveat (steady-state vs cache-creation runs) is a prerequisite for every cost claim in that track.

- 2026-07-24 — **M-8.1 built; T-08 opened, and it is the course's first CLAUDE-LED module** (T-04 was the dual-path exception; this is the single-path one). The player's existing pill logic handled it with no shell change — `nLabs===1 && labA` → "CLAUDE-LED" — and the harness now asserts that rendering so a future refactor can't silently break reduced-path tracks. **Unexpected finding worth carrying into M-8.4**: `modelUsage` showed TWO models inside one invocation (an auxiliary haiku call at 0.22% of the bill alongside opus at 99.78%). That is real per-model attribution available for free in the envelope, and it defuses the most common cost proposal ("switch to a cheaper model") with data before M-8.4 even argues it — M-8.4 should open from this baseline rather than re-deriving it. Method reminder for the rest of T-08: every cost claim needs steady-state comparison (M-7.3), and the three-run ladder (trivial / single-file / multi-step) is a good reusable shape — it isolates turns as the variable while holding the repo and the prompt size roughly constant. Next: M-8.2 (context pollution — paste vs reference-by-path; SIM-06 Session Bill Meter is pre-specced), which now has a natural hook: the 1:17,765 ratio means pasted content is charged on every subsequent turn, not once.

- 2026-07-24 — **M-8.2 built; the single most persuasive cost artifact in the course so far.** 9.7× on an identical question with an identical answer is the kind of number that changes a habit in one reading, and the "cheaper run used MORE turns" detail pre-empts the only defence people offer for pasting. Method note: the paste was simulated by piping the file into `claude -p`, which is the faithful analogue and dodges Windows command-line length limits — reuse that shape for any future paste experiments. The mechanism explanation (cache_creation 103,192 vs 6,465, i.e. the agent greps instead of swallowing 1,086 lines) is what makes the number *teachable* rather than just alarming, so keep both halves together if this is ever excerpted. Deliberately preserved nuance so the lesson doesn't become dogma: pasting is correct for content not reachable from disk (someone else's stack trace, output from a system the agent can't open) — paste the slice, not the file. Cross-links now form a chain worth keeping intact: M-8.1 (context is re-sent per turn) → M-8.2 (so don't volunteer more of it) → M-8.3 (what to do once a session is already polluted). Next: M-8.3 (compaction strategy — when /compact loses the plot; checkpoint + /clear + re-anchor), which should reuse M-7.3's verified compaction facts (project-root CLAUDE.md is re-read and re-injected; nested files aren't; conversation-only instructions are summariser fodder).

- 2026-07-24 — **M-8.3 built; the first module in this course whose headline finding is MY OWN HYPOTHESIS BEING WRONG.** I predicted re-anchoring would cut cost; measured, it cost more than rediscovering ($0.4447 vs $0.3267, same 9 turns, more output) because facts handed over are treated as a starting point, not a stopping point. The third variant (anchor + explicit scope limit) is what collapsed it to 1 turn / $0.0611 / 19%. Publishing the failed prediction *as the module's spine* is the right call and consistent with the standard set in M-6.5 (n=1 hedge) and M-7.3 (cache-creation correction) — but this is the strongest version of it, because the corrected lesson (accuracy lever vs economy lever) is genuinely more useful than the one I set out to teach and would not have been found by reasoning alone. **Generalisable technique now proven twice** (M-7.2's no-file-reads probe, M-8.3's scope limit): telling an agent what NOT to do is a precise instrument — for isolating loaded-vs-discovered context, and for collapsing turn count. Worth teaching explicitly if a later module needs it. Caveat deliberately kept in the lab: a scoped answer built on wrong anchor facts is "confidently wrong and cheap", which is worse than expensive and right — scoping must never be pulled without verified facts. Next: M-8.4 (model selection economics) CLOSES T-08 — and per M-8.1's modelUsage finding it should OPEN from the measured baseline that the auxiliary model was 0.22% of the bill, then make the three-way cost comparison the blueprint calls for.

- 2026-07-24 — **M-8.4 built; T-08 COMPLETE (4/4); engineer path at 40/60 (two-thirds).** The judgment-task result is the strongest safety finding in the course: a cheap model produced a fluent, precisely-line-cited, **factually wrong** claim about the codebase's actual behaviour and signalled no doubt whatsoever. That reframes model economics from "how much can I save" to "which errors are detectable" — and it is why the module's routing rule is the *checkability* test rather than task size. Ground truth (line 99 = strict `>`) was re-read from source before publishing the claim that the small model erred; do this for any module that accuses a model of being wrong. **T-08 retrospective**: the whole track ran on one instrument (the JSON usage envelope) and produced four numbers the course can reuse — context floor ~25.5k, turn multiplier 21×, paste penalty 9.7×, scope-limit saving to 19% — plus two corrections of my own hypotheses (M-7.3 cache-creation, M-8.3 re-anchoring). Pricing structure was taught deliberately WITHOUT price points (they drift; the structural insight — metered makes waste legible, seat pricing hides it — does not), and the harness asserts no per-seat dollar figures leaked in. **Reduced-path rendering is now proven end-to-end**: all four T-08 modules are labA-only and render CLAUDE-LED; T-04's dual-path and the tri-path default are all covered by harness checks, so the player's pill logic is regression-guarded across every track shape the blueprint defines. Next: T-09 Subagents, Skills & Custom Agents (7 modules, the largest remaining track; M-9.1 SKILL.md anatomy). Note SIM-05 (Subagent Org Chart) is pre-specced for M-9.4/9.5, and M-8.1's per-model attribution finding gives M-9.5's parallel fan-out a natural cost angle.

- 2026-07-24 — **M-9.1 built; T-09 opened, and the control-test discipline from M-6.1/M-7.2 now has a third application.** The reusable technique, worth naming: **make firing detectable** (a mandatory output format + a literal marker line in the skill body) and then run BOTH a positive and a negative prompt. Without the marker you cannot distinguish "the skill fired" from "the model answered well anyway"; without the control you cannot distinguish "the description matches" from "the skill fires on everything". Both halves were needed and both passed. **Naming/structure catch worth flagging to Vara**: custom commands have merged into skills — this repo's own `.claude/commands/` (plan-module, build-module, validate-module, refresh-module) still work exactly as before, so no action is required, but if those ever want supporting files or invocation control they can move to `.claude/skills/<name>/SKILL.md` with no behaviour change. The skills-vs-constitution economics ("a skill's body loads only when it's used") is the cleanest possible payoff for T-07+T-08 and is now the stated rule: facts in the constitution, procedures in skills. Next: M-9.2 (one skill, two tools — the blueprint calls it the killer lab) and the Agent Skills open-standard fact recorded here is its spine; note the doc says Claude Code *extends* the standard with invocation control, subagent execution and dynamic context injection, so the cross-tool lab should test the STANDARD parts and flag the extensions as Claude-only.

- 2026-07-24 — **M-9.2 built; the "killer lab" premise survived verification AND produced a finding the docs don't contain.** Two things worth carrying: (1) **the registration probe is now a core course instrument, used three times with three answer-flips** (M-7.2 AGENTS.md loaded-vs-discovered, M-9.1 skill firing vs model competence, M-9.2 skill registry vs file discovery). Its shape is always the same — forbid file reads, ask what is *loaded/registered*, invite "say so plainly" — and every time, the naive test alone would have published a wrong conclusion. Any future module claiming a tool "picks up" a file should run it. (2) **The one-directional interop is genuinely new information** (Copilot reads `.claude/skills/`; Claude does not register `.github/skills/`), so the repo-layout advice — one shared skill file, in `.claude/skills/` — is evidence-backed rather than convention. If a future Claude Code release adds `.github/skills/` registration this module needs a refresh; the recording states the measurement date so the claim is falsifiable. Cursor's skill support was deliberately left unverified rather than assumed (labC dates the student's own finding) — consistent with the course standard of not extending a verified claim to an unverified tool. Next: M-9.3 (Skills vs Hooks vs Subagents — "Skill teaches the how, Hook enforces the rule, Subagent isolates the work"), which is a boundary/definition module; the hooks-are-enforcement-not-context point is already established in M-7.1/M-7.4 and the `context: fork` frontmatter from M-9.1 is the natural bridge to subagents.

- 2026-07-25 — **M-9.3 built; the boundary module, proven with a controlled A/B where only the hook varied.** The measurement is the strongest form of the enforce-vs-advise thesis: same model, same prompt, same `acceptEdits` mode, one variable (the PreToolUse hook). With it, an instructed bad write is DENIED (git diff empty); without it, the identical prompt lands the regression (`>=`→`>`) — and in the control the model read straight past the code's own "INCLUSIVE" javadoc to obey the instruction. That is the whole case for hooks over advice, and it ties M-8.4 forward (advice is only as reliable as the model on the day; a hook doesn't care which model drives). **Method worth reusing for T-10's hooks module (M-10.2):** a PreToolUse hook that inspects edit CONTENT (not just a path, which is all a permission rule can match) is the distinguishing demo — `node` script off stdin, deny JSON `hookSpecificOutput.permissionDecision:"deny"`, unit-tested with piped fake payloads before trusting it. Reused here as a **regression-guard** (encode a fixed bug so it can't come back) — a genuinely useful pattern to name again in T-10. **Boundary discipline honoured:** M-9.3 teaches *when to reach for which*, and deliberately does NOT deep-dive hooks config (that's M-10.2's territory) — flagged so the two don't collide. **Honest limit taught in-module:** a hook always RUNS but only checks the predicate you wrote (the agent noticed it could nudge the constant to sidestep the string-match, and declined) — determinism ≠ completeness; write the predicate carefully. **Cross-tool finding (doc-verified 2026-07-25):** the "which tool lacks which" framing has collapsed — Copilot and Cursor now BOTH ship skills (`SKILL.md`), hooks (`preToolUse`/`postToolUse`, deny on exit 2, fail-closed), and isolated-context subagents, with near-identical vocabulary to Claude Code; the module pivots to "all three converged on the same three primitives, pick by the job." So the tri-path is doc-true rather than nearest-equivalent. New SIM-18 (role-routing drill + measured proof strip; colours are the tool palette repurposed for ROLES, flagged in foot + VISUALS row). Disposable clone deleted, priorauth-api untouched (main, clean), no plant disturbed. Next: M-9.4 (your first subagent — read-only PA-rules code-review subagent; SIM-05 org chart is pre-specced for M-9.4/9.5, and M-9.3's subagent-isolation quote + the fan-out card in SIM-18 set it up).

- 2026-07-25 — **M-9.4 built; the first real subagent, and three runs turned "subagents isolate work" from a claim into a measurement.** The module's spine is a read-only `pa-reviewer` and what it proves: (1) delegation is a deliberate act headless — a plain-language ask had the main agent answer correctly with **no marker**, i.e. it never delegated (the M-9.2/M-9.3 detectable-marker probe, now applied to subagents; **reusable rule: any module claiming a subagent "ran" must show a marker, because the base model answers well enough to fool you**); (2) explicit invocation delegated and the child returned a compact verdict having read four files in its own window — the isolation/token payoff shown, not asserted; (3) the read-only `tools: Read, Grep, Glob` list is a HARD boundary — told to fix under `acceptEdits`, the agent had no Edit/Write tool and `git diff` stayed empty. **The clean contrast to carry into M-9.7 (tool-scoping design):** M-9.3's hook *denies* an attempted action; least-privilege scoping means the action was *never possible* — "design the door, not the sign." **Method notes for M-9.5 (parallel fan-out, next):** the same pa-reviewer is the unit to fan out three ways (controller/service/repo); explicit delegation is the reliable trigger headless; a marker-ended output format per child is how you'll prove all three ran; SIM-05 is already built and shared, so M-9.5 needs no new visual (reuse it, or add a fan-out-specific opts flag like SIM-09's autotrace precedent). **Honest scope flagged in-module and here:** headless auto-delegation to a project subagent is undocumented — I tested it (Run 1) rather than claiming it; if a future release documents/enables it, soften the "name it explicitly" guidance. SIM-05 bars are schematic (isolation mechanism, not token counts) with the one real number — pa-reviewer's 1-violation-3-passes + read-only — in the foot; noted in the VISUALS row so spec and build don't silently diverge. The subagent also did a genuine **domain** review (1 violation + 3 passes), which is the actual product: a scoped specialist that audits the rules the domain turns on, every time, in its own context, and cannot wander into editing. Disposable clone deleted, priorauth-api untouched (main, clean), no plant disturbed. Next: M-9.5 (parallel fan-out — three subagents over controller/service/repository concurrently; SIM-05 reused; M-8.1's per-model attribution gives it a cost angle).

- 2026-07-25 — **M-9.5 built; T-09 at 5/7, and the course's cleanest measured proof of concurrency.** The module's spine is a single ratio from the usage envelope: `duration_api_ms` 185,890 vs wall-clock `duration_ms` 87,981 — ~186s of review work delivered in ~88s because three subagents ran at once. That turns the docs' "time of the slowest, not the sum" from a claim into a measurement, and the parent's own 1,415-token output (despite three whole-layer reviews) turns isolation into a number too. **Reusable instruments for the rest of T-09/T-10:** (1) `duration_api_ms` vs `duration_ms` in `--output-format json` is a free, honest concurrency meter — api > wall-clock is the parallelism factor; (2) `--output-format stream-json --verbose` + a tiny parser over the `tool_use`/`tool_result` events is how you PROVE a fan-out (markers present in the trace; all children dispatched before any returned = concurrent). **The most important honest finding, carry it forward:** when a parent synthesizes subagent output it strips the marker lines, so the synthesized prose cannot prove delegation — the M-9.4 marker probe must move to the TRACE for any multi-subagent claim. Any future module asserting "N subagents ran in parallel" should show the stream, not the summary. **Economics stance locked for the track:** parallel fan-out is faster + leaner, not cheaper (you pay for every child; no doc says otherwise) — stated in-module and pre-empts the "parallel = cheaper" misread the way M-8.4 pre-empted "cheap model = cheaper." **Bonus worth reusing:** three scoped specialists produced a *convergence* (the N+1 seen from controller + repo independently) — an emergent confidence signal a single generalist can't give; good material for M-9.7 (team design). **Visual:** reused SIM-05 via a `fanout:true` opts flag (SIM-09-autotrace precedent) rather than a new component — swaps to the three real layer reviewers + a measured foot, M-9.4's plain mount untouched and both harness-guarded (this is now the 2nd shared-visual-with-opts case; the pattern holds). Disposable clone deleted, priorauth-api untouched (main, clean), no plant disturbed. Next: M-9.6 (Custom agents in Copilot — `.agent.md`, user-level `~/.github/agents/`, `@agent` syntax, org sharing) — a Copilot-led module (M-1.3/M-4.1 doc-verification precedent; the `.agent.md` sub-agent facts from M-9.4/9.5's verification are already in hand).

- 2026-07-25 — **M-9.6 built; T-09 at 6/7, and doc-verification caught the blueprint being wrong before the course could repeat it.** The blueprint's own M-9.6 line ("~/.github/agents/, @agent syntax") turned out to be Visual-Studio-specific; VS Code and the CLI use `~/.copilot/agents/` and the agents dropdown / `--agent`. This is the 6th stale-or-imprecise-claim catch (after Composer, Edit mode, @workspace, find_symbol, the `#` memory shortcut) and the first where the stale source was **our own blueprint**, not an upstream tool rename — a good reminder that the "verify, don't quote" discipline applies to internal specs too. Handled per the established convention: blueprint text left as-is, module + this log carry the correction, and the correction is taught as the module's spine ("check your surface"). **Reusable for M-9.7 and any Copilot-team module:** the four-scope model (repo → user → org → enterprise) with **lowest-wins precedence** is the same nearest-copy-wins shape as CLAUDE.md's hierarchy (M-7.1), now company-wide — that framing lets the leadership-path governance modules (L-3.x) reuse this directly. **needsVerification avoided by scoping claims to what each surface documents:** did NOT claim `@name` works in VS Code (only Visual Studio documents it), did NOT claim description-driven auto-delegation for VS Code (only CLI/cloud), did NOT use the literal word "deprecated" for `.chatmode.md` (docs say "previously known as"/"rename"). This is the pattern for the remaining Copilot-led modules: quote per surface, never generalise one surface's behaviour to the others. Visual: new ANIM-27 (abstract staged diagram, no fake IDE chrome per the Copilot-visual rule) shows both user paths honestly rather than picking one. No live run (IDE/cloud don't run headless) — same as M-1.3/M-4.1; the "recording" is the dated quote sheet. Next: M-9.7 (Agent team design — org-chart thinking, scoping tools per agent, SubagentStop-style quality gates) CLOSES T-09; it can reuse M-9.4's read-only scoping, M-9.5's fan-out + convergence, and M-9.6's scope/precedence model, plus SIM-05 is available (fan-out variant) if a visual fits.

- 2026-07-25 — **M-9.7 built; T-09 COMPLETE (7/7); engineer path at 47/60.** The track closer earns its "synthesis" billing with a live artifact that composes the whole track: a SubagentStop quality gate (M-9.3's deterministic enforcement) applied to a subagent team (M-9.4/9.5), scoped by least privilege (M-9.4) under an org-chart allowlist. **The proof is the strongest kind — the gate changed an outcome, shown two independent ways:** a contract-less reviewer emitted `REVIEW COMPLETE (lazy-reviewer)` (a marker only the gate's feedback contained, with the agent name interpolated by the hook itself), and the gate's own counter file went to 1 (written only on the block path). **Reusable instruments locked for T-10 (Workflows, Hooks & MCP, next track):** (1) SubagentStop gates read `last_assistant_message` directly — no transcript parse needed — so quality gates on agent output are cheap to build; (2) the M-9.3 unit-test-the-hook-with-piped-payloads method now covers PreToolUse (M-9.3) AND SubagentStop (here) — the pattern generalizes to any hook; (3) **verify a hook/subagent by its EFFECT, not the visible prose** — this now holds across M-9.5 (markers in the trace), M-9.4 (git diff), and M-9.7 (counter file + adopted format); it's the course's settled discipline for anything a parent synthesizes or a hook injects server-side. **Honest gap documented and taught, not hidden:** SubagentStop lacks `stop_hook_active`, so gates need their own loop counter — a genuine footgun worth the module time, and a good example of the course testing the mechanism rather than trusting the happy path. **T-09 retrospective:** seven modules, each with a live-measured or (M-9.6) doc-verified artifact — skill firing with a control (9.1), one-directional interop via a probe (9.2), enforce-vs-advise A/B (9.3), first subagent's three beats (9.4), fan-out concurrency proven from the trace at 2.1× (9.5), the blueprint's own path/@-syntax corrected (9.6), and the quality gate proven to rewrite an outcome (9.7). Recurring wins: the detectable-marker probe (9.1/9.2/9.4/9.7), verify-by-effect (9.4/9.5/9.7), and the unit-test-the-hook method (9.3/9.7). Two new interactive visuals (SIM-18, SIM-19), one reused-with-opts (SIM-05 fanout), one new staged ANIM (27). Every clone deleted, priorauth-api untouched throughout, all BUG-API/BUG-WEB plants intact. Next: **T-10 Workflows, Hooks & MCP (M-10.1 Slash commands)** — note M-9.1 established that custom commands merged into skills (`.claude/commands/x.md` ≡ `.claude/skills/x/SKILL.md`), and this repo's own `.claude/commands/` (plan/build/validate/refresh-module) are the natural live artifacts for a "encode your team's rituals" module.

- 2026-07-25 — **M-10.1 built; T-10 opens.** The live `/fix-and-test` run gives the track a clean opener and answers a real doc gap: **`claude -p "/custom-command args"` invokes a project slash command headless** (the docs only cover the SDK `query()` path) — worth reusing for any future headless-command demo, and it means the whole T-10 ritual/workflow track can be exercised via `-p`. **Method note for the rest of T-10:** the safe live-drill pattern is now settled — hand-plant a NON-plant break on a scratch branch (here an off-by-one in ClaimHistoryService, distinct from every BUG-API plant), run the artifact under `--dangerously-skip-permissions` in the disposable clone so `mvnw`+edits actually execute (this is how to get past the "agent blocked from mvnw" wall that recurred in T-05), then delete the clone. **Best unscripted beat:** the command's *restraint* wording made the agent flag the N+1 as out-of-scope instead of fixing it — the module leans on that as the thesis (a command's value is the judgement in its steps, not the keystrokes), which is a stronger lesson than "it saves typing" and sets up M-10.2 (hooks = the rule that fires whether you invoke it or not). **Convergence recorded across the course:** commands are now the *legacy on-ramp to a skill* in ALL THREE tools — Claude (`.claude/commands/`→`.claude/skills/`), Copilot (prompt files→skills), Cursor (2.4+ `.cursor/commands/`→`.cursor/skills/`, `/migrate-to-skills`); the "encode the ritual now, upgrade the housing later" framing is doc-true everywhere and ties T-09's skills work to T-10. Cross-tool claims scoped to what each doc states, with verify-in-your-version flags (Copilot `mode:` vs `agent:`, Cursor commands-vs-skills) — no over-assertion. Next: **M-10.2 Hooks** (PostToolUse formatter, pre-commit test gate, secret redaction) — note this course has ALREADY built and proven real hooks live: M-9.3 (PreToolUse deny, content-inspecting) and M-9.7 (SubagentStop quality gate), both with the unit-test-the-hook-with-piped-payloads method; M-10.2 should reuse that method and can go deeper on PostToolUse + the hook events table rather than re-establishing that hooks are deterministic.

- 2026-07-25 — **M-10.2 built; the live debugging BECAME the module.** The pre-commit gate wouldn't fire at first, and chasing that produced the two best teaching artifacts of the module — both genuine Windows portability bugs: (1) **hook matchers are tool-NAME specific and this machine's shell tool is `PowerShell`, not `Bash`** — a `matcher:"Bash"` hook is a silent no-op on Windows; (2) **`./mvnw` doesn't run under the hook's cmd.exe** — the gate threw and false-blocked every commit. Both are the kind of "your safety net has a hole you can't see" lesson that's worth more than a happy-path demo. **Important honesty note for the record:** mid-debug I hypothesised "`--dangerously-skip-permissions` bypasses PreToolUse hooks" and started to write it as a finding — it was WRONG; the real cause was the Bash-vs-PowerShell tool name. Kept digging (the agent-under-test actually diagnosed the tool-name issue) and corrected it before it reached the module. The lesson: verify the mechanism, don't ship the first plausible hypothesis — the same discipline as M-7.3/M-8.3/M-9.5. **Reusable for M-10.3+ and any Windows hook/command work:** (a) match `Bash|PowerShell` in every shell-targeting hook; (b) platform-detect shell commands (`.\mvnw.cmd` vs `./mvnw`); (c) to get an agent to actually run `git` headless so a PreToolUse hook can fire, the settings `permissions.allow` must be COMMITTED/active at session start (an uncommitted allowlist doesn't apply) OR pass `--allowedTools` — and do NOT use `--dangerously-skip-permissions` if you want to observe hooks (it ran git but the mis-matched hook didn't fire, which confused the diagnosis). (d) A PreToolUse gate runs its check in its OWN process, so it works even when the agent lacks permission to run the tests — this is the crisp "why a gate beats asking the agent" point. **Doc correction taught:** "secret redaction" (blueprint wording) is not prompt-rewriting — UserPromptSubmit can't rewrite; redaction is PreToolUse-deny-on-read or PostToolUse-`updatedToolOutput`-on-tool-output. **Scope honesty:** the redactor is unit-tested, not live-agent-verified this session; the module says so and doesn't claim a live redaction. **Process note:** a raw `'` inside a `jq` code example silently broke the whole MODS `<script>` (SyntaxError: Unexpected string) — validation caught it (window.modsFor undefined); escaped to `&#39;`. Reminder that any literal apostrophe/quote in module HTML strings must be an entity (same class of bug as the textContent-vs-innerHTML title rule). Next: **M-10.3 MCP servers** (connect a payer-policy lookup MCP; enterprise allowlists) — a new subject area; will need fresh doc verification on MCP config (`.mcp.json`/settings), transports (stdio/http/sse), tool namespacing `mcp__<server>__<tool>`, and allowlisting — and note this session's MCP tools (Gmail/Calendar/etc.) are NOT the teaching subject; the module should build/describe a PA-domain payer-policy MCP.

- 2026-07-25 — **M-10.3 built; engineer path at 50/60 (5/6 of the way).** First module to build a real external MCP server and drive it live — and it went smoothly (contrast M-10.2's grind), because the earlier Windows/permission lessons carried: used `--mcp-config` + `--allowedTools` from the start (not `--dangerously-skip-permissions`, which M-10.2 showed can bypass/confuse the safety layer). **Reusable for M-10.4/10.5 and any MCP work:** (a) `claude -p` DOES load a `.mcp.json`/`--mcp-config` server and call its tool headless, given `--allowedTools "mcp__<server>__<tool>"` — a clean instrument for demoing any MCP; (b) the tool namespace preserves the server name verbatim incl. hyphens (`mcp__payer-policy__...`), so allowlist strings must match exactly; (c) `--permission-mode dontAsk` is the clean way to demo a DENY (auto-denies anything not in allow) without an interactive prompt; (d) a good MCP demo proves the tool actually ran by checking for a detail only the tool's real output carries (here the null-reason-on-covered) — same verify-by-effect discipline as M-9.5/9.7. **Strongest teaching beat:** the allowlist isn't a nuisance, it's governance — un-allowlisted, the agent was denied AND refused to fabricate a coverage answer (M-1.6 boundary), which is the responsible-AI payoff a regulated domain needs. **Course-architecture note:** deliberately did NOT change EligibilityService — the MCP externalisation is taught as a pattern with a live standalone server, keeping the teaching repo's plants and its "policy lives in code" starting point intact (same discipline as M-7.1 keeping the repos bare). T-10 spine now clear for the close-out: slash command *invokes* the ritual (M-10.1), hook *enforces* the rule (M-10.2), MCP *connects* the world (M-10.3), each gated by an allowlist. Next: **M-10.4 Cloud & background agents** (Copilot cloud sessions from the IDE — issue+PR on remote infra; Claude Code headless/background runs) — likely a mixed doc-verify (Copilot cloud can't run here, M-1.3 precedent) + a real Claude Code background/`&`/`run_in_background` demo; note this session has used `-p` headless extensively so background/headless Claude is well-trodden.

- 2026-07-25 — **M-10.4 built; T-10 at 4/5.** The live artifact used the session's OWN background mechanism as the demo — I launched a detached `claude -p --output-format json` analysis and genuinely kept working (spawned the doc-verification agent, edited files) while it ran, then collected its envelope. That's an unfakeable demonstration of the fire-and-forget pattern (cf. M-7.4 using the real memory dir, M-9.2 using the real skill): the module's evidence is the actual thing it teaches. **Reusable instrument:** the `-p --output-format json` envelope (`result`/`is_error`/`total_cost_usd`/`num_turns`/`duration_ms`) is now confirmed live and is the crisp "why headless = composable" artifact — a script branches on `is_error`. **The teaching axis that makes this module more than a feature tour:** execution LOCUS + compute OWNERSHIP. Copilot cloud (GitHub-hosted) and the Claude Action (`anthropics/claude-code-action@v1`, your runners) both do `@mention → PR`; the difference is whose machine and where the audit log lives — a governance point that sets up the leadership-path L-3.x modules directly. **Honest-scope discipline held:** only the local background case ran; `--bg`/Action/Copilot-cloud need remote infra and are doc-quoted, not claimed as run (same standard as M-10.3's HTTP/OAuth). **Method note carried from M-10.2/10.3:** used shell-backgrounding + `-p` (not `--dangerously-skip-permissions`), read-only task so the clone stayed clean (0 dirty lines), deleted after. Next: **M-10.5 The full workflow** (issue → plan → branch → build → test → PR, agent-driven end to end) CLOSES T-10 — it should COMPOSE the track: a slash command (M-10.1) or the Claude Action (M-10.4) kicks it, Plan Mode (T-03) plans, hooks (M-10.2) gate the commit, MCP (M-10.3) supplies policy, and the output is a reviewable PR; the natural live drill is a real headless end-to-end run on a disposable clone producing a branch+commit (stop short of a real remote PR, or note it).

- 2026-07-25 — **M-10.5 built; T-10 COMPLETE (5/5); engineer path 52/60.** The close-out earns its "synthesis" billing with a single 141s headless run that went issue→PR end to end and, crucially, whose every claim was verified against the clone (branch/diff/tests/plant) rather than trusted — the verify-by-effect discipline now applied to a whole pipeline. **The strongest beat was unscripted and is the module's spine:** the agent found BUG-API-01 on its own, left it untouched per the rule, AND reasoned that it would inflate the very queue-depth count the new feature reports — judgment, not ticket-completion, which is the real signal an agent is main-branch-ready. **Reusable end-to-end drill pattern for T-12 (capstone) and any full-workflow demo:** commit an ISSUE.md as the start point, orchestrate the 7 steps in ONE `-p` prompt (plan/branch/implement/test-green/commit/PR.md, "don't push"), then VERIFY against the repo; keep it additive + "don't touch planted-bug files" so the teaching plants survive (they did). Stop short of the real remote PR to keep the teaching repo clean — the PR body is the artifact and the push→PR step is M-10.4's proven handoff. **T-10 retrospective:** five modules, four with strong live artifacts (/fix-and-test headless; the pre-commit gate's real red→green + 2 Windows portability bugs; a real MCP server built+queried+gated; a detached background run's envelope; and this end-to-end pipeline) + M-10.4's Copilot-cloud doc-verify. Recurring wins that now span the course: unit-test-the-hook, verify-by-effect, honest-scope (name what wasn't run), and the detectable-marker/plant-discipline. The track's one-line thesis — encode the ritual (10.1), enforce the rule (10.2), connect the world (10.3), choose where it runs (10.4), and the pipeline turns an issue into a PR you sign (10.5) — is now demonstrated, not asserted. Next: **T-11 Improving & Measuring Productivity (M-11.1 Your baseline** — week-1 timed tasks without AI; capture cycle time, defects, rework). A measurement/method track; M-11.1 is largely conceptual (baseline capture) — the course's OWN recorded cost/turn numbers (T-08 envelopes, the $0.89/9-turn, this module's 141s/$?) and the DORA/SPACE framing (M-11.2) are the reusable material; note ANIM-11 is pre-specced for M-11.3.

- 2026-07-25 — **M-11.1 built; T-11 opens.** First module of the measurement track, and the first pure-methodology module since M-4.1 — no headless artifact by nature, so the discipline is doc-verification + honest hedging rather than a live run. **The one idea that makes it more than "time yourself":** baseline QUALITY (escaped defects + rework) alongside SPEED (cycle time), because the documented AI-era risk is faster-but-sloppier (GitClear's churn-rising-with-AI finding is the anchor). That framing — speed and quality move independently, so measure both — is the reusable spine for M-11.3's delta and for the leadership L-5.x measurement modules. **Sourcing discipline worth carrying into M-11.2 (which goes deep on frameworks/vanity):** the strongest verbatim anchor for "don't trust one number / activity isn't productivity" is the SPACE paper ("productivity cannot be reduced to a single dimension (or metric!)", activity metrics "never used in isolation") — rock-solid and it's GitHub/MS researchers; the acceptance-rate="vanity" line is well-attested but I could NOT pin a single verbatim GitHub/DX sentence today, so M-11.2 should lean on SPACE for the load-bearing claim and treat acceptance-rate as "widely-held framing." GitClear churn numbers are hedged as one vendor's analysis (correlation≠causation, 2024 is a projection). **Course-reflexivity used again (cf. M-7.4 memory dir, M-9.2 skill):** the module cites its OWN real timings (90s→24s, 141s) as examples of measured tasks — but carefully framed, since every course timing is a WITH-AI run and the student's baseline is WITHOUT AI; didn't conflate them. New SIM-20 (speed-vs-quality metric tiles). Next: **M-11.2 Frameworks that survive contact** (DORA/SPACE/DX Core 4 + vanity metrics to refuse) — the verification for this module already gathered the DORA four-keys defs, SPACE 5 dimensions, DX Core 4 four dimensions, and the vanity-metric SPACE anchor, so M-11.2's doc research is largely in hand (in recordings/m111-baseline-metrics.txt); it should go deep on "what AI changes vs what it doesn't" and build the refuse-list M-11.1 only teased.

- 2026-07-25 — **M-11.2 built; T-11 at 2/3.** This is the clearest example yet of the course's "verify, don't quote" standard paying off: had I written the frameworks module from general knowledge it would have taught the 2023 DORA ("four keys", "MTTR", "AI hurts throughput and stability") — all now stale. Verification caught the de-acronym, the four→five metric change, the MTTR rename, the report rename, AND the 2024→2025 finding flip (AI now helps throughput, still hurts stability). **Stale-framing catch #7** (after Composer, Edit mode, @workspace, find_symbol, the # shortcut, the blueprint's Copilot user-path). This is the sixth stale-catch that came from an UPSTREAM change (only M-9.6 was our own blueprint) — reinforces that even "settled" reference material (a famous metrics framework) drifts and must be re-checked. **The amplifier finding is the course's thesis stated by an external authority:** "AI doesn't fix a team; it amplifies what's already there" + "without robust control systems (tests, version control, feedback loops) ... instability" — i.e. the 50+ modules of discipline this course built are precisely the control systems that decide whether AI's speed becomes a delivery gain or delivery instability. Worth referencing in the T-12 capstone and the L-5.x leadership measurement modules as the empirical backbone. **Sourcing discipline for future measurement/leadership modules:** DORA 2025 primary pages are solid and current; SPACE's canonical ACM URL 403s to fetchers (use the Microsoft Research abstract for hard quotes, or open the ACM PDF manually); the "acceptance-rate = vanity" line has no crisp pinnable primary — anchor it on SPACE + the DORA adoption-vs-stability wedge instead of quoting a sentence that doesn't exist verbatim. Reusable numbers now in hand for L-4/L-5: 90% AI adoption, 80%+ feel-more-productive, 30% low-trust, and the throughput-up/stability-down relationship. Next: **M-11.3 Your delta** CLOSES T-11 — re-run the M-11.1 baseline task class with the full toolkit, read the whole PANEL (not one number) before/after, and build the improvement playbook (instruction files → workflows → agents); **ANIM-11 (Personal Delta Dashboard) is pre-specced** for it (gauges cycle time / rework / tests-per-feature from week-1 baseline → final). The course's own before/after numbers (90s→24s, 141s, the T-08 envelopes) are the illustration material.

- 2026-07-25 — **M-11.3 built; T-11 COMPLETE (3/3); engineer path 55/60 (only T-12's 5 capstone modules remain).** The measurement track lands its thesis: the only defensible productivity claim is your own before/after panel on a task class you own, read honestly. M-11.3 pulls M-11.2's "read the panel, not the number" from the abstract into the personal — the amplifier is now *your* risk (faster cycle time with rising rework = negative delta), which retroactively justifies M-11.1's insistence on three metrics. **The improvement playbook (instruction files → workflows → agents) is the course's own architecture turned into advice** and is grounded in a real measured anchor (M-7.1's 90s→24s for a 36-line constitution), so it's evidence-backed, not editorial. That ordering + the amplifier framing is prime material for the leadership L-4/L-5 economics/measurement modules and the T-12 capstone's metrics read-out. **ANIM-11 built as pre-specced** (baseline→final gauges) with an added VERDICT panel that enforces the read-the-panel lesson — honest "example data, your numbers replace them" framing since a student's real numbers can't be injected at authoring time (cycle 90→24 is the one real course anchor). **T-11 retrospective:** three methodology modules, zero live headless runs (correct — they're about the student measuring themselves), but each rigorously doc-verified with hedges (M-11.1 metric defs; M-11.2's DORA currency correction = stale-catch #7); two new interactive drills (SIM-20 tiles, SIM-21 keep/refuse) + ANIM-11; the course's own timings reused as honest illustrations throughout. Recurring standard held: verify even "settled" reference material (DORA drifted), hedge what can't be pinned, name the confounds. Next: **T-12 Graduate Arc (M-12.1 From tools to method — AI-DLC in one hour)** — the final track (5 modules: AI-DLC method, Mob Elaboration, Mob Construction, Capstone, bonus pharmacy-PA migration). NEW subject (AWS AI-DLC) → will need fresh doc verification of the AI-DLC method (intent → units of work; Mob Elaboration/Construction ceremonies); REPO-SPECS.md defines the M-12.5 pharmacy-PA migration target. The capstone (M-12.4) is the natural place for a big live end-to-end run building on M-10.5's proven issue→PR pipeline.

- 2026-07-25 — **M-12.1 built; T-12 (the final track) opens.** First module on a genuinely external methodology (AWS AI-DLC), and the verification standard held: every AI-DLC claim is AWS-primary-sourced, with the two-vintage nuance flagged (the Jul-2025 announcement has the vivid "bolts"/speed vocabulary; the Nov-2025 open-source relaunch is soberer and adds the tool-agnostic stance). **The framing that makes the whole course cohere:** AI-DLC is explicitly tool-agnostic, and its human-gate principle ("Humans decide and validate, AI plans and executes") IS the plan-gate/review-the-diff/sign-the-rule discipline drilled across 11 tracks — so M-12.1 lands as "here is the method your toolkit was always the crew for," with an explicit phase→skill slot map. That map (Inception=T-02/03/07, Construction=T-04/05/06/09/10, Converge=M-10.5, Operations=M-10.4/T-11) is the spine for the rest of T-12 and for L-6 (Leading AI-DLC). **Reusable for M-12.2–12.5 + L-6:** the AWS primary quotes + the two-vintage caveat are in recordings/m121-ai-dlc.txt; ANIM-12 is built and shared across M-12.1–12.4+L-6.1 (reuse it, don't rebuild). **Live-run plan for the rest of the arc:** M-12.2 (Mob Elaboration) gets the first live decomposition — run an agent on "Using AI-DLC, cut the manual-review queue 20%" and capture the intent→units-of-work output; M-12.3 (Mob Construction) builds a unit; M-12.4 (Capstone) is the big end-to-end run on M-10.5's proven issue→PR pipeline, shipped 3 ways + the metrics read-out; M-12.5 ports to pharmacy PA (REPO-SPECS.md §pharmacy: NDC codes replace CPT, formulary-tier + days-supply checks, PBM "ScriptGuard", threshold semantics unchanged >=0.85). Deliberately NO teaching-repo change in M-12.1 (overview). Next: **M-12.2 Mob Elaboration** — the first real ceremony; deep-dive the intent→units-of-work move that M-12.1 only mapped.

- 2026-07-25 — **M-12.2 built; T-12 at 2/5.** The live Mob Elaboration is one of the strongest artifacts in the whole course: given a vague business intent, a read-only agent reshaped it against the real system, asked the questions a staff engineer would ask, decomposed it into six grounded units of work, and — the payoff — concluded the intent is a *risk decision, not a coding task*. It independently rediscovered three course threads (T-11's "no delta without a baseline" as UoW-1; the M-3.3 no-resolve-endpoint gap as "drain rate is zero"; BUG-API-01/03 as real queue levers), which is the proof that elaboration grounded in the actual codebase beats generic planning. **This validates the AI-DLC framing empirically:** the human-decides gate isn't ceremony — the ceremony's whole value was surfacing the human decision early. **Method note for M-12.3 (Mob Construction, next):** the units are already elaborated and validated; M-12.3 should BUILD one or two of the low-risk slice (the run's recommended order 1→3→2→4: instrument, restore `>=`, resolve endpoint, config) on a scratch branch — a natural live construction run that pairs with this elaboration, red→green→commit, gated by the M-10.2 hook, converging toward M-10.5's PR shape; keep it to the low-risk units so it's reliable and leaves the clinical/yield-unknown UoW-5 as the "this needs a human" example. **Reusable:** SIM-22 carries the real elaboration; ANIM-12 remains the shared full-flow (reuse for M-12.4). Ran read-only via `--permission-mode plan` (not skip-permissions) — correct for an Inception gate; git stayed clean, plants intact. Next: **M-12.3 Mob Construction** — build the validated units with agents as the crew, human as the approver.

- 2026-07-26 — **M-12.3 built; T-12 at 3/5.** The elaboration→construction pair (M-12.2→M-12.3) is now the strongest end-to-end AI-DLC demonstration in the course, and both halves came from real runs on the actual repo. M-12.3's payoff is the agent STOPPING at the policy unit and articulating the governance — an unscripted, genuinely excellent responsible-AI beat (disparate-impact, appealability, shadow dry-run) that lands the "human as approver, not typist" thesis as a *safety property*, not a slogan, and ties it explicitly to M-1.6. **The two ran cleanly** because the earlier hard-won lessons held: M-12.2 read-only via `--permission-mode plan` (Inception gate, git clean); M-12.3 built via `--dangerously-skip-permissions` on a disposable clone (no hook being observed, so skip-permissions is correct per M-10.2) and the AGENT ran mvnw fine (the hook-vs-agent mvnw distinction from M-10.2). **Reusable for M-12.4 (Capstone, next):** the feat/cut-queue construction is essentially the capstone's shape already — the natural M-12.4 run is a full feature end-to-end (elaborate→construct→PR) shipped and then the metrics read-out (T-11 panel + ANIM-11), done three ways (once per tool per blueprint) or at least once-live-plus-doc-verified for the others; ANIM-12 (shared) + ANIM-11 (delta dashboard) are the visuals, so likely no new component needed — reuse. M-12.5 = pharmacy-PA migration (REPO-SPECS §pharmacy). **Only 2 engineer modules remain (M-12.4 capstone, M-12.5 bonus migration) → 60/60 in sight.** Next: **M-12.4 Capstone** — ship one full feature three ways, defend the diff, final metrics read-out (ANIM-12).

- 2026-07-26 — **M-12.4 built; T-12 at 4/5; engineer path 59/60 — ONE module from complete.** The capstone did what a capstone should: a genuine full-stack feature (new API endpoint + React screen + tests both sides + two PR-ready diffs) shipped live in ~7.5 min across BOTH teaching repos, quality holding (all suites green, every plant intact in both repos), with the agent surfacing its own defend-the-diff notes. It composes the entire arc end to end and the entire course: intent→units (M-12.2), build+stop (M-12.3), full feature→defended PR (here), measured against the T-11 panel, framed as ship-3-ways for a measured tool-fit verdict (M-2.5). **First module to drive BOTH repos live in one session** — the pattern (two focused headless runs, one per repo, verify each, delete clones) is the template for M-12.5's two-repo migration. **Honest scope held:** ran one tool (Claude Code) live full-stack; the three-way comparison is the student's lab (Copilot cloud needs remote infra) — not claimed as run. **VISUALS deviation logged** (4th of the build): spec's ANIM-12-for-read-out → dedicated SIM-24 carrying the real capstone numbers + a click-to-expand defend-the-diff panel; ANIM-12 remains the shared ceremony flow. **Only M-12.5 remains for 60/60 (engineer path complete).** Next: **M-12.5 Bonus capstone — the pharmacy-PA domain migration.** Per REPO-SPECS.md §pharmacy: port both repos clinical PA → pharmacy PA (PBM): NDC codes replace CPT, eligibility adds formulary-tier + days-supply checks, payers become PBMs (CaremarkRx-like "ScriptGuard"), threshold semantics unchanged (>=0.85 inclusive). Spec-first, any tool per the tool-fit table, defend the diff + measure the effort. The natural live drill: an agent-driven migration of ONE slice (e.g. EligibilityService CPT→NDC + a payer→PBM rename) on a disposable clone, spec-first, measured — demonstrating the migration pattern without fully porting both repos; keep it grounded and honest about scope. It's the M-12.4 full-stack pattern at migration scale.

- 2026-07-26 — **M-12.5 built; T-12 COMPLETE; 🎉 ENGINEER PATH COMPLETE — 60/60 modules.** The final module closes the arc it belongs to and the whole course: a spec-first domain migration where the agent did the compiler-guided mechanical bulk and — the real deliverable — produced an honest ripple map that found the true cost centre (the seed fixture / schema, not the renamed code), was explicit about inferred-vs-verified scope (the web repo it couldn't open), and flagged that a faithful migration carries the existing plant forward ("not a cleanup"). Every capstone module (M-12.2/12.3/12.4/12.5) was a REAL live run on the actual repos, each restored pristine, every plant intact. **Course-level retrospective (engineer path):** 60 modules across 12 tracks, the overwhelming majority carrying a live-measured or freshly-doc-verified artifact; the house standards that held throughout — verify even "settled" reference material (7 stale-catches: Composer, Edit mode, @workspace, find_symbol, the `#` shortcut, the blueprint's Copilot path, and DORA's 2024→2025 flip), verify-by-effect not prose, unit-test-the-hook, honest-scope (name what wasn't run), the detectable-marker/plant-discipline, and hedge what can't be pinned to a primary. 25 SIM + 12 ANIM + 5 TERM visual components, 4 logged VISUALS deviations. Both teaching repos (priorauth-api/-web) remain pristine on main with all BUG-API/BUG-WEB plants intact — the course never dirtied them. **Remaining course work: the LEADERSHIP PATH (0/22)** — L-01..L-06, no coding, reusing the engineer path's measured artifacts (the 90s→24s constitution delta, the T-08 cost envelopes, the DORA-2025/amplifier finding, the AI-DLC ceremonies, the disparate-impact refusal) as its evidence base; ANIM-11/12/13/15 and SIM already feed several L-modules per VISUALS.md. If the leadership path is in scope next, L-1.1 ("What agents actually do all day") is the opener. **Deploy/catalog tail (unchanged, low-priority):** the learnings-hub card still says "59 + 22 modules" / "60 + 22" — refresh to reflect 60 eng complete next time that repo (deploy branch `protected`) is touched.

- 2026-07-26 — **L-1.1 built; the LEADERSHIP PATH opens (1/22).** Two decisions were Vara's to make (asked via AskUserQuestion, since they set the template for all 22 lead modules AND touched CLAUDE.md's "don't refactor the shell" rule): (1) **lead lab structure = Playbook lab + Live-tool walkthrough** (2 labs, not tri-path); (2) **a minimal lead-aware shell tweak is approved** — this is completing the scaffold the shell already started (the lead legend/tracks/path-desc were there; only renderLabs/pill were eng-only). Implemented additively and regression-guarded so the 60 engineer modules render exactly as before. **The leadership-path authoring pattern established here** (reuse for L-1.2 … L-6.3): lead modules are conceptual/no-code, `path:'lead'`, `track:'L-0x'`, use `labA`=Playbook + `labB`=Walkthrough, and — crucially — **their evidence base is the engineer path's own recorded runs**, not new tool verification. L-1.1 cites six real course moments as its "shift", turning the eng path into the lead path's proof. That's the honest, unfakeable move (cf. M-7.4 memory dir, M-9.2 skill): the leader watches the same runs the engineers built. Any new external claim in a lead module (e.g. adoption stats in L-02, governance/IP in L-03) still needs fresh doc verification, but the mental-model/method modules can largely compose verified facts already in hand (DORA-2025 amplifier, the T-08 cost envelopes, the AI-DLC ceremonies, M-12.3's disparate-impact refusal). **ANIM-33 built** (a shift timeline) — a new lead visual; existing ANIM-11/12/13/15 also feed later L-modules per VISUALS.md, so several lead modules can reuse rather than build. Next: **L-1.2 Vibe vs spec as a risk lens** — where AI-written code belongs on a risk register; reuses T-02's vibe/spec material (SIM-02/07) read as a manager.

- 2026-07-26 — **L-1.2 built (leadership 2/22).** Confirms the leadership authoring pattern works well: the module reframes verified engineer-path material (the M-2.4 rubric, the M-2.2 silent-failure run, the DORA amplifier) into a manager's tool (the risk register) with zero new external claims — fast to build, honest, and it makes the engineer path's evidence pay double. **Visual decision worth noting:** the blueprint tagged SIM-02/07 (T-02 engineer drills) as L-1.2 candidates, but I built a leadership-native SIM-26 (a tier→control risk register) instead — a better fit for the audience than lifting a task-placement drill, and cheap. General rule forming for the lead path: **reuse an engineer visual when it IS the artifact (ANIM-11/12/13/15), build a small leadership-native one when the framing genuinely differs** (a risk register, a shift timeline). **Reusable spine for L-1.3/L-1.4 and the governance track:** the risk-tier + human-gate framing here sets up L-1.3 (the instruction file encodes the tiers so they're enforced by default) and L-3.x (governance/review policy) directly; the "classify by blast radius, guardrails scale with volume, police the drift" line is the through-thread. Next: **L-1.3 The instruction file is the asset** — why CLAUDE.md/AGENTS.md quality predicts team ROI; reuses T-07 (the 90s→24s constitution delta, the bloat cost, M-9.6's org/enterprise scope) read as an asset a manager invests in.

- 2026-07-26 — **L-1.3 built (leadership 3/22).** Third lead module, and the reframe pattern keeps paying: the 90s→24s / recommendation-flip from M-7.1 becomes the cleanest ROI case a manager could ask for (cheap in, compounding out, and it changed a *decision* not just speed), the M-7.3/M-7.5 costs become the liability side, and M-9.6's scope becomes the governance lever — all from verified numbers, zero new claims. **L-01 (The Leader's Mental Model) is now 3/4** — the three built (watch-a-shift / risk-lens / instruction-file-asset) form a coherent leader's foundation: what the pair does, how to classify its output, and the highest-leverage thing to invest in. **Author's note carried forward for the deeper lead tracks:** L-1.1–1.3 all ran on reframed engineer evidence (no verification needed), but L-02 (adoption playbook), L-03 (governance/IP/compliance), L-04 (economics — seats vs tokens pricing), L-05 (DORA/SPACE at org level) will contain NEW external claims (pricing, IP/legal, org-metrics practice) that DO need fresh doc verification — don't let the reframe-from-engineer-path habit skip that. **Visual reuse decision holding:** building small leadership-native SIMs (26 risk-register, 27 constitution-asset) rather than lifting engineer drills; ANIM-11/12/13/15 remain available for later L-modules per VISUALS.md. Next: **L-1.4 The fleet decision** — which tool for which use case (UI squads vs platform teams vs GitHub-native orgs; standardize vs allow choice), the tool-fit table (BLUEPRINT / M-2.5) read as a manager not a fan; closes L-01. Reuses the tool-fit table + M-2.5's overturn-with-measurement; no new external verification if scoped to the (already-verified) capability claims.

- 2026-07-26 — **L-1.4 built; L-01 (The Leader's Mental Model) COMPLETE — leadership 4/22.** The four L-01 modules form one coherent arc read entirely off verified engineer-path evidence: what an engineer+agent pair does all day (L-1.1), how to classify its output by risk (L-1.2), the asset to fund first (L-1.3), and how to choose the fleet like a manager (L-1.4). **Leadership authoring pattern now proven across a full track** — reframe verified facts, build a small leadership-native SIM per module (26 risk-register, 27 constitution-asset, 28 fleet), reuse ANIM-33 for the shift; the eng-60 regression guard runs in every lead harness so the shell tweak stays safe. **⚠️ IMPORTANT for the next tracks — the free-reframe era ends here.** L-01 needed no new verification because it reframed T-02/07/09/10/11/12. L-02 (Adoption Playbook — pilot design, champions, enablement funnel, resistance patterns) is mostly org-practice/change-management with few hard external claims, BUT L-03 (Governance & Guardrails — permission/sandbox specifics, MCP allowlists, IP/provenance/compliance in healthcare), L-04 (Economics — seats-vs-tokens PRICING, per-agent cost, budgeting), and L-05 (DORA/SPACE/DX at ORG level + vanity metrics) all carry NEW external claims that MUST get fresh doc verification (pricing especially drifts; IP/legal must be sourced, not asserted; and cite the already-verified DORA-2025 currency for L-05, not the stale four-keys). Do not let the reframe habit skip verification there. **Reusable now in hand for L-02+:** the DORA-2025 amplifier + adoption stats (90% use AI / 80% feel-productive / 30% low-trust) from M-11.2's recording; the T-08 cost envelopes for L-04; the disparate-impact refusal (M-12.3) + M-1.6 boundaries + M-10.2/9.7 gates for L-03. Next: **L-2.1 Pilot design** (opens L-02) — picking the first squad, the first repo, the first win; largely change-management (reframe L-1.x + the measurement discipline from T-11), light on new external claims.

- 2026-07-27 — **L-2.1 built; L-02 (The Adoption Playbook) opened — leadership 5/22.** First module of the rollout track. Still reframe-only (T-11 baseline + L-1.3 portable layer + a forward-seed of L-5.3 vanity metrics), so no new verification needed — but this is the LAST fully-reframe module for a while. **⚠️ Reminder still standing:** L-2.2/L-2.3/L-2.4 stay mostly change-management (champions, enablement funnel, resistance patterns) and may reuse the M-11.2 adoption stats (90% use AI / 80% feel-productive / 30% low-trust) — but confirm those stats are still the ones on the recording sheet before quoting them, and **L-03/L-04/L-05 carry hard external claims (governance/IP, seats-vs-tokens pricing, org DORA/SPACE) that MUST get fresh doc verification.** New reusable asset created here: **SIM-29 Pilot Selection Board** (candidate-scoring pattern) — the same 4-criteria/verdict shape can extend to L-2.4 resistance-persona scoring if useful. Next: **L-2.2 Champions & cadence** (office hours, show-the-diff demos, internal course drops) — reframes L-1.1's watch-don't-type demo + the pilot's champion deliverable; light on new external claims.

- 2026-07-27 — **L-2.2 built — leadership 6/22, L-02 half done (2/4).** Followed the standing verification note: before quoting the adoption stats I re-checked m112-frameworks.txt and confirmed they're primary DORA-2025 (dora.dev, verified 2026-07-25), so this stayed a reuse, not a new claim. The 30%-distrust figure became the module's spine (trust is the bottleneck) — a clean, non-obvious hook that also sets up L-2.4 (resistance) and L-5.3 (vanity metrics), both now forward-linked in-body. New reusable asset: **SIM-30 Cadence Board** (click-to-flip mechanism↔anti-pattern card pattern — distinct from the SIM-27/28/29 expand-rows, good for any "do-this-not-that" leadership contrast). Next: **L-2.3 From 2 power users to 200** — the enablement funnel + measuring adoption honestly; reframes the pilot→champion→cadence arc into a funnel, and previews the honest-measurement discipline that L-05 formalizes. Watch: "measuring adoption honestly" must stay pull-based and refuse vanity counts (consistent with what L-2.2 just seeded) — still reframe-only, no new external claims expected, but L-03/L-04/L-05 remain the hard-verification tracks.

- 2026-07-27 — **L-2.3 built — leadership 7/22, L-02 3/4 (one module from complete).** Still reframe-only; the honest-measurement half deliberately previews L-05 (frameworks) and L-5.3 (vanity list) without stealing their verified content — it stays at the principle level (progression+outcome not seats), so no new claims. The PA-pipeline-as-funnel analogy is the strongest domain reuse yet: the queue-20% initiative that spines the whole course is literally the funnel's outcome metric, so the analogy is load-bearing, not decorative. New reusable asset: **SIM-31 Enablement Funnel** (tapering-bar + leak-marker pattern — reusable for any stage/pipeline leadership visual). Next: **L-2.4 Resistance patterns** (senior-engineer skepticism, junior over-reliance, and what to do about each) — CLOSES L-02. Reframes: the 30%-distrust stat (L-2.2), show-the-diff as the skeptic-converter, junior over-reliance ties to the review-discipline/gates (M-10.2, L-1.2 vibe-vs-spec risk) and the "AI amplifies what's already there" DORA line. Likely reframe-only, but the senior/junior failure-mode framing should stay grounded in the already-verified review + risk material, not asserted psychology. **After L-02: L-03 Governance & Guardrails opens the hard-verification zone** — permission modes/sandboxes, MCP allowlists, and IP/provenance/compliance in healthcare all need fresh doc verification (do NOT reframe-skip).

- 2026-07-27 — **L-2.4 built; L-02 (The Adoption Playbook) COMPLETE — leadership 8/22, 2 tracks done.** The whole adoption arc now reads as one coherent playbook: pilot as experiment (L-2.1) → cadence that builds trust (L-2.2) → funnel measured honestly (L-2.3) → the two people-shaped failure modes (L-2.4). Still reframe-only across all four; the mirror-image resistance framing is the sharpest reuse — it recombines the amplifier quote + risk lens + gates + the threshold-bug plant + honest measurement into a genuinely new leadership lesson without any new external claim. Verification discipline held again: re-checked the amplifier verbatim on the recording sheet before quoting. **⚠️ THE HARD-VERIFICATION ZONE STARTS NOW.** Next: **L-3.1 Permission architecture** (modes, sandboxes, checkpoints; "safe by default") — opens L-03 Governance & Guardrails. This is the FIRST leadership module with hard, driftable external claims: permission-mode names/behaviour, sandbox semantics, checkpoint/auto-accept specifics across Claude Code / Copilot / Cursor. **Must do fresh doc/tool verification — do NOT reframe-skip.** Engineer-path anchors to reuse (but re-verify currency): M-1.3/M-1.4 permission modes, T-01 responsible-AI, M-10.2/9.7 gates, M-10.3 MCP allowlists (for L-3.2). Build a recording sheet for L-3.1 like the engineer-path modules (this track earns them again). SIM assets available to extend: SIM-32's mirror-card pattern, SIM-29's scorecard, SIM-31's staged pipeline.

- 2026-07-27 — **L-3.1 built; L-03 (Governance & Guardrails) opened — leadership 9/22. First hard-verified leadership module, verification discipline HELD.** Did NOT reframe-skip: ran a fresh multi-source doc verification (Claude Code via claude-code-guide agent; Cursor + Copilot via WebFetch of primary docs) and wrote a full recording sheet (recordings/l31-permission-architecture.txt) exactly like the engineer-path modules. The payoff was real — the vendors' own words handed over the thesis (Cursor: allowlist "best-effort, not a security boundary" / "no security boundary between agents and your user account"; Copilot: "deny rules always take precedence," "use agent sandboxing … instead of relying on auto-approval rules alone"). Currency catches worth noting: Claude Code now has SIX permission modes (added auto/dontAsk; "Manual" label since v2.1.200), Copilot's allowList+denyList were MERGED into chat.tools.terminal.autoApprove, Cursor's modes are Auto-review/Allowlist/Run-Everything (3.6+). The native-Windows sandbox caveat is a genuinely useful leader-level fact (not a checkbox on a Windows fleet). New reusable asset: **SIM-33 Permission Dial** (posture-tier × tool-mode-chip pattern — reusable for L-3.x tool-capability comparisons). **Verification queue for the rest of L-03 (all need fresh doc-verify like this one):** L-3.2 MCP & data boundaries (allowlists, enterprise policy, what agents may touch — reuse M-10.3 MCP + Claude Code allowedMcpServers/deniedMcpServers, Cursor .cursor protection + Browser Controls, Copilot MCP policy); L-3.3 Reviewing AI-written code (rework radar as a control — lighter external claims, more reframe of T-11/M-10.2); L-3.4 IP/compliance/audit trail in healthcare (provenance of generated code — the HARDEST verify: IP/legal must be SOURCED not asserted; Claude Code audit-logging/compliance API exists per the brief; tie to T-01 responsible-AI + M-12.3 disparate-impact refusal). Next: **L-3.2 Permission → data: MCP & data boundaries.**

## Open questions
- ~~2026-07-21: responsible AI module~~ DONE 2026-07-22: built as M-1.6 (see status board + decision log). Remaining tail: learnings-hub catalog card still says "59 + 22 modules" — refresh to "60 + 22" next time that repo is touched (deploy branch `protected`).

## Needs verification
- M-2.2 (minor, does not block ✅): the "unavailable for every user with the API fully up" beat is proven by scratch RTL test + grep (no /me route), not by an eyes-on browser pass — Docker/WSL wouldn't start this session. Re-confirm visually next time the full stack is up (2 min: boot API, open dashboard as-is, see the unavailable card). Lab wording already matches the test-proven behavior.
- ~~M-1.1 lab success checks unverified~~ RESOLVED 2026-07-20 by the lab verification pass (see decision log): all Claude Code lab paths ran live against fresh clones of both repos; all ✅.
- M-1.1 tool claims WERE verified 2026-07-19 against current docs: Claude Code (`claude` REPL, /help /clear /exit, VS Code extension + JetBrains plugin names) and Copilot (Ask/Edit/Agent modes, @workspace context in VS Code, agent mode in Visual Studio 2026).
- ~~M-1.1 Cursor retrofit~~ DONE 2026-07-19 (labC + crew beat + checklist; renders TRI-PATH LAB). Refresh changelog:
  - Crew beat now names Cursor (AI-native IDE; Agent panel Ctrl/Cmd+I; Plan Mode) — verified against cursor.com/docs 2026-07-19.
  - labC added: Cursor repo tour using Agent panel (Ctrl/Cmd+I) and Plan Mode via Shift+Tab — both verified against cursor.com/docs. Codebase-indexing-on-open wording corroborated by multiple 2026 guides; official docs confirm codebase search tooling.
  - Checklist color line adds purple = Cursor.
  - NOT adopted: "Ask mode" in Cursor (widely described in third-party guides) is not in current official docs — current modes are Agent and Plan — so the lab deliberately avoids it.
  - needsVerification stays true: unchanged reason (teaching repos not scaffolded); lastVerified already 2026-07-19.
- ~~M-1.1 lab links dead~~ FULLY RESOLVED 2026-07-20: BOTH teaching repos are LIVE and verified loading — github.com/varasrinivas/priorauth-api (main + lab/start + 5 plant tags) and github.com/varasrinivas/priorauth-web (main + lab/start + 4 plant tags). The only remaining step before clearing any module's needsVerification: re-run each built module's labs against the real repos (M-1.1 through M-1.4) and confirm every success-check ("you should see X") holds. Candidate next session: a dedicated lab-verification pass.
- ~~Remaining tool-fit table claims~~ RESOLVED 2026-07-22 by M-2.5 (see decision log): capability claims doc-verified (Tab multi-line/cross-file/jump; NES current naming); ranking claims deliberately reframed in course copy as labeled editorial defaults with the overturn-by-measurement path — never asserted as verified fact. T-04 may add measured comparisons when built. ~~Composer naming~~ (resolved 2026-07-20: stale, avoided) · ~~.cursor/rules *.mdc format~~ (resolved 2026-07-21: VERIFIED — `.cursor/rules/` with `.mdc` files, generated via /create-rule).
- ~~M-1.4 primary lab path blocked~~ RESOLVED 2026-07-20: full day-zero flow verified live on a disposable clone of the real repo. Tool claims WERE all verified (2026-07-20, dated 07-21 below in error):
  - Claude Code /init (code.claude.com/docs): scans structure/build/test/conventions, drafts root CLAUDE.md, improves rather than overwrites, consumes existing AGENTS.md/.cursorrules; Claude Code reads ONLY CLAUDE.md natively (AGENTS.md via `@AGENTS.md` import). Corroborated by a live headless /init run.
  - Copilot (code.visualstudio.com): chat `/init` generates .github/copilot-instructions.md (old "Generate Workspace Instructions" naming is stale); `chat.useAgentsMdFile` stable+default (VS Code reads AGENTS.md and even CLAUDE.md); VS 2026 instructions file is opt-in (Tools > Options) with /generateInstructions; VS 2026 does NOT read AGENTS.md.
  - Cursor (cursor.com/docs): `.cursor/rules/*.mdc`, generated via `/create-rule` (old "/Generate Cursor Rules" naming is stale); reads AGENTS.md root+subdirs as a simple alternative; no tool recommends AGENTS.md as primary → module leads with native files.
- ~~M-1.2 lab success checks blocked~~ RESOLVED 2026-07-20 by the lab verification pass. Tool claims WERE all verified 2026-07-19 against current docs:
  - Claude Code (code.claude.com/docs): install methods (native installer + npm), permission modes incl. Shift+Tab cycle default → acceptEdits → plan, checkpoints (/rewind, double-Esc, restore code/conversation/both), --continue/--resume, /help + /clear, VS Code extension + JetBrains (beta) plugin. CLI behavior additionally confirmed live on v2.1.215.
  - Copilot (code.visualstudio.com): "Restore Checkpoint" label verbatim; restores files AND rolls conversation back; checkpoints on by default (chat.checkpoints.enabled); New Chat = own context window.
  - Cursor (cursor.com/docs): "Restore Checkpoint" label verbatim; restores FILES ONLY (no documented conversation rollback — module teaches this contrast); new tab Ctrl/Cmd+T = fresh context (side chats deliberately inherit — module says "new tab", not "new chat"). "Composer" naming confirmed stale — avoided in course copy.

- M-1.3: lab success checks reference the not-yet-scaffolded priorauth-api (same blocker as M-1.1/M-1.2). Tool claims WERE all verified 2026-07-20: VS Code agents dropdown (Agent/Plan/Ask), Edit-mode deprecation, model picker (names kept generic — offerings vary by plan; Auto exists), custom agents `.agent.md`, approval scopes + Default Approvals/Bypass Approvals/Autopilot, Keep/Undo review; VS 2026 agent mode, @debug/@profiler/@test built-ins, Restore checkpoints (no stepwise undo/redo), Total changes list, solution-scoped Allow, coarser approval controls; Claude Code /model (live-confirmed picker + aliases); Cursor mode picker Agent/Plan/Ask + model picker/Auto/Ctrl+/. Exact VS Code approval BUTTON label is deliberately not hard-coded (current docs don't pin one) — module teaches scope choices instead.
- ~~M-1.1 stale on two claims~~ RESOLVED 2026-07-21 via /refresh-module. Changelog (one line per changed claim):
  - Crew beat: "Ask, Edit, and Agent modes" → "Ask, Plan, and Agent in the agents dropdown" (Edit deprecated per VS Code v1.110+; verified 2026-07-20).
  - Crew beat: Cursor line now names the same official Ask / Plan / Agent trio (cursor.com/docs/agent/modes; supersedes the 2026-07-19 "Ask not in official docs" note).
  - labB step 1: "set the mode to Ask" → "pick Ask in the agents dropdown" (current picker naming).
  - labB steps 2–3: **@workspace removed — CHANGED upstream**: current docs use automatic codebase context + `#codebase` to force repo-wide search; @workspace survives only in 2023–2025 release notes (code.visualstudio.com/docs/copilot/reference/workspace-context, verified 2026-07-21). Lab now teaches `#codebase`.
  - Unchanged after re-check: `claude` REPL + /exit, VS Code extension/JetBrains plugin naming, Cursor Ctrl/Cmd+I panel, Shift+Tab Plan Mode, codebase indexing.
  - lastVerified bumped to 2026-07-21; needsVerification stays true (unchanged reason: teaching repos not scaffolded).

## Recordings needed
- TERM-01 (M-1.2): capture is real but ran against a minimal stand-in (recordings/TERM-01-m1.2-first-session.txt has method notes). DECISION 2026-07-20: keeping the stand-in capture deliberately — the real planted repo's answer leads with the boundary contradiction (verified in the lab pass), which belongs to T-05's story, not M-1.2's sessions/modes teaching. The replay stays clearly labeled; revisit when M-5.2 is built (its TERM replay SHOULD show the contradiction).
